---
id: TASK-00071
ticket:
kind: chore
title: Prepare develop documentation for public visibility
status: in-progress
order: 1
blocked_by:
pr:
---

# Prepare develop documentation for public visibility

## Outcome

The `develop` branch no longer publishes machine-specific user-home paths or describes Fight Agent OS as a private
repository, while preserving historical provenance and intentional references to private data, installations,
networks, and other repositories.

## Scope

- In scope: Redact tracked absolute user-home paths on `develop`; remove current-repository private-visibility claims from public documentation, project instructions, foundation wording, and package metadata; refresh generated planning views.
- Out of scope: Git history rewriting; the `planning/complete-agent-os-vision` and other open branches; references to the separate private Factory repository; private application data, deployment, network, installation, cache, and implementation semantics; changing GitHub visibility or settings.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Read the public project | N/A — documentation chore | Inspect current project status, guidance, metadata, and research citations | N/A — no domain event | Readers receive visibility-neutral current documentation without machine-specific user paths |

## Validation and permissions

Only tracked documentation, package metadata, and this standalone planning record may change. Generic relative paths
and intentional security/privacy terminology remain intact. No product runtime permission applies, and this TASK
does not authorize Git history rewriting, GitHub visibility changes, open-branch mutation, push, or publication.

## Acceptance criteria

- [x] Tracked `develop` content contains no absolute `/home/<user>` or `/Users/<user>` path.
- [x] Current project documentation, instructions, and package metadata contain no private-visibility claim.
- [x] Historical provenance and intentional references to private data, installations, networks, caches, implementation state, and the separate Factory repository remain accurate.
- [x] Generated planning views are current, focused scans and `git diff --check` pass, and `./bin/build` passes with fresh evidence.

## Verification and evidence

Inspect tracked text with focused absolute-home and repository-visibility searches. Review every remaining use of
`private` in changed contexts rather than globally replacing security and architecture terminology. Run
`./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Documentation and
metadata are verified directly; do not add product tests for wording.

## Coordination

This standalone chore runs in an isolated worktree from `develop`. The existing complete-vision planning worktree
and all remote open branches remain unrelated and untouched; their owner will scrub branch-only content separately.

## Completion notes

Implementation is complete in the isolated `feature/prepare-public-repository` worktree. `AGENTS.md`, `README.md`,
`composer.json`, `planning/FOUNDATION.md`, and the WF-006 research note now use visibility-neutral wording; the
WF-006 installed-documentation citation no longer contains a user-home path. This TASK and refreshed generated
TASK views record the bounded change. Git history, GitHub settings, the complete-vision worktree, remote open
branches, Factory provenance, and intentional privacy/security terminology were not changed.

Focused scans found zero absolute `/home/<user>`, `/Users/<user>`, or Windows user-home paths and zero current-repository
private-visibility assertions in tracked public files. Manual classification retained only intentional references
to a private source tree, private repository files, a private CA/key, the separate private Factory repository, and
this TASK's scope wording. `./bin/planning-check` passed with 97 records / 80 active records, generated views were
current, and `git diff --check` passed. The canonical `./bin/build` passed on PHP 8.5.11: Composer manifest valid;
planning 97/80; Deptrac 0 violations, 0 skipped, 0 uncovered, 33 allowed, 0 warnings, and 0 errors; PHPUnit 10.5.64
with 4 tests / 37 assertions.

The first isolated-worktree build reached the planning phase but failed because the container could not resolve
the worktree `.git` pointer. A TASK-owned ignored Compose override mounted the common Git directory read-only and
set `/app` as a safe directory; the complete rerun then passed. Installing locked dependencies in the isolated
worktree installed 106 packages and emitted the informational funding notice for 76 packages. An earlier Composer
install was mistakenly invoked from the clean base checkout: it installed nothing, regenerated ignored optimized
autoload files, emitted the existing Git dubious-ownership warning and funding notice, and changed no tracked file.
Independent review has not run. No push, pull request, merge, visibility change, or open-branch scrub is claimed.
