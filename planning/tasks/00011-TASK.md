---
id: TASK-00011
ticket: TICKET-00009
kind: chore
title: Accept the PostgreSQL consistency and durable-effects ADR
status: done
order: 11
blocked_by: TASK-00010
pr: https://github.com/johnnickell/fight-agent-os/pull/15
---

# Accept the PostgreSQL consistency and durable-effects ADR

## Outcome

An explicitly accepted architecture decision defines PostgreSQL persistence, migrations, transaction ownership, repository mapping and concurrency strategies, test isolation, and recoverable durable intent before affected schema or adapter work begins.

## Scope

- In scope: Inventory foundation repository contracts from the stable Fight Access Control release; decide PostgreSQL authority, Doctrine migration policy, shared-connection transaction ownership, contract-specific ORM/DBAL/mapping and concurrency choices, database constraints, safe conflict mapping, guarded test databases, deterministic migration/reset, and the durable-intent mechanism required before external delivery.
- Out of scope: Migrations, repository adapters, database tooling implementation, external delivery, event sourcing, HTTP/UI behavior, feature commands, and speculative persistence for deferred Agent or email-change journeys beyond state required by implemented contracts.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Decide repository persistence | N/A — ADR work | Inspect stable package repository contracts, entities, revisions, references, and ordering promises | N/A — no domain event | ADR maps each required contract to a faithful storage and concurrency strategy |
| Decide transaction ownership | N/A — ADR work | Inspect `DoctrineTransactionalUnitOfWork`, connection ownership, command handlers, and post-commit coordination | N/A — no domain event | One shared connection and canonical transaction boundary are explicit |
| Decide durable intent | N/A — ADR work | Inspect required delivery effects and commit/dispatch crash window | Package events retain their true ownership | Recoverable intent commits with business state; delivery remains post-commit and at-least-once |
| Decide test safety | N/A — ADR work | Inspect local/container environments, credentials, database naming, migration/reset needs | N/A — no domain event | Integration tooling cannot target known development or production databases |

## Validation and permissions

The ADR must make PostgreSQL authoritative for persistence and concurrency evidence, Doctrine Migrations the only schema-evolution path, and `DoctrineTransactionalUnitOfWork::commitTransactional()` the canonical transaction boundary. It must prohibit request-time schema creation, SQLite concurrency claims, and external email/HTTP/filesystem effects inside transactions.

Inventory the stable package contracts needed by the approved foundation: permissions, roles, users, refresh sessions, activation grants, password-reset grants, and audit evidence. Include the atomic `UserRepository` authority/session operation and cross-repository fences. Explain ORM mapping, targeted DBAL, optimistic revisions, pessimistic locks, conditional updates, constraints, and ordering per contract rather than imposing one strategy universally.

The durable-intent decision must define atomic creation, discovery/claiming, retry state, idempotency identity, safe payload/reference storage, retention, and recovery assumptions while assigning provider execution to TICKET-00011. It must state honest at-least-once semantics.

Test databases require separate credentials/names, explicit test mode, deterministic migrations/reset, and hard refusal of known development/production targets. Secrets remain external. No runtime user permission applies; only architecture documentation is authorized.

## Acceptance criteria

- [x] A numbered ADR records context, decision, alternatives, consequences, enforcement, and deferred concerns.
- [x] Stable Fight Access Control foundation repository contracts and cross-contract fences are inventoried.
- [x] PostgreSQL, Doctrine Migrations, the shared connection, and `DoctrineTransactionalUnitOfWork::commitTransactional()` have explicit ownership rules.
- [x] Mapping, authoritative constraints, expected-state comparison, locking/versioning/conditional-update, ordering, and safe conflict strategies are justified per contract.
- [x] Request-time schema creation, SQLite concurrency evidence, event sourcing, and external effects inside transactions are rejected.
- [x] Guarded test database identity, migration, reset, credential, and refusal requirements are explicit.
- [x] The selected durable-intent mechanism closes the commit/dispatch crash window, supports idempotent at-least-once delivery, and leaves providers/workers to TICKET-00011.
- [x] Deferred package repositories or lifecycle features are named and justified rather than accidentally implied.
- [x] A human explicitly accepts the materially revised ADR before TASK-00012 begins schema work.

## Verification and evidence

Trace decisions to EPIC-00003, WF-004, TICKET-00009, the stable package source/contracts installed by TASK-00007, and `DoctrineTransactionalUnitOfWork`. Record the repository inventory, alternatives considered, concurrency rationale, durable-intent recovery analysis, test-safety analysis, reviewer feedback, revisions, and explicit acceptance.

Run `./bin/planning-check --write`, `./bin/planning-check`, suitable link/path checks, `git diff --check`, and `./bin/build`. Record fresh results, test/assertion counts, and warnings. Verify the ADR directly rather than adding product tests for Markdown.

## Coordination

TASK-00010 must first establish accepted ownership boundaries and their enforcement. TASK-00012 and all later persistence work are blocked until this ADR is accepted. Keep draft notes under a TASK-owned `.runs/` path; publish the accepted decision in `planning/adr/`.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable architecture decision and does not authorize schema or adapter implementation.

## Completion notes

ADR [0002](../adr/0002-postgresql-consistency-and-durable-effects.md) was explicitly reaccepted by the human
maintainer on 2026-09-24 after canonical review sequence 1 exposed the locked Fight Access Control `v0.2.0`
integration defect. The accepted revision makes package-owned grant delivery state the sole durable credential
queue, rejects a consumer outbox, and blocks provider integration until Fight Access Control EPIC-00006 is
implemented and qualified, stable `v0.3.0` is tagged, and Agent OS updates its lock. TASK-00016 and TICKET-00011
remain `needs-info` on that prerequisite. PostgreSQL, migration, mapping, concurrency, guarded-test, leasing, and
honest at-least-once decisions remain unchanged.

Canonical review sequence 2 found one remaining blocker: executable invitation and password-reset records still
mandated the rejected consumer credential outbox despite the revised ADR. This revision aligns every located
concrete downstream requirement with package-owned discovery, committed claims, direct handlers, package retry and
terminal policy, stable delivery-generation idempotency, and matching package outcomes. TASK-00040 and TASK-00053
now own only product/provider mapping, canonical links, templates, catcher composition, and secret-safe invocation;
they no longer authorize intent registration, reconciliation, duplicate schemas, or pipeline outcome authority.
TASK-00037, TASK-00038, TASK-00041, TASK-00042, TASK-00054, TASK-00061, TASK-00062 and parent TICKET-00015,
TICKET-00016, TICKET-00019, and TICKET-00021 use the same package-owned model and require absence of a consumer
intent where relevant. The ADR decision itself did not change, so its recorded human reacceptance remains the
applicable acceptance evidence.

The cumulative authored implementation changes are ADR 0002 and its index; TASK-00011, TASK-00016, TASK-00022
through TASK-00024, TASK-00037, TASK-00038, TASK-00040 through TASK-00042, TASK-00053, TASK-00054, TASK-00061,
and TASK-00062; and TICKET-00011, TICKET-00015, TICKET-00016, TICKET-00019, and TICKET-00021. Earlier generated
views remain current; the sequence 2 revision changed no metadata and produced no additional generated-view diff.
No package source, dependency lock, schema, adapter, runtime configuration, provider, product code, or product test
changed.

Fresh sequence 2 verification passed: 17 direct correction assertions across 6 focal files and 14 required-path
checks passed; the concrete stale consumer-pipeline scan returned no matches; planning validation reported 95
records / 79 active with current generated views; and `git diff --check` passed. `./bin/build` passed on PHP 8.5.10
with strict Composer validation, no known vulnerability advisories, 5 PHPUnit tests / 48 assertions, a fresh
67-package production install, and all intended copied-namespace/symlink rejection probes. Composer emitted
informational funding notices for 76 development and 45 production packages. An earlier ad hoc assertion probe
exited 1 because its required phrase differed in capitalization/wording and its broad scan matched a prohibition;
no implementation file changed before the corrected passing probe. No PHPUnit warning, skip, deprecation, security
advisory, or required-check failure remained. Canonical review sequence 2 is dispositioned; independent rereview,
publication, PR, merge, release, and deployment remain pending and unclaimed.

Independent canonical review sequence 3 accepted cumulative implementation head
`bc167093bc0f5ea2ff9be9de30a3c091bed040ac` against `develop` base
`e7c1eb1346cc8d53407e274b45ccbf6d41e4a6cb`. It reported no blocking or non-blocking findings. Its limitations and
residual risks remain explicit: Fight Access Control `v0.3.0` is not yet implemented, qualified, tagged, or locked;
exact future APIs, persistence fields, PostgreSQL version, retry constants, lock-key derivation, scheduler settings,
provider enrollment, and retention remain downstream decisions. Human reacceptance is recorded in repository
history without a separate approval-system receipt.

Fresh landing pre-publication verification passed on the accepted head. The focused architecture correction probe
passed 18 assertions across 8 ADR/TICKET/TASK files; `./bin/planning-check` passed with 95 records / 79 active and
current generated views; `git diff --check develop...HEAD` passed; and `./bin/build` passed Composer validation and
advisory checks, planning validation, PHPUnit 10.5.64 on PHP 8.5.10 with 5 tests / 48 assertions, a fresh 67-package
production install, and all intended copied-namespace/symlink rejection probes. Composer emitted informational
funding notices for 76 development and 45 production packages. No required check was skipped and no PHPUnit warning,
skip, deprecation, or security advisory was reported.

Publication is pending. The intended handoff is an open pull request from
`feature/postgresql-consistency-durable-effects` to `develop`; approval and merge remain human actions, and archive,
release, deployment, certification, and local/remote branch deletion are not authorized. Cleanup preview found only
the registered isolated worktree, TASK-owned `.runs/notes/TASK-00011` logs/outlines, ignored Composer `vendor/`, and
ignored PHPUnit `var/cache/` output. No TASK-owned container, volume, process, or database was found. The shared
`fight-agent-os-build:latest` image is preserved as ambiguous. After final remote-head verification, the clean
isolated worktree and its disposable ignored contents are planned for removal while the local feature branch and
canonical base-worktree review history are retained.

Landing commit `07754e293797a4cf4d24a0c12e58560bce3e0a9c` was pushed without force to
`origin/feature/postgresql-consistency-durable-effects`, and GitHub opened
[PR #15](https://github.com/johnnickell/fight-agent-os/pull/15) against `develop`. Final metadata verification, final
push, remote-head verification, cleanup, and human approval/merge remained pending at this point.

Post-publication metadata verification passed before the final metadata commit: the focused probe again passed 18
assertions across 8 files; planning validation again reported 95 records / 79 active with current generated views;
`git diff --check` passed; and `./bin/build` again passed Composer validation/advisories, PHPUnit 5 tests / 48
assertions, the fresh 67-package production install, and all intended production-contract negative probes. Funding
notices remained informational at 76 development and 45 production packages; no required check, warning, skip,
deprecation, or security advisory was hidden. Final metadata commit, final push, GitHub head verification, and owned
worktree cleanup remained pending at this point.
