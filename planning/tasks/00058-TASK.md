---
id: TASK-00058
ticket: TICKET-00020
kind: feature
title: Deliver confirmed revocation of another owned session
status: ready-for-agent
order: 58
blocked_by: TASK-00057
pr:
---

# Deliver confirmed revocation of another owned session

## Outcome

An authenticated user can explicitly confirm and revoke one other usable owned refresh session through package `RevokeSession`; actor and current session remain principal-derived, current-session revocation directs to Logout, unsafe target outcomes do not enumerate cross-user state, the authoritative list converges, and applicable same-origin tabs invalidate without credential persistence.

## Scope

- In scope: Versioned Bearer-authenticated revoke Action/validated target route parameter/safe outcome/OpenAPI; principal-derived actor/current session; deny-all cross-user administration composition; package `RevokeSession` dispatch; current-session safe conflict; generic nonexistent/cross-user/expired/revoked/conflict mapping; explicit client confirmation and pending/result states; list invalidation/refetch; versioned session-revoked/list-invalidated cross-tab signal; volatile self-session comparison and terminal handling; PostgreSQL ownership/race/refresh/access-token/HTTP/client/accessibility/redaction tests.
- Out of scope: Current-session revocation outside Logout, logout everywhere, cross-user Super Admin session administration, administrative reason input, raw refresh credentials, editing lifetimes, guaranteed cross-device push, persistent session coordination, mass selection, and broad browser automation.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Revoke another owned session | `RevokeSession(actorId, currentSessionId, targetSessionId, null)` | Resolve principal and target package session ownership/usability | `RefreshSessionRevoked` | Target revokes atomically; current session remains usable; list invalidates/refetches |
| Reject current-session target | Same command or pre-dispatch safe guard without replacing package authority | Compare principal current ID with target | Package current-session rejection | No mutation; client directs user to established Logout journey |
| Converge after race | Same command may meet revoked/expired/concurrently changed target | Requery authoritative active-session list | Existing package failure/event only | Generic result followed by refetch removes stale row without revealing target ownership |
| Propagate revocation signal | N/A — client coordination after accepted outcome/event response | Compare target ID with volatile current session identity where available | Versioned client-only invalidation/terminal signal | Same-origin lists refetch; matching target tab fails closed; no cross-device guarantee is claimed |

## Validation and permissions

Require valid Bearer authority and one request-scoped `CurrentPrincipalProvider`. Accept only one bounded canonical target session ID from the versioned route; body cannot override actor/user/current session/reason/role/permission or request administrative behavior. Construct package command with actor from principal, current session from principal, target from validated route, and null reason. Authenticated ownership is the only authority.

Compose package `SessionAdministrationAuthorization` to deny cross-user administration for this installation feature. Do not infer permission from role or expose a hidden administrator path. Package handler remains authoritative for usable target, current-session prohibition, ownership/administrative denial, optimistic replacement, audit rules, and event. Do not directly update repository/session flags.

Current-session target receives an explicit safe conflict code/message directing to Logout because the caller already receives its current marker through TASK-00057; no mutation occurs. Cross-user valid ID, nonexistent, expired, already revoked, and non-current competing revocation must share an allowlisted non-enumerating public class/status/body. Malformed identifier can receive transport validation without lookup. On success or safe stale/conflict outcome, invalidate and requery list rather than optimistic fabrication.

Require an explicit accessible confirmation naming only safe coarse descriptor and relative/absolute time needed to distinguish the target. Never place credential/digest or unescaped raw metadata in dialog. Disable duplicate submission while pending, manage focus into/return from dialog, announce result, and preserve a retry/refetch path. Current row exposes Logout rather than this action.

After successful server commit, emit a versioned bounded same-origin coordination signal containing only approved session ID/generation/invalidation semantics. Every tab invalidates/refetches session lists. If a tab's existing volatile authentication coordinator safely knows its own stable session ID and it equals the target, clear memory/principal and enter terminal Login; do not parse JWT as trusted state or persist the ID. Different current tabs remain authenticated. Other browsers/devices receive no BroadcastChannel promise, but TASK-00044's authoritative session resolution rejects their access token immediately and refresh rejects credential immediately.

No session ID/token/authority/coordination state enters `localStorage`, `sessionStorage`, IndexedDB, Cache Storage, service workers, URL query, or HTML. Session ID remains a safe opaque transport identifier but is redacted from unnecessary diagnostics and cannot authorize without current principal ownership.

## Acceptance criteria

- [ ] Versioned revoke API requires Bearer authentication and accepts only a validated target session ID.
- [ ] Actor and current session derive only from request-scoped principal; body cannot override identity/session/reason/authority.
- [ ] Package `RevokeSession` owns target usability, current prohibition, ownership, optimistic revocation, and `RefreshSessionRevoked` emission.
- [ ] Cross-user administration is explicitly denied with no role/permission bypass or administrative-reason path.
- [ ] Current session cannot be revoked through this endpoint and receives a safe explicit instruction to use Logout.
- [ ] Cross-user/nonexistent/expired/already-revoked/competing targets share a non-enumerating public outcome and converge after list refetch.
- [ ] Successful revocation atomically revokes only target; current session remains authoritative and no credential/hash changes.
- [ ] Revoked target refresh fails immediately and validly signed target access token fails authoritative session resolution before JWT expiry.
- [ ] Confirmation is explicit, uses only safe coarse target presentation, prevents accidental/duplicate activation, and manages focus/status accessibly.
- [ ] Success/safe conflict invalidates/refetches active-session lists across same-origin tabs using bounded versioned messages.
- [ ] A volatile matching target tab fails closed where identifiable; no cross-device broadcast or exactly-immediate UI claim is made.
- [ ] Session/token/authority/coordination data is absent from prohibited browser persistence and unnecessary diagnostics.
- [ ] Responsive confirmation/pending/success/conflict/error states meet accepted keyboard/focus/announcement/theme/reflow requirements.
- [ ] OpenAPI truthfully describes ownership, current-session exclusion, generic target failures, and no cross-user/logout-everywhere capability.
- [ ] Focused PostgreSQL/concurrency/ownership/refresh/access-token/HTTP/cross-tab/component/accessibility/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Create current, other-owned, cross-user, revoked, idle-expired, absolute-expired, and concurrently rotating sessions with deterministic IDs/clocks. Exercise valid revoke, current target, malformed/unknown/cross-user/repeated target, and simultaneous revoke/refresh/logout/expiry using separate PostgreSQL connections and controlled synchronization without sleeps. Compare generic public outcomes and prove only one package-valid commit/event. Verify current refresh/access continues, target refresh fails, and target access token receives sanitized `401` immediately.

Drive confirmation keyboard/focus/cancel/confirm/duplicate/error/refetch behavior with safe and hostile metadata. Use controlled channels/promises to test list invalidation in multiple tabs, target/non-target volatile identity, malformed/duplicate/out-of-order signals, unmount/cleanup, and unsupported BroadcastChannel fallback. Record cross-device limitation. Seed session/token/cookie/digest/raw-UA/IP/SQL markers and inspect responses, OpenAPI, logs, audit, snapshots, DOM/messages/URLs/history and browser storage.

Run focused backend architecture/style/static/coverage, PostgreSQL/concurrency/HTTP/OpenAPI/security tests, frontend type/lint/Vitest/RTL/Storybook/build and minimal channel smoke where needed, accessibility automation plus manual keyboard/reflow/theme observations, storage/redaction scans, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record API/outcome/message contracts, race/state matrix, tests/assertions/scenarios, accessibility limits, redaction evidence, versions, durations, and warnings.

## Coordination

TASK-00057 supplies authoritative list and coarse target presentation; TASK-00049 supplies principal; TASK-00044/00047 make revoked access/refresh fail; TASK-00048 supplies volatile cross-tab coordination; TASK-00051 supplies shell states. Preserve current-session Logout in TASK-00047. Cross-user administration and logout-everywhere remain deliberately deferred.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable other-owned-session revocation outcome.

## Completion notes

Not started. Record Action/outcome/OpenAPI, package authorization composition, confirmation/list/message integration, ownership/race/token/accessibility/storage/redaction evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and deferred cross-device/administrative capabilities here.
