---
id: TASK-00008
ticket: TICKET-00007
kind: chore
title: Replace inherited receipts with the Agent OS smoke baseline
status: done
order: 8
blocked_by: TASK-00007
pr:
---

# Replace inherited receipts with the Agent OS smoke baseline

## Outcome

The test and production-contract baseline proves a small truthful Agent OS HTTP boundary rather than inherited Fight Common support compatibility. Application-owned root, unknown-route, and sanitized-error smoke evidence replaces obsolete fixtures, while the production source contract permits owned Domain/Application code and rejects copied Fight package source or namespaces.

## Scope

- In scope: Add deterministic Agent OS-owned root, unknown-route, and sanitized unknown-error smoke coverage; preserve correlation/logging evidence appropriate to the existing HTTP boundary; remove inherited support-campaign tests and fixtures after useful coverage exists; simplify test bootstrap; replace/rename the obsolete production public-contract check; permit legitimate `App\Domain` and `App\Application` code; reject copied `Fight\Common` and `Fight\AccessControl` source/namespaces; continue checking required production packages, autoload, and development exclusions.
- Out of scope: Product features, authentication journeys, reproducing upstream package suites, broad provider/dependency pruning, HTTP API architecture expansion owned by TICKET-00010, upstream changes, and the complete quality gate owned by TICKET-00013.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Smoke the application root | N/A — HTTP test interaction | Request the real root through application composition | N/A — no application domain event | Response proves owned boot/routing/presentation behavior without support-only fixtures |
| Handle an unknown route | N/A — HTTP test interaction | Request an unregistered application path | N/A — no application domain event | Application returns the configured safe not-found response without internal leakage |
| Sanitize an unknown failure | N/A — HTTP middleware test interaction | Exercise an owned test boundary that throws an unexpected error and inspect response/log context | N/A — no application domain event | Client receives generic safe failure while logs retain correlation context, not sensitive response detail |
| Enforce production source boundaries | N/A — production contract tooling | Inspect production package/autoload state and owned source paths/namespaces | N/A — no application domain event | Valid `App` Domain/Application code passes; copied Fight package source/namespaces fail |
| Retire inherited receipts | N/A — repository test cleanup | Trace tests/fixtures to application behavior or former support campaign | N/A — no application domain event | Support-only journeys disappear only after useful owned smoke evidence is green |

## Validation and permissions

Smoke tests must use real Agent OS composition where practical, remain deterministic, and assert status, content type/envelope where applicable, sanitized output, and correlation/log behavior without requiring production-only failure routes. Test-only failure injection must be explicitly scoped and unavailable in production composition. Unknown exceptions must never expose arbitrary messages, traces, paths, secrets, or provider details.

Remove support-only Integration journeys and Messaging, Serialization, Socket, Security, and HTTP fixtures according to verified provenance; retain or rewrite only tests that describe current application behavior. Simplify `tests/bootstrap.php` to actual test needs. Do not replace inherited breadth with tests of Markdown, wrapper text, or upstream package behavior.

The production contract must allow `src/Domain` and `src/Application` under the `App\` namespace, continue proving required Fight packages and production autoload are present, prove development packages are absent, and reject vendored/copied `Fight\Common` or `Fight\AccessControl` implementation beneath owned source. Contract checks must fail clearly and without mutating the tree.

No runtime permission model applies. Approved scope permits application smoke tests, test-fixture removal, and production-contract/build updates; it grants no product behavior, upstream modification, or security-policy expansion.

## Acceptance criteria

- [x] Agent OS-owned smoke tests cover real root behavior, unknown routes, and generic sanitized unknown-error responses.
- [x] Unknown-error evidence proves appropriate correlation/log context while client output excludes arbitrary exception messages, traces, paths, secrets, and internals.
- [x] Test-only error injection is isolated from production routes/composition.
- [x] Inherited support-campaign tests and fixtures are removed only after equivalent useful application boundaries are covered.
- [x] Remaining tests describe Agent OS behavior rather than Fight Common or starter capability conformance, and test bootstrap contains only application-required setup.
- [x] The renamed/reworked production contract accepts legitimate `App\Domain` and `App\Application` source and production autoload.
- [x] The contract demonstrably rejects copied `Fight\Common` and `Fight\AccessControl` source/namespaces beneath owned production paths.
- [x] Production checks continue proving required Fight dependencies are installed and development-only packages are excluded.
- [x] Focused smoke and contract checks and the full canonical build pass with fresh reduced-but-truthful test/assertion counts.

## Verification and evidence

First record the inherited test/fixture inventory and map every removal to support-campaign provenance or replacement coverage. Add and run focused HTTP tests before deleting obsolete evidence. Capture expected status/body/content-type and safe logging/correlation assertions for root, unknown route, and unknown failure. Directly exercise the production contract against the real production copy plus owned temporary positive/negative fixtures without adding product tests solely for script text.

Run focused PHPUnit smoke tests and production-contract checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record before/after test and assertion counts, removed files, retained boundaries, production package/dev-exclusion results, negative copied-namespace proof, warnings, and incomplete verification. A lower test count is acceptable only when the remaining evidence is more truthful and all required boundaries pass.

## Coordination

TASK-00007 must first establish and prove the stable locked dependency graph. Preserve its canonical dependency/install checks while changing smoke and production-contract behavior. Keep contract fixtures and diagnostic output under a TASK-owned `.runs/` path and preserve unrelated tests or local work.

This TASK owns one independently reviewable smoke/contract-cleanup outcome. Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen, and arrange independent review before landing.

## Completion notes

Replaced the inherited support receipt with `ApplicationSmokeTest`, which exercises the real application composition. The root now returns `200`, `text/plain; charset=utf-8`, and the owned `Fight Agent OS is ready.` body. An unknown route returns the existing safe `404` JSend envelope. A test-local route throws an exception containing credential, path, and provider-like detail; production composition proves that route is absent, while the injected request returns the exact generic `500` JSend envelope, `no-store`, and a 32-character correlation header. The matching error log retains method, path, correlation ID, and the original exception without returning those internals to the client. Both HTTP middleware classes now live under `src/Adapter/Http/Middleware`.

Recorded the inherited inventory before removal. Deleted `BootstrapTest`, both inherited Functional classes, all eight Integration journey/contract classes, and the HTTP, Messaging, Security, Serialization, and Socket fixtures after the new focused smoke suite passed 3 tests / 22 assertions. The removed baseline was 26 tests / 152 assertions. `tests/bootstrap.php` now loads only Composer autoload; the remaining suite is the three application-owned smoke tests and passes 3 tests / 22 assertions.

Renamed `scripts/public-contract-check.php` to `scripts/production-contract-check.php` and replaced the obsolete Domain/Application directory prohibition. The production contract now requires Fight Common and Fight Access Control, rejects PHPUnit in production, verifies the production `App\\` PSR-4 mapping to `src`, permits positive `App\Domain` and `App\Application` fixtures, and rejects reserved copied-source paths or namespaces for both `Fight\Common` and `Fight\AccessControl`. `bin/build` exercises the real clean production copy, both positive namespaces, and both negative copied-package namespace cases without mutating the checkout.

Fresh focused syntax checks passed for the contract, smoke test, and middleware. Focused smoke passed before inherited removal and again after cleanup and middleware relocation: 3 tests / 22 assertions. A TASK-owned clean production copy installed 67 packages without development dependencies; the real and positive-fixture contracts passed, both copied-namespace fixtures failed with the expected diagnostics, and the clean contract passed again after fixture removal. Final planning validation passed with 95 records and 82 active records, and `git diff --check` passed. The final canonical `./bin/build` passed on PHP 8.5.10: strict Composer validation, locked development install, advisory audit with no known vulnerabilities, current planning validation, 3 tests / 22 assertions, clean 67-package production install, required packages, development exclusion, production autoload/source checks, and both negative namespace proofs.

The first focused smoke attempt failed because Slim cannot bind a static test-route closure; changing only that test-local closure to a bindable closure produced the recorded passing rerun. The first exploratory TASK-owned production-copy command reported an unreadable host `var/` path and Composer Git safe-directory diagnostics; the subsequent contract checks passed, and the canonical build's clean copy/install superseded that setup evidence without either diagnostic. Composer printed informational funding messages for 69 development and 45 production packages. No final test warning, deprecation, advisory, or unresolved acceptance item remains. Independent review is pending; no PR, merge, release, deployment, or broad application-readiness claim is made.

### Revision sequence 1

Resolved review sequence 1's blocking high finding under human-authorized revision scope. A new regression test reproduced the arbitrary-message generic Slim HTTP exception leak before repair: the focused run failed with 4 tests / 26 assertions because the response omitted `Cache-Control: no-store`. `HttpExceptionMiddleware` now returns only fixed public messages for explicitly mapped Slim client exceptions and rethrows generic, unmapped, and server-error HTTP exceptions to `UnexpectedErrorMiddleware`, which provides the generic `500` envelope, `no-store`, correlation ID, and matching structured error log without returning the original exception detail.

Also completed the separately human-authorized rename from `SlimHttpExceptionMiddleware` to `HttpExceptionMiddleware`, including the file, class, constructor docblock, import, and composition reference. This rename does not broaden runtime behavior beyond the required error-handling correction. The repaired focused smoke suite passes 4 tests / 37 assertions, and focused syntax checks pass for the renamed middleware, composition file, and smoke test. `./bin/planning-check --write` refreshed generated views, read-only planning validation passed with 95 records / 82 active, and `git diff --check` passed. The final canonical `./bin/build` passed on PHP 8.5.10: strict Composer validation, locked development install, advisory audit with no known vulnerabilities, current planning validation, 4 tests / 37 assertions, clean 67-package production install, production dependency/source checks, and both expected copied-namespace negative proofs. Composer printed informational funding messages for 69 development and 45 production packages. An initial final-inspection name guard exited 1 because it also searched the required completion-note record of the old name; the corrected runtime/test-only guard passed with no stale reference. No PHPUnit warning, skip, deprecation, security advisory, required-check failure, or unresolved acceptance item remains.

The review finding is resolved and the authorized rename is complete. Independent re-review is pending; review acceptance, push, PR publication, landing, merge, archive, release, and deployment remain separate.
