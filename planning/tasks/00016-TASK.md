---
id: TASK-00016
ticket: TICKET-00009
kind: feature
title: Establish atomic audit evidence and package delivery persistence
status: ready-for-agent
order: 16
blocked_by: TASK-00014, TASK-00015
pr:
---

# Establish atomic audit evidence and package delivery persistence

## Outcome

Agent OS persists package audit evidence and the qualified Fight Access Control credential-delivery lifecycle on the
shared PostgreSQL connection, proving originating state, encrypted delivery work, and secret-safe evidence commit or
roll back together while claims, outcomes, and recovery obey package-owned expected-state rules.

## Scope

- In scope: After qualified Fight Access Control `v0.3.0` is tagged and locked, audit-evidence migration and production `AuditEvidenceRepository`; subject typing for user/agent evidence without inappropriate references; safe context serialization; package-compatible email-change grant/delivery persistence; cross-family secret-free due-work discovery; claim/lease/retry/outcome persistence needed by invitation, password-reset, and email-change repositories; atomicity, restart recovery, and concurrency tests.
- Out of scope: Provider implementation or invocation, worker/scheduler operation, product email-change routes/provider wiring, HTTP/UI, consumer credential outbox, event sourcing, exactly-once claims, broad audit query/admin UI, external audit publication, and Agent persistence.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Record audit evidence | Package/application sensitive behavior calls `AuditEvidenceRepository` | N/A — append-only foundation contract | Preserve the originating package/application event | Secret-free actor/action/subject/context evidence commits with owning state |
| Persist required credential work | Qualified package originating handlers write grant-owned delivery state through their repositories | N/A during originating transaction | Package events may coordinate only after commit | Aggregate, encrypted delivery material, and audit evidence commit or roll back together without a second outbox |
| Discover and claim due work | Direct package processing will consume repository contracts | Select bounded pending, due-retry, and expired-lease generations in package-defined order | Original post-commit event is optional | One exact generation gains a committed opaque claim token/lease and expected revision |
| Persist retry/final outcome | Direct package handler records its typed provider outcome later | Compare delivery ID, claim token, lease, revision, and current generation | Package outcome events remain package-owned | Stale workers cannot overwrite current state; retry timing and terminal ciphertext destruction match package policy |

## Validation and permissions

Audit storage preserves actor ID, action, explicit subject type and ID, and approved string context. It must support package `UserId|AgentId` subjects without requiring an Agent row and without recording passwords, credentials, tokens, hashes, ciphertext, provider payload secrets, or arbitrary exception data. Context serialization is deterministic and bounded.

Credential delivery persistence implements the exact locked `v0.3.0` package contracts; it does not recreate their lifecycle in Agent OS. Secret-free discovery is deterministic and bounded. Claims persist token, lease, attempts, and expected revision before any provider call. Retryable outcomes use package-calculated bounded increasing backoff until grant expiry. Delivered, permanent, expired, replaced, revoked, and cancelled terminal states destroy ciphertext when required. No generic credential outbox is created.

All originating business/grant/audit writes use the same connection and enclosing `commitTransactional()` call. Claim and outcome writes use later short transactions. Provider execution must not occur inside this TASK. Concurrency controls reject stale generations, claim races, expired claim tokens, and outcomes from superseded workers according to the package contract.

No end-user permission is introduced. Persistence composition is authorized; providers, direct processing, recovery scheduling, and product delivery flows belong to TICKET-00011.

## Acceptance criteria

- [ ] Versioned `snake_case` migrations create bounded secret-safe audit evidence plus exact locked-package delivery fields, indexes, and constraints without a duplicate credential outbox.
- [ ] A production adapter implements `AuditEvidenceRepository` directly and supports user and agent subjects without requiring Agent persistence.
- [ ] Audit actor/action/subject/context round-trip exactly and reject or safely handle invalid, oversized, or secret-bearing data.
- [ ] Activation, password-reset, and email-change adapters satisfy the qualified package due-work, claim/lease, retry, terminal, and expected-outcome contracts on the shared connection.
- [ ] Originating aggregate/grant state, encrypted delivery material, and audit evidence commit atomically or roll back together.
- [ ] Deterministic discovery finds newly committed pending work after simulated loss of the post-commit event.
- [ ] Controlled competing claims permit one live owner; expired leases recover; stale outcomes cannot overwrite a current generation.
- [ ] Retry timing, grant-expiry boundary, delivered/permanent terminalization, and ciphertext destruction follow package policy rather than consumer configuration.
- [ ] Safe projections and diagnostics expose no credentials, hashes, tokens, ciphertext, URLs, provider secrets, or arbitrary errors.
- [ ] Focused migration/repository/transaction/concurrency checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Use the guarded PostgreSQL lifecycle from TASK-00012. Run audit contract tests for user/agent subjects, context bounds, secret-negative fixtures, rollback, and append behavior. Run the qualified package repository/conformance suites for all three credential families. Prove originating rollback, deterministic restart discovery, claim contention, lease recovery, stale-outcome rejection, retry-until-expiry, terminal ciphertext destruction, and purpose separation with independent controlled connections and deterministic clocks.

Run focused database/capability tests and architecture checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record migrations/schema, locked package version/contracts, PostgreSQL/Doctrine versions, test/assertion counts, transaction/claim/recovery outcomes, redaction evidence, warnings, and intentional provider deferrals.

## Coordination

Fight Access Control `v0.3.0` is tagged and locked by TASK-00012's PR revision. Its published migration guide and directly inspected grant repository contracts confirm package-owned due-work discovery, claim/outcome state and direct processing. This resolves the information gate; TASK-00014 and TASK-00015 remain implementation blockers. Do not patch `vendor/`, add a consumer credential outbox, nest an Agent OS transaction, use a post-commit subscriber as recovery authority, configure consumer retry policy, or duplicate package transition rules.

TASK-00014 and TASK-00015 supply the activation and password-reset repository foundations. This TASK adds audit evidence, qualified cross-family recovery persistence, and email-change contract compatibility while product/provider email-change wiring remains deferred. TICKET-00011 must dispatch direct package handlers over these adapters rather than inventing another queue.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable audit/package-delivery persistence outcome.

## Completion notes

Not started. The `v0.3.0` tag at `22ffab6452b2278b82df9b147e8949630b97355f` is locked in TASK-00012's PR revision. Its `docs/credential-delivery-v0.3-migration.md` and the three grant repository contracts provide the package-owned due-work/claim/outcome contract; the release guide lists upstream qualification cases, not fresh Agent OS adapter evidence. Implementation still waits for TASK-00014 and TASK-00015. Record migrations, adapters/contracts, atomicity/claim/restart/redaction evidence, changed files, focused/canonical verification with counts, warnings, review state, commit/PR details, and deferred TICKET-00011 work here.
