---
id: TASK-00020
ticket: TICKET-00010
kind: feature
title: Centralize correlated and sanitized API failures
status: done
order: 20
blocked_by: TASK-00019
pr: https://github.com/johnnickell/fight-agent-os/pull/32
---

# Centralize correlated and sanitized API failures

## Outcome

One centralized HTTP failure boundary classifies known validation, authentication, authorization, conflict, explicit not-found, and framework routing failures while producing correlated secret-safe diagnostics and generic sanitized responses for unknown throwables.

## Scope

- In scope: Explicit exception-to-status/JSend classification; transport and Domain validation categories; authentication/authorization/conflict/explicit resource-not-found mappings; unknown route/method handling; explicit exclusion of generic `LookupException` from automatic 404; correlation ID validation/generation/propagation policy; PSR logging of unknown failures; safe generic 500; middleware ordering; representative functional tests.
- Out of scope: Product-specific exceptions/endpoints, authentication implementation, observability backend, tracing platform, CORS, debug UI, exposing stack traces, and broad retry policy.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Map known failure | No additional command | Inspect explicit exception category/context | No new event | Documented safe status and JSend envelope are returned centrally |
| Handle route/method miss | No command | Inspect framework HTTP exception safely | No event | Sanitized not-found/method response contains no arbitrary framework message |
| Handle unknown throwable | No additional command | Validate/generate correlation and inspect throwable for logs only | No event | Generic 500 reaches client; structured secret-safe correlated diagnostic is logged |
| Reject malicious correlation input | No command | Validate allowed format/length and trust policy | No event | Invalid external value is replaced/rejected and cannot inject logs/headers |

## Validation and permissions

Build an explicit allowlisted mapping rather than status inference from arbitrary exception code/message. Keep malformed transport and declared validation distinct where the API contract requires, while using safe envelopes. Authentication, authorization, stale/conflict, and resource-not-found mapping must rely on explicit types/context. Generic `LookupException` must not become 404 automatically.

Unknown throwable responses may contain only the generic public message and correlation reference allowed by policy. Never return arbitrary messages, traces, file paths, SQL, credentials, grants, tokens, payloads, provider details, internal IDs, or debug data. Logs retain sufficient exception/correlation/request metadata for diagnosis but redact credentials/bodies/sensitive headers and resist newline/structured-log injection.

Define accepted inbound correlation format/length, generation, replacement, response header/body behavior, propagation, and trusted/untrusted handling. Middleware order must catch Action, validation, JSON, and Slim routing failures without converting known client errors to generic 500 or leaking framework text.

No user-facing permission is introduced. Central error and logging composition are authorized; product handlers are not.

## Acceptance criteria

- [x] A centralized allowlisted mapper defines safe statuses/envelopes for transport/Domain validation, authentication, authorization, conflict/stale state, and explicit resource not found.
- [x] Generic `LookupException` is demonstrably excluded from automatic 404 mapping.
- [x] Unknown routes and unsupported methods return deterministic sanitized API responses.
- [x] Unknown throwables return generic 500 output without arbitrary exception/internal/sensitive data.
- [x] Unknown failures produce structured logs with sufficient correlation and exception context but no request credentials/body or log injection.
- [x] Correlation IDs are validated/generated under explicit format, length, trust, propagation, response, and logging rules.
- [x] Malformed/oversized/injection correlation values cannot control diagnostics or response headers unsafely.
- [x] Middleware ordering preserves TASK-00019 validation and TASK-00018 successful responses.
- [x] Production configuration cannot enable stack traces or arbitrary exception-message responses.
- [x] Focused mapping/logging/functional checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Create a mapping matrix with representative allowed exception types plus generic lookup and unknown throwables. Assert status/content type/JSend/public fields and forbidden substrings. Capture logs to prove correlation and useful exception classification while checking redaction for credentials, authorization/cookie headers, bodies, control characters, SQL, paths, and grant-like values. Exercise route/method misses and malformed correlation values through the real kernel.

Run focused HTTP/logging tests and architecture checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record mapping matrix, middleware order, test/assertion counts, log/redaction evidence, warnings, and known exception types deferred to feature TICKETs.

## Coordination

TASK-00019 supplies transport-validation behavior and TASK-00018 supplies the real CSRF bootstrap composition. Prove each supported exception mapping at the mapper boundary, but claim end-to-end HTTP evidence only where a real operation can produce it; later activation/login/protected Actions own their actual failure-path integration. TASK-00021 documents only implemented HTTP behavior. Preserve TASK-00008 smoke behavior while replacing inherited unsafe middleware behavior; do not create test-only product routes or broaden mappings speculatively.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable centralized API-failure boundary.

## Completion notes

Implemented on the human-selected clean main checkout from `develop` `ef351440` on `feature/correlated-api-failures`; no canonical `.runs/reviews/TASK-00020/review.md` was present. Replaced the inherited two HTTP error middleware with one outermost `ApiFailureBoundary`, before routing, group validation and Actions. The allowlisted `ApiFailureMapper` classifies declared transport validation as 400 JSend `fail`, Domain validation as 422 JSend `fail`, explicit authentication/permission/conflict/resource exceptions as 401/403/409/404, and named Slim routing/client classes as 400/401/403/404/405/410/429 JSend `error`. All returned messages and validation field labels are fixed; upstream exception messages, validation fields, codes and payloads are never projected. Generic `LookupException`, generic `HttpException` (even with a 404 code), other exceptions and unknown throwables become generic 500 errors; optimistic event concurrency is an explicitly recognized conflict. Application-level failure markers are transport-free; later feature owners must choose only contextually appropriate markers. Persistence-specific and Access Control feature exceptions are not broadly mapped by class without an owned use-case decision.

Correlation policy: an inbound `X-Correlation-ID` is accepted as a *diagnostic reference only* when exactly one header value is 32 lowercase hexadecimal characters; otherwise a fresh random 128-bit hex ID replaces it. No client-provided ID grants authority. The boundary propagates that ID to downstream request attributes (`correlation_id`) and overwrites the response header on success, direct validation rejects, routing misses and exception responses. No response body carries the ID; unknown bodies contain only `Internal server error.`. A dedicated Monolog JSON-to-stderr error logger, separate from the inherited NullHandler for other messages, receives only the validated ID, `client`/`generated` source, an uppercase bounded method or `UNKNOWN`, and the exception's PHP type. No request URI/path/query, headers, cookies, body, throwable object, message or stack enters its context. JSON formatter newline escaping and bounded context prevent structured-log injection; PSR-7 rejects literal CR/LF header values before middleware. HTTP failures use safe JSend with JSON content type and `no-store`; there is no debug/show-details switch in this boundary.

Changed code: `config/middleware.php`, the new mapper/classification/boundary under `src/Adapter/Http/Middleware/`, four transport-free `src/Application/Failure/` markers, and removal of the two inherited error middleware. Tests: mapper matrix/lookup/code exclusion and direct boundary logging/redaction/correlation tests in `tests/Unit/Http/`, with real-kernel root, missing route, unsupported method, valid bootstrap and rejected bootstrap in `tests/Functional/Http/`; no test-only product route was added. End-to-end throwable logging, Domain validation and future authenticated/permission/conflict/not-found Action paths cannot be claimed until a real operation raises them. A literal CR/LF header was rejected by Slim PSR-7 before the boundary in an early test run; replaced the test with a syntactically valid structured-log-injection value. An early style run found docblock/array alignment errors, corrected; static, Rector and style then passed.

Fresh focused PHPUnit passed 26 tests/60 assertions, including JSON-formatted error-log redaction. Backend syntax passed 108 files/0 failures; style, PHPStan, Rector, architecture (0 violations/skips/uncovered/warnings/errors; 651 allowed) and `git diff --check` passed. Initial full `./bin/build` passed on PHP 8.5.10/PHPUnit 10.5.64: Composer valid, planning 170 records/134 active/current views, architecture 0 violations/skips/uncovered/warnings/errors (651 allowed), PHPUnit 145 tests/1119 assertions. After recording completion, `./bin/planning-check --write`, read-only validation and `git diff --check` passed; final done-status `./bin/build` passed with 170 records/133 active/current views, architecture 0 violations/skips/uncovered/warnings/errors (651 allowed), PHPUnit 145 tests/1126 assertions on the final application tree. No gate warnings, skips or deprecations reported. Independent review accepted the clean `feature/correlated-api-failures` commit `c8aeb2759fef6a9806b91c3304855dfe17489365` against `develop` `ef3514407615154d5659235ba7547437675b9ea8` in the canonical ignored `.runs/reviews/TASK-00020/review.md` (version 3). No blocking or non-blocking findings require disposition. The reviewer limited end-to-end evidence to existing routes; future authenticated, authorization, conflict and resource-not-found Actions must prove their integrations when implemented. Before publication, landing independently reran focused HTTP tests (26 tests/60 assertions), read-only planning validation (170 records/133 active), `git diff --check`, and `./bin/build` (145 tests/1126 assertions; architecture 0 violations/skips/uncovered/warnings/errors, 651 allowed); all passed without reported warnings, deprecations or skips. The reviewed base and current local/remote `develop` are identical; no reconciliation or implementation changes were needed. Published the accepted branch without force as [PR #32](https://github.com/johnnickell/fight-agent-os/pull/32) against `develop`; review provenance stays anchored to `c8aeb275` and only TASK review/PR metadata followed it. Human approval and merge, release and deployment remain pending.
