---
id: TASK-00126
ticket: TICKET-00031
kind: chore
title: Qualify the upstream v0.4.0 authorization contract and consumer update
status: needs-info
order: 120
blocked_by: TASK-00036
pr:
---

# Qualify the upstream v0.4.0 authorization contract and consumer update

## Outcome

A deliberate, tagged Fight Access Control consumer update replaces assumed handler authorization with the verified v0.4.0 contract while preserving Agent OS permission and package ownership/target invariants; later role-permission delegation and user-role assignment can then depend on exact evidence rather than a recommendation.

## Scope

- In scope: Inspect the owning project's accepted decision, tagged release and changelog/API; compare locked v0.3.0 port/handler/tier behavior; deliberate composer-lock update; adapt Agent OS Application authorization composition and the retained cross-user session/email-change/pending-invitation-correction actor/target capabilities; adapt the consumer nullable `permissions.tier` schema and PostgreSQL repository to the proposed non-null package model, with guarded migration only after actual target inventory; verify intrinsic managed-role/reference/tier/Agent grant rules and fail-closed behavior with owned integration contracts; record upgrade compatibility and warnings.
- Out of scope: Editing Fight Access Control source/vendor, assuming the recommendation was accepted, adding product tests of package internals, converting tier labels into authorization by assertion, adding public routes, removing ownership callbacks or letting unguarded package handlers become user-accessible.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Qualify release | None | Inspect tagged public source, signatures/contracts and lock versions | None | Evidence identifies actual removed permission checks and retained actor/target callbacks |
| Update consumer | Existing installed package operations only | Verify Agent OS authorization/application use sites | Existing package events only | Locked dependency and adapters compile; installation data remains valid |
| Check grant boundary | Package role/user/permission commands through Agent OS service | Resolve actor/target/permission tier and managed reference | Package events only after authorized commit | No unsupported global/unknown tier delegation or managed-role bypass |

## Validation and permissions

**Needs info:** upstream EPIC-00008, TICKET-00009–00011 and TASK-00041–00045 plan v0.4.0, but do not implement or tag a release. Do not change `composer.lock` or claim new behavior until the actual tag, public contracts and ownership of tier eligibility are inspected. The upstream closed Wayfinder's WF-011–WF-016 decisions (not a release) say: the consumer classifies managed permission tiers and protects every command entry point; Access Control owns protected membership only through exact-name managed `ROLE_SUPER_ADMIN` to human Users, custom-role/Agent grant eligibility, non-null permission tiers (custom permissions start `ADMIN_SAFE`), reserved Role name, managed reclassification and intrinsic references. Actor-only checks are slated for removal from Role, User-role and Agent-permission handlers; cross-user session, email-change administration and pending-invitation correction target callbacks remain. The package cannot infer that a misclassified `ADMIN_SAFE` global security permission should be protected. Future Workspace/Repository admins require consumer-owned team scope and command-entry controls. An HTTP attribute is only an early gate; Agent OS Application must authorize every exposed entry path. Avoid duplicating the same permission rule in package handlers, but retain package intrinsic/reference/ownership checks. Verify package tier/Role/Agent invariants and WF-016's accepted atomic grant-versus-promotion outcome against the eventual tag; package TASK-00043/00044 strengthen grant-side repository contracts and TASK-00045 owns cross-path proof, while Agent OS must prove its own PostgreSQL tier/membership fencing and HTTP entry behavior; until the package's safe intrinsic grant rule is proven, Agent OS exposes no custom-role permission grant operation. Actor permission/scope remains a shared Agent OS Application decision; do not duplicate verified package role/tier invariants. Generate a new Doctrine migration version at actual generation time and preserve recorded historical versions; verify owned target contents before making tier non-null, never assume a production database is empty or rewrite live data.

## Acceptance criteria

- [ ] Tagged release and exact handler/port/tier changes are verified against source/lock, not inferred from closed Wayfinder decisions or the planned upstream EPIC/TICKET/TASK records.
- [ ] Consumer lock/version and actual application adapters agree with the new API; no speculative vendor edits or silent authorization bypass.
- [ ] Permission decisions are enforced by Agent OS for HTTP and non-HTTP callers; cross-user session/email-change administration/pending-invitation correction actor/target callbacks remain enforced.
- [ ] Package-managed/reference/tier/Agent-grant and unknown-tier failure paths are proven at meaningful owned integration boundaries; consumer schema and repository support non-null custom tiers after a safe guarded migration. Unsafe grants remain unavailable if the tagged contract lacks the required invariant, pending a human-approved owner decision.
- [ ] Compatibility, migration/reconciliation outcomes, focused checks and `./bin/build` pass with counts and all warnings recorded.

## Verification and evidence

Compare installed v0.3.0 and tagged upstream APIs/source, release notes, Composer install/runtime and the upstream closed WF-011–WF-016 decisions plus EPIC-00008/TICKET-00009–00011/TASK-00041–00045 handoff; inspect current consumer nullable tier constraint/repository reconstruction and actual data before migration. Invoke scoped package commands through Agent OS authorization paths with PostgreSQL and actor/target/managed/custom/Agent cases. Validate container/DB/migrations directly with owning tools, not product tests of lock, wiring or package internals. Run focused checks, `./bin/planning-check`, `git diff --check`, `./bin/build`; record actual tag/source hashes, changed files, contract/race counts, warnings and deferred paths.

## Coordination

TASK-00036 defines initial managed catalog; TASK-00121 and TASK-00127 depend on this gate. The upstream owning project's acceptance/release is independent work; this TASK updates only this consumer after human confirmation that the tag is suitable. Keep the ignored recommendation separate. Ask for checkout/worktree before implementation; use a `feature/*` branch from `develop`.

## Completion notes

Not started; release not verified. Record precise tagged contract, decision and integration evidence, remaining concerns, review and PR separately.
