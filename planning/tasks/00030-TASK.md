---
id: TASK-00030
ticket: TICKET-00013
kind: chore
title: Complete PHP style and static analysis
status: ready-for-agent
order: 30
blocked_by: TASK-00010
pr:
---

# Complete PHP style static-analysis and dependency enforcement

## Outcome

Pinned, Docker-backed PHP syntax, Fight Common 1.2 PHPCS, PHPStan, Deptrac, and Rector dry-run checks enforce accepted Fight PHP conventions, static correctness, and Domain/Application/Adapter direction across owned backend code with narrow justified exclusions.

## Scope

- In scope: Pin PHPCS and its plugins and consume the Fight Common 1.2 shipped standard directly; add deterministic PHP syntax linting; encode only justified repository additions; select/pin/configure PHPStan at the highest evidence-supported baseline; extend Deptrac across representative namespaces/paths; pin/configure Rector in dry-run mode; focused `./bin/*` wrappers against the running Compose PHP service; ignored cache/report paths; documentation of narrow exceptions.
- Out of scope: Backend line coverage, frontend tooling, canonical-build integration, source repair beyond violations introduced/exposed within authorized tool-adoption scope, upstream package analysis, broad baselines/ignore lists, and style preferences not established by repository conventions.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Check PHP syntax and style | N/A — repository tooling | Lint every owned PHP file and inspect it with the Fight Common 1.2 PHPCS standard plus justified repository additions | N/A — no domain event | Parse/style violations fail with file/line/rule diagnostics and no source mutation |
| Analyze static correctness | N/A — repository tooling | Inspect owned types, calls, generics, returns, and configuration boot shapes | N/A — no domain event | Owned type errors fail without hiding behind a broad baseline |
| Check automated-refactoring drift | N/A — repository tooling | Run pinned Rector configuration in dry-run mode over owned PHP | N/A — no domain event | Proposed changes fail clearly without rewriting source |
| Enforce dependencies | N/A — repository tooling | Inspect layer/package imports and composition boundaries | N/A — no domain event | Prohibited inward/package dependencies fail; accepted composition passes |
| Iterate with focused wrappers | N/A — repository tooling | Read pinned config/source | N/A — no domain event | Developers run deterministic checks with caches only under ignored paths |

## Validation and permissions

PHP syntax linting must cover every owned production, test, configuration, migration, and executable PHP file exactly once per canonical gate. PHPCS must load the standard shipped by the locked Fight Common 1.2 package directly rather than copying it, then apply only documented Agent OS additions. It must enforce applicable Fight/repository conventions, including strict declarations, naming, imports, formatting, final/readonly defaults where mechanically appropriate, multiline capitalized active-verb docblocks without trailing periods, `@inheritDoc` usage, and owned namespace/path conventions. Do not encode subjective rules lacking an accepted source or auto-fix source during acceptance checks.

PHPStan must run at the highest level supported by representative owned code with explicit extensions/stubs only where necessary. Do not generate a broad baseline or wildcard-ignore owned paths/errors. Every ignore must identify a narrow tool limitation or external contract and include a removal owner where possible. Vendor code is not reanalyzed as owned source.

Rector must use a committed, pinned configuration derived from demonstrated Agent OS needs and run only with `--dry-run` in acceptance checks. Any skip must name an exact compatibility or behavior reason; the gate must never rewrite source automatically.

Extend rather than replace TASK-00010's architecture check. Enforce accepted Domain/Application/Adapter direction, composition-root exceptions, capability dependencies, direct Fight package use, and copied/renaming-wrapper prohibitions where mechanically reliable. Exclude generated/cache/vendor paths, not meaningful owned behavior.

All tools and plugins are pinned in the committed Composer lock, run non-interactively through wrappers that execute in the already-running Docker Compose PHP service, and place cache/results under ignored `var/` or `.runs/`. Focused wrappers may select one phase for iteration but must not cause the canonical gate to duplicate a phase. No runtime permission applies; tooling/configuration changes are authorized.

## Acceptance criteria

- [ ] PHP syntax lint covers every owned PHP path and fails with clear file diagnostics.
- [ ] PHPCS and required plugins are pinned, load the Fight Common 1.2 shipped standard directly, and enforce documented Fight/repository additions on all owned backend PHP.
- [ ] PHPCS check mode does not rewrite source and excludes only vendor/generated/cache paths.
- [ ] PHPStan is pinned at a documented evidence-supported level with no broad baseline or wildcard suppression of owned failures.
- [ ] Rector is pinned, narrowly configured, and passes in dry-run mode without changing tracked source.
- [ ] Every static-analysis ignore/stub/extension is narrow, justified, and recorded with its consequence.
- [ ] TASK-00010's dependency checks are extended across representative Domain, Application, Adapter, configuration, and package-ownership paths rather than replaced.
- [ ] Composition exceptions are explicit and narrow; meaningful owned code is not excluded to make the gate pass.
- [ ] Docker-backed focused wrappers are deterministic, non-interactive, and write only ignored caches/reports.
- [ ] Tool configuration is inspected and each tool passes against the real owned source without product tests or seeded-invalid fixtures for the tools themselves.
- [ ] No upstream package suite or vendor source is treated as owned code.
- [ ] Focused checks and `./bin/build` pass with tool versions, file/error counts, exceptions, and warnings recorded.

## Verification and evidence

Run each tool over the real owned tree and capture versions/configuration scope, including the exact Fight Common standard path. Inspect configuration and diagnostics directly; do not create product tests or seeded-invalid fixtures to test the tools. Verify cache/report locations and compare tracked state before/after check mode. Review every exclusion/ignore against the accepted ownership ADR.

Run focused wrappers, `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record versions, analyzed file counts, ignored paths/rules, durations where useful, warnings, and any currently review-only architecture rule deferred to TASK-00033 or later code.

## Coordination

TASK-00010 supplies the accepted architecture rules and initial dependency enforcement. Keep its configuration/history recognizable so later work extends one policy. TASK-00031 uses these checks alongside coverage; TASK-00033 integrates them into the canonical gate. Preserve unrelated source and do not repair broad pre-existing code outside authorized scope without recording it.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable backend analysis-tooling outcome.

## Completion notes

Implementation on `feature/task-00030-php-analysis` in the user-selected isolated worktree, reconciled with `develop` at `115c005`. Review, PR publication, and merge remain separate; no independent review has occurred.

- Locked Fight Common v1.2.0; PHPCS 4.0.4, Slevomat 8.31.1, PHPStan 2.2.16, Rector 2.6.7, and Deptrac 4.7.2. `phpcs.xml` loads the shipped `vendor/johnnickell/fight-common/src/Standards/Phpcs/ruleset.xml` directly with **no local rules, exemptions, or skips**. Do not require final/readonly-class sniffs without an entity-safe exception: the current Fight Common standard does not enforce those defaults. A focused syntax companion checks owned `App\\`/`Tests\\` namespace-to-file mappings and named type basenames (the procedural `tests/bootstrap.php` is syntax-checked but has no namespace); semantic anti-wrapper judgments remain review responsibilities. No PHPCS exception is used.
- Added focused non-interactive Docker-backed syntax, style, PHPStan, and Rector dry-run commands in `bin/backend-analysis`. Syntax visits the explicit owned PHP paths once; its namespace check covers class-like source/tests without exempting them from PHP lint. PHPCS, PHPStan, and Rector include owned configuration, tests, migrations, scripts, and source. `phpstan.neon` uses level 7 without a baseline, stubs, extensions, ignores, or excluded owned files (level 8 reports 70 current findings, so was not adopted). Rector enables only `SimplifyTautologyTernaryRector`; no skips. Vendor and generated/cache paths are excluded from owned analysis; tool caches stay in ignored `var/`.
- Extended TASK-00010 Deptrac to inspect configuration/bootstrap/public/scripts as a composition layer while retaining the original Domain/Application/Adapter and Fight-package restrictions. This composition layer may depend on owned layers and installed infrastructure; there are no skipped violations. Direct-package use, service-location intent, and copied/renaming-wrapper intent cannot be proved by this dependency graph alone and remain for review.
- Repaired Fight Common style and PHPStan findings exposed by adoption and the merged persistence/audit code; changes include PHPDoc-only annotations, formatting of already-applied migration SQL (whitespace only), list-preserving DBAL result normalization, and explicit guarded row/int return types. No product tests of tooling, architecture, migration files, or intentionally invalid fixtures were added. `scripts/build.php` has only PHPCS formatting changes; integration of the new analysis phases into the canonical build belongs to TASK-00033, not this TASK.
- Fresh verification after the final source edits: syntax 72 files, 0 failures; PHPCS 72 files, 0 errors/warnings; PHPStan level 7, 0 errors; Rector dry-run passed; Deptrac 586 allowed, 0 violations/uncovered/skips/warnings; `./bin/planning-check` passed (154 records, 124 active); `git diff --check` passed; existing `./bin/build` passed with 69 tests and 1049 assertions. An earlier level-8 probe was not used as a green check; the current probe reports 70 errors. No required check warnings were suppressed. Focused checks do not substitute for the future TASK-00033 full gate.
