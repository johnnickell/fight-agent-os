---
id: TASK-00062
ticket: TICKET-00021
kind: feature
title: Deliver the confirmed elevated invitation journey
status: ready-for-agent
order: 62
blocked_by: TASK-00061
pr:
---

# Deliver the confirmed elevated invitation journey

## Outcome

A deliberately isolated elevated invitation route uses TASK-00041's existing orchestrator only when the current principal has both `INVITE_USERS` and exact `ASSIGN_SUPER_ADMIN`, requires unambiguous confirmation, and creates one pending identity assigned managed `ROLE_SUPER_ADMIN` with normal seven-day activation, recoverable delivery, and durable safe actor/target audit evidence.

## Scope

- In scope: Separate protected elevated invitation route and complete two-permission metadata; permission-controlled navigation; canonical email form; elevated risk explanation and explicit confirmation interaction; typed existing-API request; pending/validation/forbidden/conflict/throttle/success/system states; directory/status invalidation/navigation; integrated exact-permission/audit/PostgreSQL/HTTP/component/accessibility/redaction tests.
- Out of scope: New elevated backend endpoint/orchestrator, elevation option in ordinary form, arbitrary role/permission assignment, standalone active-user promotion, custom-role administration, role removal/demotion, bootstrap reset/bypass, lifecycle administration, administrator-selected password, bulk elevation, raw credentials, and broad browser automation.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Invite pending Super Admin | Existing TASK-00041 elevated Action/orchestrator uses `InvitePendingUser` and `AssignRoleToUser` | Resolve current principal permissions, canonical email, explicit confirmation, managed role and target/policy state | `UserInvited`, `RoleAssignedToUser` plus package/owned audit | Pending managed Super Admin, seven-day grant and recoverable delivery reach coherent committed outcome |
| Reject absent elevation authority | N/A before command dispatch | Evaluate exact `INVITE_USERS` and `ASSIGN_SUPER_ADMIN` | N/A — no domain event | Safe `403`; no identity/assignment/grant/audit-success/delivery created |
| Reject missing/ambiguous confirmation | N/A before elevated orchestration | Validate exact confirmation contract and canonical target | N/A — no domain event | Controlled validation result; ordinary flow is not substituted silently |
| Continue after success | N/A — cache/navigation | Evaluate list/manage permissions and registered routes | Client invalidation/navigation signal | Directory refetches and authorized actor may inspect credential-free status |

## Validation and permissions

Use a distinct route and interaction from TASK-00061. Complete route metadata requires both exact `INVITE_USERS` and `ASSIGN_SUPER_ADMIN`; TASK-00041 server Action independently rechecks both through authoritative principal resolution. Possession/display of `ROLE_SUPER_ADMIN`, role names, client route visibility, JWT/HTML claims, or one permission alone grants nothing. Unknown/stale/mismatched authority fails closed.

Accept one bounded canonical email and TASK-00041's exact elevated mode/confirmation fields only. Expose no arbitrary role ID/name, permission set, actor, target user ID, password, grant, template/provider, lifetime, or bootstrap flag. The server resolves only stable managed `ROLE_SUPER_ADMIN`; malformed/hidden values cannot select another role or elevate the ordinary endpoint.

Require deliberate explicit confirmation after showing canonical target, managed elevated role, installation-wide consequence, invite-only activation, and actor accountability. Use an unambiguous boolean/typed protocol accepted by TASK-00041; absent/false/wrong-type/stale confirmation rejects before mutation. Avoid fragile phrase entry unless accepted design requires it. Confirmation cannot be preselected and duplicate submission is disabled while pending.

Reuse existing orchestrator/package behavior for pending invitation, role assignment authorization, managed-role reference, audit, seven-day grant/delivery, rates, conflicts, and transaction/race fences. This flow's “assignment” is the role assignment within creation of a newly invited pending identity. General promotion of an existing active user is not exposed because broad role administration is out of scope; document this boundary rather than silently adding an endpoint.

Audit must durably distinguish authenticated actor, target, elevated action/role, time/result and approved safe context without password/grant/credential/link/provider secret. Success does not claim delivery/receipt. After success invalidate directory and, with exact manage permission, offer TASK-00060 status; otherwise safe success only. Do not broadcast target authority snapshots or persist form/permission state.

Recreate accepted high-risk warning/confirmation/form states with semantic alert/heading/dialog behavior, labels/instructions, keyboard/focus/escape/return, status announcements, visible focus, target/reflow, themes, reduced motion, long/Unicode email, and clear distinction from ordinary invitation. No color-only risk meaning.

## Acceptance criteria

- [ ] Elevated route is distinct from ordinary invitation and requires both exact authoritative `INVITE_USERS` and `ASSIGN_SUPER_ADMIN` in client metadata and server Action.
- [ ] `ROLE_SUPER_ADMIN`, one permission alone, submitted actor, JWT/HTML/client visibility, and stale authority cannot grant elevation.
- [ ] Request accepts only canonical email plus exact elevated mode/confirmation and no arbitrary role/permission/actor/password/provider/bootstrap input.
- [ ] Explicit confirmation is unambiguous, not preselected, identifies safe target/consequence, and absent/false/malformed/stale confirmation causes no writes.
- [ ] Existing orchestrator resolves only managed `ROLE_SUPER_ADMIN` and uses package invitation/assignment behavior without direct persistence/copied policy.
- [ ] Success coherently creates one pending identity assigned managed `ROLE_SUPER_ADMIN`, safe audit, seven-day activation, recoverable delivery, and no administrator password.
- [ ] Ordinary invitation remains incapable of elevation through hidden/malformed input.
- [ ] Missing/stale permissions, duplicate/canonical identity, policy/role drift, throttle, confirmation failure, and concurrent requests produce no partial or misleading success.
- [ ] Audit identifies actor/target/elevated action safely and contains no grant/password/token/link/provider/internal secrets.
- [ ] No standalone active-user promotion, custom-role administration, demotion, bootstrap reset/bypass, or broad lifecycle operation is introduced.
- [ ] Directory/status invalidation/navigation follows exact LIST/MANAGE permissions and does not persist or broadcast authority snapshots.
- [ ] Responsive warning/form/confirmation/pending/success/conflict/forbidden/system states meet accepted semantic, keyboard/focus, announcement, theme, target, and reflow requirements.
- [ ] Focused permission-combination/audit/PostgreSQL/race/HTTP/client/component/accessibility/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Test the complete `INVITE_USERS`/`ASSIGN_SUPER_ADMIN` permission matrix with misleading role names/`ROLE_SUPER_ADMIN`, stale/mismatched authority, actor/role/permission/bootstrap field injection, ordinary endpoint elevated fields, and absent/false/wrong-type/duplicate confirmation. Exercise malformed/Unicode/case-equivalent/duplicate lifecycle email, policy/role drift, limiter boundaries, and concurrent elevation; inspect pending identity, exact managed role, audit, seven-day grant/delivery/intent, rollback, and safe response.

Drive client direct route/navigation visibility, confirmation open/cancel/confirm/focus return, keyboard, duplicate/delayed/out-of-order submission, permission loss, status/list permission combinations, and unmount. Render accepted narrow/wide/system/light/dark/reduced-motion/forbidden/conflict/success states. Seed grant/password/token/link/provider/SQL/stack markers and inspect response/OpenAPI support fixtures, audit, DOM/state/messages/history/storage/logs/analytics/screenshots/snapshots.

Run focused backend integration/permission/audit/PostgreSQL/concurrency/HTTP support checks and frontend type/lint/Vitest/RTL/Storybook/build, accessibility automation plus manual observations, storage/redaction scans, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record request/confirmation/UI contracts, permission/audit/race matrices, tests/assertions/stories, accessibility limits, redaction evidence, versions, durations, and warnings.

## Coordination

TASK-00061 supplies isolated ordinary invitation and shared form conventions; TASK-00060 supplies permitted status; TASK-00059 supplies directory. Reuse TASK-00041's elevated endpoint/orchestration and TASK-00038's normal exact `ASSIGN_SUPER_ADMIN` authorization foundation. Keep bootstrap composition unreachable and general active-user promotion deferred.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable elevated pending-invitation journey.

## Completion notes

Not started. Record route/service/form/confirmation/navigation contracts, exact permission/persistence/audit/race/accessibility/storage/redaction evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and deferred role-administration boundary here.
