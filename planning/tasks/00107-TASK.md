---
id: TASK-00107
ticket: TICKET-00029
kind: feature
title: Deliver repository registration, selection, and context workspace
status: ready-for-agent
order: 104
blocked_by: TASK-00106
pr:
---

# Deliver repository registration, selection, and context workspace

## Outcome

Authenticated users can enter a permission-aware repository-scoped Planning route, register/select/switch visible repositories, manage designated checkout links and inspect or revise permitted context configuration/conflict choices without carrying stale authority across URL scope or client state.

## Scope

- In scope: Production React route/layout/page/services/components from accepted handoff; visible Repository list/search/empty state; existing Repository registration/source confirmation; URL-owned selection; registration/source/lifecycle/reconciliation summaries/actions where TICKET-00024 exposes them; designated Checkout/Worktree/Host selection/relink/availability; context summary/revision/mode/instruction provenance/conflicts/warnings/projection diagnostics; configuration history/preview/revise/restore and session-choice forms where permitted; typed API mapping; query cancellation/cache keys by scope/revision; permission-aware navigation/actions; responsive/state/component-catalog evidence.
- Out of scope: Filesystem browser beyond approved roots, raw instruction contents/editing, Planning hierarchy/artifact UI, browser Agent conversations, model/Pi sessions, migration import, local path guessing, client authorization, generic configuration JSON and automatic context refresh/fallback.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Register/select Repository | Dispatch exact TICKET-00024 registration command when authorized; selection is navigation | List visible repositories and get registration/source/lifecycle | TICKET-00024 event only on registration | URL scope changes after server success; prior Repository cache/context does not become current |
| Designate/relink checkout | Dispatch exact preview/confirm/designate/relink command | Query approved candidates/evidence/availability/current link | TICKET-00024 semantic event | Confirmed physical link changes; UI never invents identity from path/name |
| Inspect context | N/A — read-only interaction | Resolve safe context summary, provenance, conflicts, revision and availability | N/A — read emits no event | Current facts/warnings and permitted actions display without instruction contents/secrets |
| Revise/restore configuration | Dispatch exact TICKET-00025 semantic command | Preview validation/diff/impact/history/current revision | TICKET-00025 event | Accepted revision/rollback updates server state; stale proposals remain unapplied |
| Resolve session conflict | Dispatch exact narrowing choice where supported | Preview structured sources/recommendation/authority effect | TICKET-00025 event | Exact choice binds only accepted scope and never widens authority |

## Validation and permissions

Repository scope is canonical URL state using stable safe route identifiers, not display name/path. On switch, cancel/ignore in-flight old-scope results, key API cache by authenticated principal/Workspace/Repository/revisions, clear transient forms and refetch server-authoritative principal/scope/context. Never render previous Repository data under a new URL or carry a context token implicitly.

Forms use typed TICKET-00024/25 API Views and exact commands/previews against expected revisions. Registration does not migrate Planning or grant execution. Checkout candidate evidence shows only safe relative/display facts; no free-form path becomes authority. Context UI records paths/digests/provenance safely but does not load complete instruction text or parse prose. Configuration is closed typed fields, not arbitrary JSON.

Client visibility/disabled copy reflects current principal Permissions but every query/command remains server-authorized. Distinguish view, register, source/lifecycle, checkout, context, configuration revise/restore and conflict-choice authority. Unknown principal/scope fails closed. Do not infer role names or show hidden Repository existence.

Cover loading, no visible repositories, no selection, registration validation/conflict, missing/ambiguous/unavailable checkout, archived Repository, stale revision, context conflict/stale/offline, denied/not-found/partial/service unavailable and success. Do not silently refresh authority-bearing context; changed context prompts the accepted new-binding flow. Offline/cached registered data is visibly stale/read-only and cannot enable actions.

Use existing shared API client/error/current-principal/routing/component/theme boundaries. Production pages coordinate; forms/components do not encode Domain policy. Warn before leaving dirty forms; no autosave/persisted drafts. Accessible status, dialog confirmation, focus restoration and narrow/wide layout follow accepted handoff.

## Acceptance criteria

- [ ] Visible Repository registration/list/selection uses safe server Views and URL-owned stable scope; display/path never establishes identity.
- [ ] Switching Repository cancels/invalidates old-scope work and cannot display/carry stale data/context/authority in the new scope.
- [ ] Registration/source/lifecycle and checkout designation/relink interactions dispatch exact TICKET-00024 previews/commands only.
- [ ] Context summary exposes safe mode/revision/provenance/conflict/availability/diagnostics without complete instructions, secrets or internal paths/services.
- [ ] Configuration revise/restore and conflict choices use exact typed TICKET-00025 previews/commands and expected revisions; no arbitrary JSON or widening.
- [ ] Permissions independently filter navigation/actions but never replace server authorization; unknown scope/principal fails closed.
- [ ] Loading/empty/ambiguous/archived/stale/conflict/unavailable/offline/denied/partial/success states are honest and recoverable.
- [ ] Offline/cached data remains visibly read-only and no silent context refresh/fallback occurs.
- [ ] Dirty forms warn before navigation and no manual draft/autosave or browser business authority is introduced.
- [ ] Responsive, keyboard/focus/status/confirmation and component-catalog evidence matches the accepted handoff.
- [ ] Focused client behavior/API-contract/redaction tests and the canonical build pass.

## Verification and evidence

Use mocked typed API boundaries to behavior-test selection/switch cancellation, cache keys, permissions, forms/previews/stale conflicts and state recovery; test owned mapping/orchestration, not React/router mechanics. Add component-catalog stories for accepted representative states with sanitized data. Functional API tests remain with backend owners unless a critical browser integration contract is unique here.

Manually exercise representative narrow/wide keyboard/focus paths and network stale/denied/unavailable responses in production components. Scan DOM/storage/URLs/logs/captures for paths, instruction content, credentials and stale cross-Repository identifiers. Run frontend focused tests/type/lint/build/accessibility checks, backend contract checks as needed, `./bin/planning-check`, `git diff --check`, and `./bin/build`; record counts/durations/warnings.

## Coordination

TASK-00106 supplies accepted handoff; TICKET-00024/25 and TASK-00103 supply server authority. TASK-00108 nests Planning browsing in this exact scope. TICKET-00029 does not own instruction-content editing or browser Agent sessions.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable repository/context browser journey.

## Completion notes

Not started. Record routes/components/services/API mappings, scope/cache/permission/state/accessibility evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details and deferred Planning/conversation work here.
