---
id: TASK-00001
ticket: TICKET-00001
kind: feature
title: Add the initial project-local planning skills
status: done
order: 1
blocked_by:
pr:
---

# Add the initial project-local planning skills

## Outcome

The repository has an initial planning skill suite and no longer treats inherited framework-support receipts or lowest-lock files as part of the canonical application foundation.

## Scope

- In scope: Remove inherited support artifacts, update the build gate to match, add initial planning skills, record third-party notices, and refresh planning views.
- Out of scope: Build Pi extensions, agent orchestration, worktree management, database planning storage, or React observability screens.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Invoke a project-local planning skill | N/A until Pi invocation configuration is planned | Skill file reads | N/A | Agent follows repository-specific planning rules |
| Run the canonical gate | `./bin/build` | Composer and test suite reads | N/A | Gate no longer requires deleted framework-support or lowest-lock evidence |

## Validation and permissions

Planning validation must pass after generated views are refreshed. The inherited full gate must not reference deleted support files. No new runtime permissions are introduced.

## Acceptance criteria

- [x] `.pi/skills/` contains `grill`, `wayfinder`, `research`, `prototype`, `to-tickets`, and `to-tasks` skill documents.
- [x] Matt Pocock's MIT notice is recorded for skill inspiration.
- [x] `evidence/framework-support`, `composer-lowest.lock`, and `composer-lowest.lock.sha256` are removed.
- [x] Build scripts and tests no longer require the deleted artifacts.
- [x] Planning validation passes.

## Verification and evidence

Run `./bin/planning-check --write`, `./bin/planning-check`, and a focused PHP test or full `./bin/build` if practical after removing inherited gate references.

## Coordination

No SUBTASK files are required for this small foundation change.

## Completion notes

Implemented initial skill documents, planning records, third-party notice, support-artifact cleanup, and canonical gate updates. Renamed the conversational and disposable-evidence skills to `grill` and `prototype`. Verification passed with `./bin/planning-check --write`, `./bin/planning-check`, and `./bin/build`. The build still reports Composer's inherited pinned Fight Common commit-ref warning; this is a known dependency qualification topic for later foundation planning.
