---
id: TASK-00053
ticket: TICKET-00019
kind: feature
title: Deliver recoverable secret-safe password-reset email
status: ready-for-agent
order: 53
blocked_by: TASK-00040
pr:
---

# Deliver recoverable secret-safe password-reset email

## Outcome

Package-owned encrypted password-reset delivery generations become recoverable mail through qualified package discovery, claims, direct handlers, and outcomes: invocation materializes a canonical fragment-based HTTPS completion link only in memory, terminal package outcomes destroy ciphertext, and crashes retain honest at-least-once recovery without a consumer intent pipeline.

## Scope

- In scope: Password-reset mapping for the exact locked package provider-neutral credential-delivery capability; stable delivery-generation idempotency; protected credential handling at the invocation boundary; canonical reset URL builder; deterministic text/HTML templates; generic mail capability mapping; development catcher reuse; production key/provider/origin guards; provider/outcome crash recovery; retry/expiry/redaction tests and operational documentation.
- Out of scope: Package discovery/claim/outcome policy or persistence, reset-request HTTP/UI, reset completion HTTP/UI, production mail-vendor enrollment, arbitrary template engine/endpoints/callables, any consumer outbox/queue, user-facing delivery status, plaintext credentials in operational views, analytics/tracking, and scheduler/platform enrollment.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Recover reset delivery | Existing TASK-00024 runner dispatches exact package handlers | Discover package-owned pending, due-retry, and expired-lease reset work | `PasswordResetRequested` may prompt immediate processing but is not recovery authority | Committed package work remains discoverable without consumer registration or reconciliation |
| Invoke reset mail | Exact locked package handler invokes its provider capability after claim commit | Receive only package-approved invocation-lifetime destination, credential material, and delivery-generation ID | Package outcomes remain package-owned | Credential is materialized transiently, canonical mail is invoked with stable idempotency, and a typed result returns to the handler |
| Record successful delivery | Same package handler lifecycle | Compare exact live claim token, revision, and delivery generation | Package event only after durable state as defined upstream | Matching delivered outcome commits and package ciphertext is destroyed |
| Expire undelivered work | Qualified package discovery and handler lifecycle | Resolve recoverable delivery at/after one-hour expiry | Package expiry event as defined upstream | Package terminal state destroys ciphertext and prevents later mail invocation |

## Validation and permissions

Treat the password-reset delivery generation persisted with the one-hour grant as the sole durable work authority. `PasswordResetRequested` may trigger prompt post-commit package processing, but correctness depends on bounded deterministic package due-work discovery recovering the originating commit/event gap. Agent OS must not register, reconcile, map, or persist a second intent/reference for that generation.

Implement reset-mail mapping through the exact locked package provider-neutral capability from TASK-00022. Persisted package state cannot dynamically select Agent OS code, service, template, endpoint, or callable; those choices remain fixed composition. Protected material remains encrypted at rest and is materialized under package control only for invocation. Plaintext exists only during that call; PHP zeroization limitations are documented and references are minimized.

Build completion URLs only from configured canonical HTTPS origin/path, never request Host/forwarded headers/user input. Place bounded user routing identity and reset credential in the URL fragment according to the accepted contract so neither reaches request lines/referrers. Reject userinfo, ambiguous host/path, query credentials, and non-HTTPS outside explicit test/development.

Use fixed/versioned escaped text and HTML templates with expiry, ignore-if-unrequested, and recovery guidance. Include no remote image/font/script, tracking pixel, analytics parameter, unnecessary identity data, or mutable content. Reuse TASK-00040's mail capability/catcher/provider composition and never print links to console/logs.

Preserve package-owned claim/invoke/outcome phases, stable delivery-generation idempotency, bounded retry, leases, stale-worker protection, and honest at-least-once semantics. The direct package handler records only a matching delivered, retryable, or permanent outcome; no pipeline-to-package confirmation bridge exists. Provider-success-before-outcome may duplicate under recovery. At expiry, package policy prevents new invocation and terminalizes work with ciphertext destruction. Internal package runner authority is not an anonymous product endpoint.

## Acceptance criteria

- [ ] Package reset-delivery state committed with the grant is the sole queue; no consumer intent, registration, reconciliation, schema, or competing outcome authority exists.
- [ ] Deterministic bounded package discovery recovers the grant-commit/event gap and dispatches exact direct package handlers.
- [ ] Reset mail maps the exact locked package provider capability without dynamically resolving arbitrary code, template, service, or endpoint from persisted work.
- [ ] Stable delivery-generation idempotency survives retries, worker crashes, lease recovery, and duplicate attempts.
- [ ] Credential ciphertext remains protected at rest; plaintext exists only during invocation and is absent from ordinary reads/diagnostics/test records.
- [ ] Canonical links derive only from validated configured HTTPS origin/path and keep credential material in an approved fragment, out of request lines/referrers.
- [ ] Deterministic text/HTML templates escape data, explain one-hour expiry/recovery, and contain no tracking or remote mutable content.
- [ ] A matching package-owned delivered outcome destroys recoverable ciphertext; outcome-commit crashes recover through the same package work and stable identity.
- [ ] Expired undelivered generations invoke package expiry, destroy ciphertext, and cannot later send.
- [ ] Provider-success/outcome-commit crash behavior remains explicitly at-least-once with no exactly-once claim.
- [ ] Development catcher is scoped/disposable; production-like boot fails for absent provider, missing/weak key, or invalid canonical origin.
- [ ] Focused package-handler/PostgreSQL/worker/mail/catcher/idempotency/expiry/composition/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Persist eligible reset delivery state, stop after package commit and before event handling, then run package discovery and prove the same generation is processed without a consumer intent. Run competing package workers with separate PostgreSQL connections and controlled synchronization. Exercise stale, replaced, delivered, permanent, and expired generations plus originating and claim rollback. Stop/restart after claim, decryption, provider success, and before outcome persistence; record attempts, stable idempotency, final package state, stale-worker fencing, and the accepted duplicate window.

Deliver deterministic catcher mail and inspect text/HTML, canonical origin/path, fragment fields, expiry/recovery copy, escaping, and absence of remote/tracking content; clean catcher afterward. Seed credential/ciphertext/provider-key/URL markers and inspect database status, logs, exceptions, audit, metrics, snapshots, adapter records, console output, and generated artifacts. Boot invalid production-like key/provider/origin configurations safely.

Run focused delivery/package-handler/PostgreSQL/concurrency/runner/mail/composition tests, architecture/style/static/coverage checks and secret scans, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record locked package capability, template version, URL/key policy without secrets, state/crash matrix, tests/assertions/attempts, catcher/tool versions, redaction evidence, durations, warnings, and operational enrollment deferral.

## Coordination

TASK-00040 supplies canonical invitation-mail patterns, catcher, mail capability, and production provider guard; TASK-00016/TASK-00022 through TASK-00024 supply qualified package-owned persistence, provider capability, direct handlers, and recovery runner. Preserve package reset-grant/delivery ownership and exact locked package outcome commands. TASK-00054 creates source work and TASK-00055 consumes completion links. Do not create invitation/reset-specific queue infrastructure or a pipeline confirmation bridge.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable password-reset delivery outcome.

## Completion notes

Not started. Record locked package provider mapping, template/link composition, package-owned outcome handling, protected material and at-least-once semantics, crash/retry/idempotency/expiry/redaction results, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and provider/scheduler deferrals here.
