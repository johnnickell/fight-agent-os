---
id: TASK-00007
ticket: TICKET-00007
kind: chore
title: Adopt and prove the stable dependency graph
status: done
order: 7
blocked_by: TASK-00004, TASK-00006
pr:
---

# Adopt and prove the stable dependency graph

## Outcome

Fight Agent OS resolves and installs a committed stable dependency graph using Fight Common `~1.2.0` and Fight Access Control `^0.2.0`. The canonical build proves strict manifest validity, locked installation, advisory status, application tests, planning validity, and a development-package-free production install without candidate-reference or lowest-version machinery.

## Scope

- In scope: Replace Fight development references with approved stable constraints; set stable minimum stability; update and commit the compatible lock; remove obsolete candidate commit-reference validation; make `./bin/build` prove strict Composer validity, locked development installation, advisory audit, planning validation, PHPUnit, and clean `--no-dev` installation; record fresh install and audit evidence.
- Out of scope: Application behavior changes, inherited support-test removal, production source-contract redesign, upstream package changes, unreleased dependency exceptions, lowest-version lanes, broad dependency pruning, and the complete quality-tool expansion owned by TICKET-00013.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Resolve stable dependencies | N/A — Composer is repository tooling | Inspect approved constraints, package metadata, release tags, transitive requirements, and lock changes | N/A — no application domain event | Manifest and lock contain compatible stable Fight package versions without branch aliases or commit references |
| Install the development graph | N/A — locked Composer installation | Inspect platform requirements, lock integrity, autoload generation, and installed packages | N/A — no application domain event | A fresh development installation succeeds from the lock without updating dependencies |
| Prove the production graph | N/A — clean `--no-dev` Composer installation | Inspect production package set, autoload, platform requirements, and development exclusions | N/A — no application domain event | A clean production copy installs reproducibly and contains no development-only packages |
| Audit the graph | N/A — Composer validation/audit operations | Read strict validation and locked production advisory results | N/A — no application domain event | Build reports validity, advisories, warnings, and network limitations honestly |

## Validation and permissions

`composer.json` must require Fight Common `~1.2.0`, Fight Access Control `^0.2.0`, and stable minimum stability. The committed lock must resolve compatible stable releases and pass content-hash validation. The canonical build must install from the lock without an update, run `composer validate --strict`, run `composer audit --locked --no-dev`, preserve read-only planning validation and PHPUnit, and prove a clean production install excludes development packages.

Remove `scripts/validate-composer-candidate.sh` and every invocation only after confirming no remaining candidate identity contract needs it. Do not restore commit-reference exceptions, support-receipt generation, or lowest-version locks/lanes. Do not suppress Composer warnings or advisory/network failures; record their exact effect on verification.

No runtime permission model applies. Repository dependency, lock, wrapper, and build changes are authorized. Upstream repositories, product behavior, broad provider pruning, and unreleased refs are not authorized.

## Acceptance criteria

- [x] `composer.json` uses `~1.2.0` for Fight Common, `^0.2.0` for Fight Access Control, and stable minimum stability without branch aliases or commit references.
- [x] `composer.lock` is committed, compatible with the manifest, and resolves approved stable Fight releases and transitive requirements.
- [x] Obsolete candidate-reference validation and its canonical-build invocation are removed.
- [x] `composer validate --strict` passes without the inherited commit-reference warning.
- [x] Fresh locked development installation succeeds without an implicit update and generates usable autoload files.
- [x] Fresh clean `composer install --no-dev` succeeds and contains no development-only packages.
- [x] `composer audit --locked --no-dev` is part of the gate, and advisories, warnings, or network limitations are disclosed rather than suppressed.
- [x] The current application test behavior remains green against the stable graph.
- [x] No lowest-version lane, upstream package modification, unreleased reference, or complete quality-tool expansion is introduced.

## Verification and evidence

Record the manifest/lock diff and exact selected Fight package tags/commits. In disposable clean directories or the canonical build container, prove locked development and `--no-dev` production installs with no implicit update. Record strict Composer validation, installed-package/development-exclusion evidence, autoload checks, audit output, platform details, network limitations, warnings, and PHPUnit counts.

Run focused Composer validation, install, audit, and application smoke checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. The final gate must exercise the new stable graph and production install. Report any time-sensitive advisory caveat and do not treat prior WF-003 receipts as fresh implementation evidence.

## Coordination

TASK-00004 and TASK-00006 must complete so the full EPIC-00002 execution and design-skill foundation is usable before EPIC-00003 implementation begins. Keep clean-install scratch and logs under a TASK-owned `.runs/` path; do not overwrite unrelated local `vendor/` or lock work. This TASK is the dependency prerequisite for TASK-00008.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable dependency/gate outcome and must receive independent review before landing.

## Completion notes

Implemented the stable dependency graph in `composer.json` and `composer.lock`: Fight Common resolves to `v1.2.0` at `a2cd615d9b5064c9c30e994655536176249cd73b`, and Fight Access Control resolves to `v0.2.0` at `c986b488ef8490c16c7a69e4675b5348f8d6dbf5`. Minimum stability is explicitly `stable`; aliases and development stability flags are gone. Removed `scripts/validate-composer-candidate.sh`. Updated `bin/build` to run strict Composer validation, locked development installation, the locked production-only advisory audit, planning validation, PHPUnit, and a clean production install after deleting the copied `vendor/`; the gate also fails if the root development dependency `phpunit/phpunit` appears in production.

Fresh disposable installs on PHP 8.5.10 and Composer 2.10.3 used only the committed lock. Development installed 93 packages, generated usable autoload files, and reported both exact Fight tags/commits. Production installed 67 packages, generated usable autoload files, and proved `phpunit/phpunit` and `vendor/bin/phpunit` absent. `composer validate --strict` passed without the inherited commit-reference warning. `composer audit --locked --no-dev` reached the advisory service and found no known security vulnerability advisories at verification time. Focused PHPUnit passed 26 tests / 152 assertions; planning validation passed with 95 records / 84 active records before completion status changed; the corrected obsolete-machinery scan passed. The final post-completion-record `./bin/build` passed after the explicit production exclusion check, including strict validity, a successful advisory audit, planning validation with 95 records / 83 active records, 26 tests / 152 assertions, a clean 67-package production install, development-package exclusion, and the public dependency contract. Composer reported informational funding messages for 69 development and 45 production packages; no advisory, deprecation, or test warning was reported.

Initial evidence commands exposed three non-product issues and were corrected: the first disposable development verification used two package names in one `composer show` invocation and exited after the successful install; its clean rerun passed. Host cleanup of read-only dist files failed, so only the TASK-owned ignored scratch copy was removed through the container before rerunning. An initial custom absence scan treated Composer's generated `"prefer-lowest": false` lock metadata as a lowest-version lane; the corrected scan targets actual lane/candidate/development-reference artifacts and passed. Standalone `./bin/composer` focused commands also printed Git safe-directory diagnostics from the wrapper container, while the canonical gate configures the safe directory and emitted no such diagnostic.

Changed durable files are `composer.json`, `composer.lock`, `bin/build`, this TASK record, generated planning views, and removal of `scripts/validate-composer-candidate.sh`. No application behavior, inherited tests, production source contract, upstream package, broad dependency set, lowest-version lane, or expanded quality tooling changed. Implementation and required local verification are complete. Independent review is pending; no push, PR, merge, release, deployment, or upstream support certification is claimed.
