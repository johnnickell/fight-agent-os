---
id: TASK-00063
ticket: TICKET-00022
kind: feature
title: Establish the production-like HTTPS security envelope
status: ready-for-agent
order: 63
blocked_by: TASK-00052, TASK-00055, TASK-00056, TASK-00058, TASK-00062
pr:
---

# Establish the production-like HTTPS security envelope

## Outcome

All completed browser journeys run inside one production-like security envelope with explicit reversible trusted local HTTPS, mandatory HTTPS and validated proxy/origin assumptions elsewhere, same-origin API delivery with CORS denied, compatible CSP and standard response headers, and fail-closed production startup validation for every signing, encryption, CSRF, cookie, provider, canonical-origin, and rate-limit control.

## Scope

- In scope: Pinned local TLS/proxy tooling and repository wrappers; local certificate/trust setup/status/removal documentation; same-origin application/API routing; production HTTPS/proxy/origin enforcement; CSP and standard security-header policy/middleware; sensitive cache/referrer policy; CORS deny behavior; environment-scoped Storybook/Swagger/catcher exceptions; consolidated typed secret/security/rate configuration validation; production-like startup checks; TLS/header/cookie/CORS/configuration/composition tests and operator documentation.
- Out of scope: Public certificate/production reverse-proxy enrollment, deployment, DNS, production mail-provider enrollment, cross-origin clients, broad infrastructure automation, weakening journey-owned request controls, secret generation/storage service, and release certification.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Enroll local HTTPS trust | Repository setup/status/remove wrappers | Inspect supported local trust tool, generated cert scope, and hostnames | N/A — no domain event | Explicit user trust is installed/reported/reversed; private CA/key remains untracked and user-controlled |
| Start secure local stack | Repository run wrapper | Validate local certificate, origin, proxy, application and catcher configuration | N/A — no domain event | Browser reaches same-origin application/API with Secure-cookie behavior over trusted HTTPS |
| Start production-like mode | N/A — composition/startup | Validate HTTPS origin/proxy trust, keys/secrets, cookies, provider capability, limit policies and environment exceptions | N/A — no domain event | Unsafe/missing/default/weak configuration prevents serving requests |
| Serve browser response | N/A — HTTP middleware | Resolve route/environment-specific CSP, cache, referrer, transport and standard header policy | N/A — no domain event | Application/API receives compatible restrictive headers; unapproved cross-origin requests remain denied |

## Validation and permissions

Select and pin a supported local certificate/trust mechanism and TLS termination path compatible with repository Docker wrappers. Enrollment must be opt-in, show exact trust scope/hostnames/files, never require hidden privilege escalation, and provide status/removal/cleanup. Generated CA private keys, leaf keys, certificates, credentials, and machine-specific trust state remain ignored outside tracked source. Record platform/browser limitations honestly.

Use one canonical configured HTTPS origin for browser/application/API links and same-origin requests. Validate scheme, normalized host, optional approved port, path policy, no userinfo/query/fragment, and trusted proxy topology. Never trust arbitrary `Host`, `Forwarded`, or `X-Forwarded-*`; configure explicit proxy sources/hops. Production-like composition refuses insecure origin/proxy/cookie combinations before serving. Edge redirect policy for safe navigation and application rejection of insecure sensitive mutations must be explicit and tested.

Keep CORS disabled/deny-by-default: no wildcard/reflected origins or credentialed cross-origin API. Handle preflight and absent/malformed Origin consistently with accepted browser-auth ADR. Development tools use separate explicit composition/routes/origins and cannot broaden production API policy.

Define a route-compatible CSP from actual production assets with at least restrictive `default-src`, `script-src`, `style-src`, `connect-src`, `img-src`, `font-src`, `object-src`, `base-uri`, `form-action`, and `frame-ancestors`; use nonces/hashes or self-hosted static assets rather than broad `unsafe-inline`/`unsafe-eval` unless a narrowly evidenced transitional exception is separately recorded and blocking for closure. No mutable remote runtime resources. Add compatible content-type sniffing, referrer, permissions, framing, transport, and cross-origin isolation/resource headers where they do not break approved behavior. Apply HSTS only under safe HTTPS/environment semantics. Sensitive API/HTML routes use explicit no-store/private policy.

Keep Storybook, Swagger, mail catcher, development diagnostics, and any browser test hooks narrowly environment-scoped, authenticated/restricted as previously required, and absent/inert in production-like build/composition. Their CSP/header needs cannot weaken normal application routes.

Consolidate typed startup validation for JWT signing/key rotation, delivery encryption, CSRF, cookie names/scope/security, canonical app/mail origins, provider capabilities, trusted proxies, CSP nonce/hash inputs, and all required limiter policies. Reject absent/default/placeholder/malformed/weak/duplicated/incompatible values without printing them. Verify configured limits exist for login, activation, reset request/completion, refresh, invitation issuance, retry/resend/correction, and other approved delivery actions, while journey TASKs retain behavioral ownership.

No runtime permission grants access to infrastructure configuration. Startup/headers are enforcement, not evidence of endpoint authorization.

## Acceptance criteria

- [ ] Repository wrappers explicitly install/status/remove scoped local trust and start a trusted HTTPS stack without committing generated keys/certs/state.
- [ ] Trust enrollment is opt-in, reversible, documented, and records supported platform/browser limitations.
- [ ] Actual local HTTPS path proves Secure/HttpOnly/SameSite cookie issuance/rotation/deletion and same-origin application/API operation.
- [ ] Production-like startup rejects HTTP/ambiguous origins, unsafe proxy trust, weak/default/missing/malformed secrets, unsafe cookies, absent provider capability, and invalid limiter policies.
- [ ] Arbitrary Host/forwarded headers cannot alter canonical links, secure detection, cookie policy, redirects, or origin decisions.
- [ ] Application/API stay same-origin; unapproved CORS preflight/cross-origin credential requests fail without wildcard/reflection.
- [ ] CSP covers every production route/asset with no unjustified broad unsafe directive or mutable remote dependency.
- [ ] Framing, content-type, referrer, permissions, transport, cache and other selected headers are explicit, compatible, and route-tested.
- [ ] Credential-fragment routes and sensitive APIs apply no-referrer/no-store behavior appropriate to accepted contracts.
- [ ] Storybook/Swagger/catcher/test exceptions are narrow to explicit development/test composition and absent/inert in production-like startup/artifacts.
- [ ] Every required authentication/reset/activation/invitation delivery-action limiter has typed bounded secret-safe configuration validated at startup.
- [ ] Configuration/startup failures and diagnostics contain no key, secret, cookie, provider credential, connection string, SQL, or private environment value.
- [ ] Focused TLS/startup/header/cookie/CORS/proxy/configuration/composition checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Enroll trust in an explicitly authorized environment, record tool/browser versions and exact scope without private material, start the stack, inspect browser trust and HTTPS request/cookie behavior, then exercise documented status/removal/re-enrollment and cleanup. Inspect tracked/untracked/ignored files and production artifact for generated certificates/keys. Record environments where trust enrollment cannot be safely automated.

Boot production-like composition against absent/default/weak/malformed/duplicate signing/encryption/CSRF values, HTTP/userinfo/ambiguous origins, unsafe cookie/path/domain, arbitrary proxy headers/sources, missing provider, and each absent/invalid limiter. Exercise application/API/tool routes with header matrix, CSP violation collection in controlled tests, secure/insecure requests, safe redirects, mutations, preflights, foreign/null/malformed origins, caching and fragments. Confirm actual assets/features work without CSP exceptions.

Run focused TLS/proxy/configuration/composition/HTTP/security tests and browser header/cookie smoke where platform behavior requires it, production-install/artifact scans, architecture/style/static/coverage checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record wrappers/tool versions, origin/proxy/header/CSP/secret/limiter matrices, tests/assertions/routes, startup denial and redaction evidence, durations, warnings, and production enrollment deferrals.

## Coordination

This integrated envelope starts after representative tails TASK-00052, TASK-00055/00056, TASK-00058, and TASK-00062 so CSP/headers/configuration cover every journey. Preserve TASK-00017 ADR and TASK-00044/00045 controls, TASK-00040/00053 canonical delivery links, and TASK-00033 canonical gate. Journey defects remain owned there; do not paper over them with proxy behavior.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable HTTPS/security-envelope outcome.

## Completion notes

Not started. Record TLS/trust/proxy/origin/header/CSP/CORS/cache/configuration/limiter contracts, setup/removal/startup/browser evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and production enrollment limitations here.
