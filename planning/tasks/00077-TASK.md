---
id: TASK-00077
ticket: TICKET-00024
kind: feature
title: Accept the repository identity and link-consistency ADR
status: ready-for-agent
order: 74
blocked_by:
pr:
---

# Accept the repository identity and link-consistency ADR

## Outcome

An explicitly accepted ADR defines the stable Workspace/Repository/Host/Checkout/Worktree identity model, PostgreSQL transaction and uniqueness boundaries, conservative source/Git matching rules, and recoverable consistency protocol for database state plus the repository-local uncommitted Git marker before production registration changes begin.

## Scope

- In scope: Inspect accepted WF-008 semantics, ADR 0001/0002 boundaries, Fight Common COMB identity support, current PostgreSQL transaction capability, Git common-directory/config/worktree behavior and approved-root contract; decide aggregate ownership, current-state/event/audit persistence, identity generation, one initial Workspace/Host bootstrap, active slug/source uniqueness, source descriptor normalization/equivalence, matching evidence precedence, marker namespace/shape, preview/confirmation binding, database/marker ordering, idempotency, crash recovery, relink fencing, archive/restore conflicts, permission boundaries and downstream handoff.
- Out of scope: Production schema/code/API/UI, registering or linking a real repository, writing a durable Git marker, changing remotes/branches/source, Planning migration, Runner/Workflow authority, new-project creation, multi-workspace isolation, and implementing TICKET-00025 through TICKET-00029.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Decide registration consistency | N/A — architecture decision only | Inspect transaction, uniqueness, identity and audit contracts | N/A — no production event | Accepted ADR fixes the safe implementation boundary or records a blocking decision |
| Decide first checkout linking | N/A — architecture decision only | Inspect approved-root, Git common-directory, marker and confirmation behavior | N/A — no production event | Exact database/marker crash states and recovery rules are accepted without linking anything |
| Qualify matching evidence | N/A — disposable read-only Git observations | Compare clone, worktree, remote, marker, move and copy facts | N/A — observations emit no Domain event | Evidence precedence and automatic/confirm/refuse outcomes are documented without mutating source |
| Decide lifecycle conflicts | N/A — architecture decision only | Inspect active uniqueness and retained downstream-reference requirements | N/A — no production event | Archive/restore and source-change conflict policy preserves identity and history |

## Validation and permissions

Preserve WF-008: Agent OS mints COMB-backed typed identities; paths, names, slugs, remotes, provider IDs and markers are locators or evidence only. One Repository represents one intended collaboration in one Workspace. Checkout is one Git common directory on one Host; Worktrees sharing it retain distinct Worktree identities. A clone does not inherit Checkout identity, while a directory move may preserve an explicitly linked marker.

Define closed typed source descriptors with explicit normalization and equivalence semantics. Directory/slug similarity never matches. Existing durable links and shared common-directory evidence have priority; a unique source match only proposes the first link; fork/upstream, multiple, missing, changed, copied or conflicting evidence requires explicit confirmation or refusal. Provider-native immutable identifiers may strengthen evidence but never replace Repository identity.

Settle transactional uniqueness for active Workspace slugs and approved source identities, internally generated IDs, stale expected-state handling, audit attribution, and safe constraint translation. Reuse the one Doctrine/PostgreSQL connection and transaction authority from ADR 0002. Do not introduce reflection mapping, request-time schema mutation, client-selected normal IDs, a second identity authority, or filesystem policy in HTTP Actions.

PostgreSQL and `.git/config` cannot commit atomically. The ADR must enumerate every interruption point before/after confirmation, database mutation, marker observation/write and response; select an idempotent recoverable ordering with durable state/fencing where needed; define retry/reconciliation ownership; and prohibit silent rollback claims. Bind confirmation to actor, Repository, Host, exact canonical approved path, common-directory/marker/source observations, expected revisions and expiry. Changed evidence invalidates it.

Choose an Agent-OS-owned non-reserved Git configuration namespace and typed marker format. Inspect an existing value before writing. Never overwrite unknown/conflicting values, commit the marker, modify `.git/config` beyond the exact key, mutate branches/remotes/worktree files, or treat write success as database success. Define safe cleanup and operator escalation for every partial state.

Separate permissions for view, register, metadata/source change, link/relink, designate and archive/restore. Registration and linking do not grant Planning, Execution, Git publication or Agent authority. The future TICKET-00029 UI consumes commands/queries but owns no matching rule.

## Acceptance criteria

- [ ] ADR states why typed Agent-OS COMB identities, not paths/slugs/remotes/markers, own Workspace, Repository, Host, Checkout and Worktree identity.
- [ ] Aggregate, current-state, event/audit, PostgreSQL connection/transaction and active uniqueness boundaries are explicit and compatible with ADR 0001/0002.
- [ ] Source descriptor types, normalization, equivalence, fork/upstream handling and provider-native evidence are closed and deterministic.
- [ ] Matching table covers existing link, shared common directory, unique source proposal, multiple/fork/upstream/missing/changed source, move, reclone, copied marker and conflicting marker outcomes.
- [ ] Confirmation is bound to exact actor/identity/path/Git/source/revision evidence and expires or fails stale after any material change.
- [ ] Every PostgreSQL/Git-marker interruption point has an honest idempotent recovery or explicit refusal path without claiming cross-resource atomicity.
- [ ] Marker namespace, shape, inspection, write, conflict, cleanup and non-commit behavior are accepted and cannot alter branch, remote or working files.
- [ ] Archive/restore, source changes and relinks preserve identity/history and define active uniqueness conflicts without permanent deletion.
- [ ] Permissions and authority exclusions are explicit; downstream Planning, Execution, Dashboard and context consumers cannot infer extra authority.
- [ ] Human maintainer explicitly accepts the ADR before implementation TASKs proceed.

## Verification and evidence

Inspect installed Fight Common ID/transaction contracts and the actual PostgreSQL adapter direction without treating package planning as implemented behavior. In TASK-owned ignored scratch, create disposable local repositories/clones/linked worktrees and use read-only Git queries to record common-directory, local-config sharing, move, reclone, changed remote, copied marker and conflicting marker facts. Any disposable marker write stays inside owned scratch and is removed; do not touch registered or unrelated repositories.

Build a decision table and database/marker failure matrix covering competing registration, source change, first link, retry, stale confirmation, response loss, partial marker/database outcomes, relink and restore. Review security, audit, redaction, permission and cleanup implications directly. Run `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and documentation/ADR review; do not add product tests for architecture text, Git behavior, configuration, or decision tooling. Record inspected versions/commits, commands, observations, unresolved risks and explicit human acceptance.

## Coordination

This prefactor consumes accepted WF-008, ADR 0001, ADR 0002 and the planned approved-root contract without requiring its runtime implementation. TASK-00078 implements registration from the accepted ADR. TASK-00079 additionally waits for TASK-00073's actual approved-root capability before touching physical checkouts. Any required upstream package change is a separate authorized dependency, not a vendor patch.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. Keep all experiments under ignored TASK-owned scratch and preserve unrelated repositories. This TASK owns one independently reviewable ADR and stops before production implementation.

## Completion notes

Not started. Record inspected contracts and Git evidence, accepted identity/source/matching/transaction/marker/recovery decisions, alternatives, risks, changed files, planning/diff verification, explicit acceptance, review state, commit/PR details, and implementation handoff here.
