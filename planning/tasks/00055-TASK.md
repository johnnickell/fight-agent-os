---
id: TASK-00055
ticket: TICKET-00019
kind: feature
title: Deliver one-time password-reset completion
status: ready-for-agent
order: 55
blocked_by: TASK-00054
pr:
---

# Deliver one-time password-reset completion

## Outcome

A canonical public completion route captures password-reset authority transiently from a URL fragment, removes it from visible history before ordinary rendering, applies the shared password policy through a versioned API, and uses `AuthenticationService::resetPassword()` to consume one current grant, replace the Argon2id hash, advance authority, revoke every target-user session, and require fresh login.

## Scope

- In scope: Canonical public completion route; pre-render fragment capture/history replacement; transient credential owner/clearing; password/confirmation form and policy guidance; typed feature service; versioned Action/validated DTO/generic View/OpenAPI; shared structural/compromised checks; package reset orchestration; request/rate controls; same-user memory/cookie/cross-tab invalidation after success; generic invalid/used/expired/replaced recovery; accepted production states; PostgreSQL race/session/HTTP/history/storage/accessibility/redaction tests.
- Out of scope: Issuing a new access/refresh session, remembered-session choice, administrator password assignment, reset request/delivery internals, authenticated password change, external password checker, periodic expiration/history, preserving password fields through navigation, and broad browser automation.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Open completion link | N/A — client bootstrap/navigation | Read expected fragment fields once and validate route identity | Client-only history replacement | Credential is held only in transient memory and removed before ordinary rendering/navigation |
| Complete reset | `AuthenticationService::resetPassword(userId, credential, password)` after owned policy | Resolve latest one-hour grant, active identity, password policy, sessions, and authority | `PasswordResetCompleted` | Grant consumed, Argon2id hash/authority replace, all target sessions revoke, audit commits atomically |
| Handle target session invalidation | N/A — client authentication coordination | Compare transient target ID to safe current principal and observe terminal server authority | Client logout/invalidation signal | Same-user tabs clear cookie/memory; every old server credential fails; unrelated current identity is not silently trusted for target |
| Reject invalid completion | N/A beyond rejected service | Resolve only generic malformed/unknown/used/expired/replaced/raced outcome | Redacted package failure only | No identity/grant state leaks; transient secret clears and safe request-new-link guidance appears |

## Validation and permissions

TASK-00053 links carry only bounded user routing identity and reset credential in a URL fragment. A minimal pre-render bootstrap reads expected fields once, validates shape/size, stores them in one ephemeral memory owner, and immediately replaces visible/history URL before analytics, remote resources, screenshots, or ordinary route rendering. Never persist in local/session storage, IndexedDB, Cache Storage, service worker, HTML, runtime config, API cache, intended route, error object, telemetry, or snapshot. Apply no-referrer policy and clear references on success/terminal failure/unmount.

The public JSON endpoint accepts only explicit bounded user ID, reset credential, password, and confirmation/transport fields justified by validation. Apply Origin, Fetch Metadata, HTTPS, JSON/body controls, accepted CSRF policy, and atomic secret-safe reset rate limits. Grant possession supplies reset authority; no authenticated role/permission applies and ambient refresh cookies are not authority for this operation.

Client password checks are usability only. Server independently applies TASK-00039's exact untrimmed Unicode structural policy and local compromised-password checker before invoking package service. Confirmation mismatch is transport validation. Mark all secret parameters sensitive; do not serialize CQRS password/credential messages or send candidates externally.

Call `AuthenticationService::resetPassword()` directly for current latest purpose-bound grant matching, active user, one-hour usability, credential verification, grant consumption, Argon2id hash replacement, authentication-version/revision advancement, all-session revocation, audit, transaction, and event. No new TokenSet is issued. Invalid/malformed/unknown/used/expired/revoked/replaced/mismatched/raced cases map to one generic request-new-link state without identity detail. Policy feedback can explain candidate requirements without proving grant/account state.

After success, require Login. Existing access JWTs fail on authoritative version checks and every refresh session is revoked. If the client currently represents the reset target, expire matching refresh cookie, clear access/principal state, and broadcast terminal logout; do not persist identity just to coordinate. An unrelated authenticated principal cannot authorize the reset and must not be accidentally mutated, but completion success still navigates safely without exposing target state.

Recreate accepted responsive form and loading/policy/common-password/generic-invalid/expired/throttled/success/system states. Permit paste/password managers/Unicode/spaces; support labels/instructions, confirmation, accepted show-password behavior, keyboard/focus, announcements, themes, reduced motion, and recovery links.

## Acceptance criteria

- [ ] Canonical completion link keeps credential out of query/request line and applies no-referrer behavior.
- [ ] Pre-render bootstrap validates/captures fragment fields once and replaces visible/history URL before ordinary rendering/navigation.
- [ ] User credential/password never enters prohibited browser storage/cache/service worker/HTML/config/intended route/log/analytics/error/screenshot/snapshot surfaces.
- [ ] Versioned endpoint applies validated input, Origin/Fetch/HTTPS/JSON/CSRF policy and atomic secret-safe rate limits.
- [ ] Server applies TASK-00039 structural/common-password policy without trimming or external requests; client checks are non-authoritative.
- [ ] `AuthenticationService::resetPassword()` owns grant/user/session mutation; no duplicate reset policy/direct persistence exists.
- [ ] Success atomically consumes latest usable one-hour purpose-bound grant, stores Argon2id hash, advances authority, revokes all target sessions, records audit, and emits event.
- [ ] Completion issues no access/refresh token and always requires fresh Login.
- [ ] Old target access/refresh credentials fail immediately; same-user memory/cookie/principal state clears and terminal logout propagates across tabs.
- [ ] Invalid/unknown/used/expired/revoked/replaced/mismatched/raced grants receive one generic safe recovery class.
- [ ] Controlled duplicate completion and completion-versus-successor request races produce exactly one valid current outcome without sleeps.
- [ ] Accessible responsive form/states match accepted handoff and preserve paste/password-manager/Unicode behavior.
- [ ] OpenAPI marks sensitive write-only fields, generic failures, no session issuance, and required request protections truthfully.
- [ ] Focused PostgreSQL/concurrency/session/HTTP/history/storage/component/accessibility/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Open a real deterministic catcher link and prove the fragment never reaches server request logs/referrer and history is replaced before normal rendering. Exercise malformed/missing/oversized fields, reload/back/forward/direct route, unmount, policy failure, terminal clearing, and no-referrer behavior. Instrument all browser storage/cache/service-worker/error/telemetry/screenshot/snapshot surfaces with seeded credential/password markers.

Test every password-policy/checker boundary and request control. Use deterministic clock and separate PostgreSQL connections with controlled synchronization for valid, wrong-user/credential/purpose, one-hour boundary, used/revoked/replaced, duplicate completion, successor request, user lifecycle/version changes, and transaction failure around grant/user/session/audit. Verify all sessions become unusable and old access versions fail. Test same-user versus unrelated current-client invalidation.

Run focused backend/security/HTTP/PostgreSQL/concurrency/session and frontend type/lint/Vitest/RTL/Storybook/build checks, minimal browser history/referrer smoke, accessibility automation plus manual keyboard/reflow/theme observations, OpenAPI/storage/redaction scans, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record route/API/security contracts, race/session outcomes, tests/assertions/states, accessibility limits, redaction/history evidence, versions, durations, and warnings.

## Coordination

TASK-00054 supplies request/successor behavior and TASK-00053 canonical links. Reuse TASK-00039 policy, TASK-00043's secret-fragment bootstrap patterns, TASK-00045 request/rate controls, and TASK-00048 authentication invalidation without copying state owners. TASK-00056 separately owns authenticated current-password change.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable reset-completion journey.

## Completion notes

Not started. Record URL/bootstrap/history behavior, Action/DTO/View/OpenAPI, password/rate/request policy, package reset/session mapping, design states/accessibility, atomicity/race/storage/redaction evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, and commit/PR details here.
