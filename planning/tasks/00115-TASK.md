---
id: TASK-00115
ticket:
kind: chore
title: Remove redundant root index and placeholder and align path indentation
status: done
order:
blocked_by:
pr:
---

# Remove redundant root index and placeholder and align path indentation

## Outcome

The project root no longer carries an unnecessary navigation index, the populated `config/common/` directory no longer has a Git placeholder, and path assignments use consistent indentation.

## Scope

- In scope: Delete only the root `_index.md` and `config/common/.gitkeep`; indent the existing directory assignments in `config/parameters/paths.php` without changing their values or behavior; inspect `src/` and `tests/` for `_index.md` and the repository for other `.gitkeep` files.
- Out of scope: `.pi/`, `client/`, and `docs/` indexes; generated planning indexes and templates; changing configuration values, runtime behavior, dependencies, or tests.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Tidy navigation and configuration formatting | Delete two redundant files and reindent existing assignments | Inspect tracked indexes, placeholders, PHP syntax, and diff | N/A — no application event | Only named paths and this TASK's planning views change; PHP path values remain unchanged |

## Validation and permissions

No user-facing permissions or application validation apply: this chore changes a navigation aid, a placeholder in a populated directory, and whitespace. Preserve all other indexes and unrelated work. Pi loads project instructions and skills through their documented paths, not `_index.md`; verify removal directly rather than adding a product test for repository or configuration text.

## Acceptance criteria

- [x] Root `_index.md` is removed; no `_index.md` exists under `src/` or `tests/`, and other navigation indexes remain untouched.
- [x] `config/common/.gitkeep` is removed after confirming the directory is populated; no other tracked `.gitkeep` is unintentionally changed.
- [x] Existing `app.*_dir` path assignments in `config/parameters/paths.php` have consistent indentation and unchanged keys/values.
- [x] PHP syntax, planning views, diff whitespace, and canonical `./bin/build` pass; counts and limitations are recorded.

## Verification and evidence

Inspect `git ls-files '*_index.md' '*.gitkeep'` and `find src tests -name '_index.md'` before/after; compare the path-configuration diff to ensure only indentation changed. Run `php -l` in the repository container, `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Do not add product tests for formatting or navigation files.

## Coordination

Standalone user-approved chore; no EPIC or TICKET parent and no blockers. Use the user-selected main checkout on `feature/tidy-navigation-placeholders` from `develop`. Independent review and PR publication remain separate operations. Preserve shared containers, build output, and unrelated local work.

## Completion notes

User approved the standalone scope and selected the main checkout. Started from clean `develop` at `c63dabc978c1f9075dbb266b525cde269c303e98` on `feature/tidy-navigation-placeholders`. First implementation: canonical ignored `.runs/reviews/TASK-00115/review.md` and its history directory were absent; the sole registered worktree's canonical Git directory is the common directory. No unrelated tracked/untracked changes were present.

Deleted the root `_index.md` (not a Pi resource) and the sole `.gitkeep` under populated `config/common/`. Direct `find src tests -name _index.md` returned no entries; `.pi/`, `client/`, and `docs/` navigation indexes remain. Reindented nine existing `app.*_dir` assignments in `config/parameters/paths.php`; the diff contains no changed key, expression, or value. Generated `planning/tasks/BOARD.md` and `planning/tasks/README.md` for this standalone chore. No product test was added for documentation or formatting, and no application behavior changed.

Fresh checks: `./bin/exec php -l config/parameters/paths.php` passed with no syntax errors; `./bin/planning-check --write` and `./bin/planning-check` passed with 154 records / 129 active and current views; `git diff --check` passed. Canonical `./bin/build` passed Composer manifest validation, planning 154 / 129, Deptrac (0 violations, skipped violations, uncovered dependencies, warnings, and errors; 123 allowed dependencies), and PHPUnit (16 tests / 201 assertions on PHP 8.5.10). No warnings, deprecations, skips, or test failures were reported. The PHP test counts cover existing application behavior, not new tests for this chore. After updating this record, the planning refresh and read-only validation, PHP lint, whitespace check, and full build were rerun with the same passing counts before the implementation commit. Independent review accepted committed implementation `4d7e6a295c35dbc9d2a4bac30168d9139e6d0e4d` against `develop` at `c63dabc978c1f9075dbb266b525cde269c303e98` on a clean checkout: `.runs/reviews/TASK-00115/review.md` (immutable `review-00001.md`, verdict `accept`). It supports all four acceptance criteria and records no blocking or non-blocking findings; there is nothing to resolve or defer. Its historical pre-change evidence is distinguished from fresh checks; no residual implementation risk is reported. Shared Compose services, vendor/cache output, and unrelated ignored review history remain untouched.

Landing pre-finalization checks on the reviewed commit: `./bin/exec php -l config/parameters/paths.php` exit 0, no syntax errors; `./bin/planning-check` exit 0 (154 records, 129 active, views current); `git diff --check develop...HEAD` exit 0; `./bin/build` exit 0 (Composer manifest valid, planning 154 / 129, Deptrac 0 violations, 0 skipped, 0 uncovered, 123 allowed, 0 warnings/errors; PHPUnit 16 tests / 201 assertions on PHP 8.5.10). No reported warnings, skips, deprecations, or failures. The suite is inherited application behavior, not a new formatting test. After this status/evidence update and planning refresh, `./bin/exec php -l config/parameters/paths.php`, `./bin/planning-check` (154 records / 128 active; views current), `git diff --check` (working tree and `develop...HEAD`), and final `./bin/build` all exited 0. The gate again reported Composer valid, planning 154 / 128, Deptrac 0 violations/skips/uncovered/warnings/errors and 123 allowed, PHPUnit 16 tests / 201 assertions on PHP 8.5.10; no warnings, skips, deprecations, or failures reported. Completion means accepted implementation and local verification only; publication will be recorded separately. The only planned landing changes are this TASK's status/evidence, generated planning views, and eventual PR metadata. There are no ownership-proven TASK-specific containers, volumes, processes, databases, or disposable build artifacts to remove; retain the canonical checkout, shared services/volume, `vendor/`, `var/`, `.idea/`, and ignored review reports. Human review/approval and merge remain pending; branch deletion, archive, release, deployment, and certification are not authorized.
