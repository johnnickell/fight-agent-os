---
id: TASK-00045
ticket: TICKET-00017
kind: feature
title: Enforce browser authentication request controls
status: ready-for-agent
order: 45
blocked_by: TASK-00043
pr:
---

# Enforce browser authentication request controls

## Outcome

Reusable server controls enforce the accepted refresh-cookie lifecycle, CSRF protocol, exact trusted Origin, Fetch Metadata, JSON-only requests, HTTPS/same-origin assumptions, production `LoginThrottle`, and atomic bounded endpoint rate limiting before login, refresh, or logout can mutate authentication state.

## Scope

- In scope: Typed cookie policy/factory/deletion; ordinary/remembered persistence and exact expiry mapping; accepted CSRF issue/rotation/validation contract and endpoint if required; exact Origin and Fetch Metadata middleware; method/content-type/JSON shape guards; trusted proxy/HTTPS configuration constraints; production package `LoginThrottle`; reusable login/refresh/logout rate limiting; safe rejection/error correlation; startup validation; focused security/concurrency/redaction tests and OpenAPI components.
- Out of scope: Login/refresh/logout Actions/UI, token claim validation, general cross-origin support, credentials in JavaScript, CAPTCHA, WAF/proxy deployment, arbitrary global rate-limiting framework, and changing accepted ADR controls.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Construct refresh cookie | N/A — transport adapter | Read TokenSet/session policy and trusted cookie configuration | N/A — no domain event | Opaque credential is emitted only as matching secure HttpOnly session/persistent cookie |
| Protect auth request | N/A until guards pass | Validate method/content type/body, CSRF, Origin, Fetch Metadata, HTTPS, and limiter | N/A — no domain event | Rejected request reaches no authentication service and receives safe failure |
| Throttle credential verification | Package login calls `LoginThrottle::allows()` | Atomically consume bounded canonical-email attempt dimension | N/A — no domain event | Excess attempts are denied without identity enumeration or raw-key storage |
| Delete refresh cookie | N/A — transport adapter | Read exact cookie identity attributes | N/A — no domain event | Browser receives expiry using attributes matching issuance |

## Validation and permissions

Implement exactly the CSRF mechanism and lifecycle accepted in TASK-00017's ADR. CSRF material is unpredictable, bounded, non-authoritative, separate from refresh/session/access credentials, and compared safely. Define issuance/bootstrap, rotation, expiry, multi-tab behavior, and invalidation. Never put authority/refresh tokens into CSRF state.

Require an exact configured trusted Origin for browser auth mutations, with documented handling for absent Origin according to accepted profile. Parse, normalize, and compare origins without suffix/wildcard/userinfo ambiguity. Enforce Fetch Metadata combinations where supported while retaining Origin/CSRF as authoritative controls. Reject cross-site and impossible/malformed values before service dispatch.

Accept only approved methods and `application/json` media type/charset/body bounds with expected object shape. Reject form/multipart/text, empty/unexpected bodies, method overrides, and oversized input. Require trusted HTTPS in production and validate proxy trust explicitly; do not trust arbitrary forwarded headers. Same-origin deployment remains CORS deny-by-default.

Refresh-cookie name/domain/path/Secure/HttpOnly/SameSite and prefix follow ADR. Ordinary sessions use a browser-session cookie; remembered sessions use explicit expiry bounded by package absolute expiry. Replacement/deletion use matching identity attributes. Cookie values never enter JSON, logs, errors, metrics, audit, or browser-readable APIs.

Implement `LoginThrottle` with real atomic persistence/expiry and safe one-way/bounded canonical-email keying. Because the package port only asks `allows()`, document whether each call consumes an attempt and test success/denial behavior. Layer safe network/actor endpoint limits where accepted, reusing TASK-00041's rate infrastructure rather than creating incompatible stores. Unknown and known identities receive equivalent public behavior.

Production startup fails closed for missing/weak CSRF, invalid cookie/origin/HTTPS/proxy settings, or unavailable mandatory limiter. No end-user permission applies; these controls precede authentication.

## Acceptance criteria

- [ ] Refresh cookie is Secure, HttpOnly, narrowly scoped, accepted SameSite/prefix, and never exposed through JSON/JavaScript.
- [ ] Ordinary cookie has no persistent expiry; remembered cookie expiry never exceeds its one-year absolute session boundary.
- [ ] Cookie rotation/deletion use matching name/domain/path/security attributes and clear obsolete variants according to policy.
- [ ] Accepted CSRF issuance/rotation/expiry/comparison/invalidation is implemented with non-authoritative secret-safe material.
- [ ] Exact Origin and Fetch Metadata checks reject cross-site/malformed/disallowed requests before authentication service invocation.
- [ ] Only approved methods and bounded JSON content/body shape pass; form/multipart/text/method override/oversized input fails safely.
- [ ] Production HTTPS/proxy trust and same-origin/CORS-deny configuration is explicit and fails closed when unsafe.
- [ ] A production atomic `LoginThrottle` implements the package port without raw email/password retention or identity enumeration.
- [ ] Login/refresh/logout endpoint limits are bounded/configurable/concurrency-safe and return safe retry behavior.
- [ ] Production-like startup rejects missing/weak/invalid CSRF, cookie, Origin, HTTPS/proxy, or limiter configuration.
- [ ] Failures are correlated and sanitized without cookies, CSRF values, passwords, email keys, SQL, stack traces, or internals.
- [ ] Focused cookie/CSRF/Origin/Fetch/JSON/throttle/concurrency/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Build a request-control matrix across methods, content types/parameters, body sizes/shapes, CSRF absent/wrong/expired/replayed/rotated, exact/foreign/null/malformed origins, Fetch Metadata values/absence, HTTPS/proxy combinations, and CORS preflights. Prove rejection before authentication-service calls. Inspect Set-Cookie for ordinary/remembered/rotation/deletion and test browser parsing assumptions.

Use deterministic clocks and separate PostgreSQL connections for LoginThrottle and endpoint-limit boundaries/concurrency/expiry/isolation. Compare known/unknown identity responses. Seed cookie/CSRF/password/email/SQL markers and inspect response/log/correlation/audit/snapshot surfaces. Boot production-like invalid configurations safely.

Run focused security/HTTP/PostgreSQL/concurrency/composition tests, architecture/style/static/coverage/OpenAPI checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record control/cookie/limiter matrices, configuration shape without secrets, tests/assertions/races, redaction, versions, durations, warnings, and proxy/environment limitations.

## Coordination

TASK-00043 supplies initial activation cookie/request seams; consolidate rather than duplicate. Reuse TASK-00041's general rate-store capability where suitable. TASK-00046 through TASK-00047 apply these controls, and TASK-00048 consumes CSRF/refresh behavior across tabs. TASK-00044 may proceed independently after the shared activation prerequisite.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable browser-auth request-control outcome.

## Completion notes

Not started. Record cookie/CSRF/origin/fetch/JSON/HTTPS/throttle configuration and matrices, startup/concurrency/redaction evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and residual deployment assumptions here.
