---
id: TASK-00061
ticket: TICKET-00021
kind: feature
title: Deliver the ordinary web invitation journey
status: ready-for-agent
order: 61
blocked_by: TASK-00060
pr:
---

# Deliver the ordinary web invitation journey

## Outcome

The accepted Invite User route uses TASK-00041's versioned API to let an authoritative actor with exact `INVITE_USERS` submit one canonical email, always select managed `ROLE_USER` server-side, create the pending identity/assignment/seven-day activation/durable delivery/audit through existing orchestration, and reach a truthful success or permitted status destination without exposing invitation authority.

## Scope

- In scope: Protected ordinary invitation route and exact permission metadata; typed existing-API client service/runtime decoding; canonical email form; fixed ordinary request mode with no role input; pending/validation/conflict/throttle/success/system states; safe success navigation conditional on manage permission; directory/status cache invalidation; accepted responsive components/Storybook/accessibility; integrated permission/PostgreSQL/HTTP/UI/redaction tests.
- Out of scope: New invitation backend endpoint/orchestrator, elevated invitation, arbitrary role/permission choice, public registration, bulk import, administrator password, invitation recovery implementation, provider choice, delivery-receipt claim, credential/link display, and broad browser automation.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Invite ordinary user | Existing TASK-00041 Action/orchestrator dispatches package invitation/assignment | Resolve authoritative actor, canonical email, managed `ROLE_USER`, identity/policy state, limiter | `UserInvited`, `RoleAssignedToUser` as applicable | Pending ordinary identity, audit, seven-day grant, and package-owned recoverable delivery state reach one package-valid outcome |
| Reject invalid/conflicting invite | N/A beyond existing API request | Validate email/authority/policy/existing identity/rate | Safe existing API failure | No client claim of partial success; form offers controlled correction/retry |
| Continue after success | N/A — navigation/cache effect | Evaluate exact `MANAGE_USER_INVITATIONS` and registered routes | Client invalidation/navigation signal | Directory refetches; authorized manager may open status, otherwise receives safe success guidance |

## Validation and permissions

Complete route metadata and TASK-00041 server Action both require exact `INVITE_USERS`; actor is the request-scoped current principal. Do not infer access from `ROLE_SUPER_ADMIN`, client visibility, JWT/HTML, or submitted actor. Unknown/stale authority fails closed. Client checks are presentation only.

The form accepts one bounded canonical email. The request uses only TASK-00041's ordinary invitation contract and offers no role ID/name, permission list, elevated flag/mode, confirmation override, actor, user ID, password, credential, template, provider, lifetime, or return URL. Server always resolves stable managed `ROLE_USER`; client copy may explain this but cannot determine assignment authority.

Use existing API/orchestration for canonical conflicts, pending/active/disabled/deleted state, policy drift, role references, atomic rate limit, assignment/audit/grant/delivery transaction behavior, and safe errors. Do not pre-query identity for availability or duplicate package policy. Success means accepted authoritative creation/registration, not provider delivery or recipient receipt.

After success, invalidate user directory. If current principal has exact `MANAGE_USER_INVITATIONS` and response includes approved safe target ID, offer/navigate to TASK-00060 status. Otherwise show generic safe success and Users/Dashboard destination permitted by exact route metadata. Do not broaden status data based solely on inviting authority. Suppress stale/duplicate submission results with request generations.

Recreate accepted form with email autocomplete, paste, labels/instructions, visible focus, keyboard submission, pending duplicate prevention, validation/conflict/throttle/success/system announcements, long/Unicode content, narrow/wide, themes, reduced motion, and safe recovery. Do not put email form state into persistent storage/history unless a separately approved URL representation exists; never retain credentials because none are returned.

## Acceptance criteria

- [ ] Route presentation and existing server Action independently require exact authoritative `INVITE_USERS` with no role-name bypass.
- [ ] Actor cannot be supplied/overridden by form and stale/unknown authority fails closed.
- [ ] Form/request accepts only canonical email and exposes no arbitrary/elevated role, permission, actor, password, credential, template/provider, lifetime, or redirect input.
- [ ] Existing TASK-00041 operation always assigns stable managed `ROLE_USER`; ordinary flow cannot elevate under malformed/hidden input.
- [ ] Existing/case-equivalent/reserved/pending/active/policy-drift/throttled/concurrent outcomes use safe controlled API/UI results.
- [ ] Success copy distinguishes committed invitation/delivery work from provider delivery and recipient receipt.
- [ ] Directory invalidates after success; status continuation appears only with exact `MANAGE_USER_INVITATIONS` and safe target ID.
- [ ] Duplicate/out-of-order submissions cannot create misleading UI or overwrite a newer result.
- [ ] Credential/grant/digest/ciphertext/link/provider/audit/internal data remains absent from API-derived client state, UI, diagnostics, history, and storage.
- [ ] Responsive form/states meet accepted email autocomplete/paste, semantic, keyboard/focus, announcement, theme, target, and reflow requirements.
- [ ] Focused permission/PostgreSQL/race/HTTP/client/component/accessibility/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Exercise every relevant permission combination and misleading role, stale authority, actor/role/elevated/permission/password/provider field injection, malformed/oversized/Unicode/case-equivalent email, duplicate lifecycle states, policy drift, limiter boundaries, and concurrent invitation. Inspect exact persisted managed role, pending state, audit, seven-day package-owned grant/delivery state, absence of a consumer intent, and safe response without duplicating TASK-00041 tests unnecessarily.

Drive client submit/cancel/navigation, duplicate click, delayed/out-of-order success/error, actor losing permission, status permission present/absent, directory invalidation, and unmount. Render narrow/wide/system/light/dark/reduced-motion/conflict/throttle/success states and test labels, autocomplete, keyboard/focus, announcements, targets, and reflow. Seed grant/credential/link/provider/SQL/stack markers and inspect DOM/state/messages/history/storage/logs/analytics/screenshots/snapshots.

Run focused backend integration/permission/PostgreSQL/HTTP support checks and frontend type/lint/Vitest/RTL/Storybook/build, accessibility automation plus manual observations, storage/redaction scans, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record request/UI/state contracts, permission matrix, tests/assertions/stories, persisted outcome, accessibility limits, redaction evidence, versions, durations, and warnings.

## Coordination

TASK-00060 supplies permitted status destination and TASK-00059 user-directory cache. Reuse TASK-00041's ordinary invitation endpoint/orchestration, TASK-00040 delivery, and TASK-00036 managed `ROLE_USER`; do not recreate backend policy. TASK-00062 extends only through a separate elevated route and cannot add elevation to this ordinary form.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable ordinary web-invitation journey.

## Completion notes

Not started. Record route/service/form/state/navigation contracts, permission/persistence/race/accessibility/storage/redaction evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and elevated-flow isolation here.
