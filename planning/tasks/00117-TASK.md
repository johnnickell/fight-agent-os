---
id: TASK-00117
ticket: TICKET-00010
kind: feature
title: Guard API-v1 Actions with JWT and permission attributes
status: ready-for-agent
order: 49
blocked_by: TASK-00020, TASK-00044
pr:
---

# Guard API-v1 Actions with JWT and permission attributes

## Outcome

Every registered `/api/v1` route defaults to TASK-00044 authoritative Bearer authentication, and matched FQCN Actions declare an explicit public/credential-flow exception, authenticated access or conjunctive `#[RequiresPermission]` metadata checked before instantiation or dispatch. A genuine protected `/api/v1/me` query with a safe no-store current-principal View proves the gate before other protected routes exist. This is an early HTTP gate, not the sole policy boundary.

## Scope

- In scope: Slim route group and routing order; final FQCN Action registration/metadata inspection without resolving all Actions; JWT context from TASK-00044; exact request-scoped permission evaluator; explicit public bootstrap/credential-flow markers; default-deny missing/invalid metadata; multiple permission conjunction; first protected `GET /api/v1/me` Action/query with safe identity and current role/permission projection, no-store JSend/OpenAPI, 401/403, route/method misses and no-dispatch proof; focused tests.
- Out of scope: Implementing JWT cryptography, login, permission policy/catalog, owner/target-role decisions, role-name bypasses, `/mcp` HMAC, test-only product endpoints, or claiming HTTP attributes authorize non-HTTP package callers.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Project current principal | None | Inspect matched Action metadata; use `CurrentPrincipalProvider` and package-owned safe identity lookup | None | `/api/v1/me` returns current authorized safe no-store View without trusting role claims |
| Dispatch permitted Action | Existing Action owns its use case | Inspect matched Action metadata and current principal/permissions | Owning use case only | Exactly one authorized Action is instantiated and dispatched |
| Reject missing/stale/insufficient authority | None | Resolve TASK-00044 authoritative principal and exact permissions | None | Safe 401/403 before Action construction or package dispatch |
| Admit explicit public credential flow | Only the endpoint's own use case | Inspect the single matched public Action marker | Endpoint-owned event only | CSRF bootstrap and separately controlled credential flows bypass JWT alone, not their own security guards |

## Validation and permissions

After Slim route selection and before Action resolution, inspect the matched FQCN class without instantiating it. Default API-v1 to Bearer; require one explicit marker for public/credential-controlled or authenticated routes, or `#[RequiresPermission]` with one or more valid catalog names. Unmarked, conflicting, unknown or malformed metadata fails closed. Public exceptions are allowlisted by actual Action identity, not URL-pattern string or inherited blanket group bypass. A permitted attribute checks current request-scoped authoritative permissions, not JWT role claims, role names, client state, or stale cached snapshots. Revocation or permission changes between requests fail closed.

Credential-controlled login/refresh/logout/activation/reset retain their separate CSRF/Origin/Fetch/JSON/rate/grant policies; an explicit public marker never declares them unprotected. Owner/self checks, package target authorization, tier delegation rules, last-admin protection and confirmation/audit remain inside application/package services. Do not register protected API endpoints before this boundary exists. Leave `/mcp` to future independent HMAC authentication; preserve root/web routes and honest route/method misses.

## Acceptance criteria

- [ ] A matched FQCN Action is inspected post-routing without eager instantiation of unrelated Actions.
- [ ] API-v1 denies missing/conflicting/invalid security metadata by default; only explicit credential-flow Actions bypass JWT.
- [ ] `/api/v1/me` is a genuine authenticated, no-store FQCN Action with a safe package-backed View; it never includes token/session/grant/secret material and requires no `VIEW_DASHBOARD` permission.
- [ ] Authenticated Actions check request-scoped current authority before instantiation and dispatch; one-/multiple-permission metadata is evaluated through a reusable permission guard, with HTTP allow/deny proven when real protected operations are registered.
- [ ] Anonymous, invalid/stale/revoked JWT and insufficient permissions return sanitized correlated 401/403 with zero Action/package work.
- [ ] Permission or session changes between requests take effect; role names/JWT roles cannot bypass checks.
- [ ] Route/method misses, web/root, and future `/mcp` cannot become JWT exceptions or false authentication.
- [ ] Package/application self, target, grant-tier and audit rules remain separately enforced by owning operations.
- [ ] Focused real-route security/HTTP tests and `./bin/build` pass with fresh evidence and warnings.

## Verification and evidence

Exercise the real bootstrap and `/api/v1/me` with anonymous/valid/revoked/stale authority and safe View projection; prove no Action construction/query dispatch on rejection and current authority on successive requests. Verify permission conjunction against the evaluator at its owned behavior boundary; later protected operations prove real HTTP permission allow/deny and no-dispatch, not a fixture route. Use the owning HTTP/integration tools, not product tests of route configuration text or deliberately invented routes. Validate Slim routing and middleware order, `./bin/planning-check`, `git diff --check`, and `./bin/build`; record counts, warnings, exclusions and any protected interaction not yet registered at this TASK's delivery.

## Coordination

TASK-00020 supplies safe failures, TASK-00044 supplies authoritative Bearer/principal resolution, TASK-00018 supplies only the explicitly public CSRF bootstrap. This TASK owns the first real `/me` server query/View and API security gate; TASK-00049 later adds typed client/current-principal cache integration without a duplicate backend endpoint. Every protected Action must compose through this gate and also enforce its package/application target policy. Prove permission-attribute HTTP matrices with the first subsequent real permission-protected route; do not invent a fixture route or mark future enforcement as tested. Upstream Fight Access Control tier enforcement is separate and cannot be replaced by this middleware.

## Completion notes

Not started. Record actual Actions/metadata, `/me` projection and safe View, ordering, authentication/no-dispatch matrix, reusable permission-evaluator verification, focused/full results and warnings, review state, commit/PR, and real permission-protected HTTP matrix deferred to its first owning Action.
