---
id: TASK-00010
ticket: TICKET-00008
kind: feature
title: Prove and enforce application ownership boundaries
status: done
order: 10
blocked_by: TASK-00009
pr: https://github.com/johnnickell/fight-agent-os/pull/14
---

# Prove and enforce application ownership boundaries

## Outcome

A small representative composition path demonstrates direct Fight Access Control public-contract use with injected capabilities and CQRS composition, while deterministic architecture checks enforce the accepted ownership and dependency directions. Feature-specific endpoints, schemas, and product behavior remain absent.

## Scope

- In scope: Select the minimum non-user-facing package-owned behavior needed to prove direct public-type composition; wire its handler/capabilities through the existing composition/CQRS boundary; add focused tests; configure Deptrac or an equivalent deterministic architecture check for currently enforceable ownership/layer rules; prove positive structure and seeded invalid dependencies; document rules deferred to later representative HTTP/persistence/delivery code.
- Out of scope: Authentication, invitation, session, or authorization journeys; public endpoints and representations; database schemas/repositories; external delivery; React; event sourcing; package-type renaming wrappers; speculative application contexts; complete style/static-analysis/coverage gate expansion.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Compose package-owned behavior | Reuse an appropriate Fight Access Control command directly, or justify N/A for a query-only proof | Reuse an appropriate package query/View directly, or justify N/A for a command-only proof | Preserve package events at package ownership; do not translate merely to rename | Composition resolves package handler and injected capability without reversing dependencies or exposing a feature endpoint |
| Dispatch through CQRS | Existing Fight Common command/query bus contracts | Inspect routing from public message type to handler | Existing event dispatch contract where selected behavior emits events | Deterministic test proves message routing and capability invocation through the composition root |
| Enforce dependency direction | N/A — architecture analysis is tooling | Inspect owned namespaces, imports, paths, configuration, and package dependencies | N/A — no application domain event | Valid structure passes and prohibited inward/ownership dependencies fail deterministically |
| Report unenforced boundaries | N/A — architecture evidence | Compare accepted ADR rules with what current source/tooling can inspect | N/A — no application domain event | Gaps name their reason and later owning TICKET rather than being presented as enforced |

## Validation and permissions

Choose the smallest Fight Access Control public command/query/handler contract that can prove composition without implementing a feature journey or persistence schema. Use package commands, queries, Views, events, policies, and capability interfaces directly. Any owned `App\Application` type must document a genuine cross-boundary/application-specific responsibility; omit it when package behavior alone suffices. Capabilities are constructor-injected; business code must not access the service container.

Architecture checks must enforce currently applicable rules: Domain cannot depend on Application, Adapter, framework, persistence, or transport; Application cannot depend on Adapter/framework implementations; Adapter may depend inward and implement capabilities; configuration/composition is the explicit outer wiring boundary; owned source must not copy Fight namespaces or create generic alias wrappers. Tool configuration and justified exceptions must be narrow, readable, and deterministic.

Do not manufacture placeholder Actions, Responders, repositories, or contexts merely to make a rule testable. Action–Domain–Responder, transport safety, transaction/effect behavior, and authorization checks that lack representative code must be listed honestly for TICKET-00009 through TICKET-00011 and final quality-gate closure. Architecture tooling may begin here; TICKET-00013 owns complete gate integration and final policy.

No user-facing permission model applies because no endpoint is introduced. Repository source, composition, tests, and focused architecture tooling are authorized; product behavior, schemas, external effects, and upstream changes are not.

## Acceptance criteria

- [x] Representative composition uses Fight Access Control public Domain/Application types directly and does not duplicate or rename them.
- [x] Selected package behavior and its injected capabilities are documented as a composition proof, not a user-facing feature.
- [x] CQRS routing resolves the selected message/handler through explicit outer composition without service-location from business code.
- [x] Any `App\Application` orchestration is justified by a genuine cross-boundary concern; otherwise no speculative orchestrator is added.
- [x] Deterministic architecture checks enforce the currently applicable Domain/Application/Adapter, composition-root, and package-ownership rules from the accepted ADR.
- [x] Positive production examples pass and seeded invalid dependencies or equivalent isolated fixtures fail with clear diagnostics.
- [x] Checks reject copied `Fight\Common`/`Fight\AccessControl` namespaces and generic renaming wrappers within owned source to the extent deterministically enforceable.
- [x] No feature endpoint, persistence schema, external provider effect, role-name bypass, entity transport exposure, or speculative context is introduced.
- [x] Every accepted ADR rule not yet enforceable is documented with reason and a downstream owner.
- [x] Focused composition tests, architecture checks, and `./bin/build` pass with tool versions, counts, warnings, and exceptions recorded.

## Verification and evidence

Record the selected package contract and why it is the minimum safe proof. Test message-to-handler routing, injected capability use, result/event behavior as applicable, and absence of container access from business code. Run architecture analysis against the real source. In isolated TASK-owned fixtures or a non-mutating tool facility, demonstrate at least one valid dependency and representative failures for prohibited inward and package-ownership directions; do not leave invalid source in production paths.

Run focused PHPUnit composition tests and architecture checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record tool/config versions, passing/failing fixture evidence, test/assertion counts, warnings, justified exceptions, and rules deferred to later TICKETs. Verify configuration directly rather than adding product tests solely for configuration wording.

## Coordination

TASK-00009 must first produce an explicitly accepted ADR. Preserve TASK-00008 smoke and production-source boundaries. Coordinate any early architecture-tool dependency/configuration so TICKET-00013 can extend rather than replace it. Keep seeded-invalid fixtures and diagnostic output beneath a TASK-owned `.runs/` path unless the selected tool requires a narrowly scoped committed fixture.

This TASK owns one independently reviewable composition/enforcement outcome. Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen, and arrange independent review before landing.

## Completion notes

Selected Fight Access Control's query-only `ListPermissions` behavior as the minimum safe composition proof. The outer query configuration maps the package `ListPermissions` message directly to its package `ListPermissionsHandler`; the handler receives the package `PermissionRepository` capability by constructor injection. The integration test binds only a recording test fake, dispatches through the configured Fight Common `QueryBus`, proves the requested pagination reaches the capability once, and receives the package-owned `PermissionView`. Production intentionally leaves the repository implementation unbound for TICKET-00009. No command, event, endpoint, schema, provider effect, owned `App\Application` orchestrator, wrapper, or speculative context was introduced.

Added Deptrac 4.7.2 as a focused development tool, `deptrac.php`, `bin/architecture`, and an architecture source-contract checker. The production check passed with 0 violations, 0 skipped, 0 uncovered, 33 allowed dependencies, 0 warnings, and 0 errors across three owned PHP files. One-time ignored fixtures produced the expected two Deptrac failures (`App\Domain` to `App\Adapter` and `App\Application` to Slim) and three ownership failures (copied Fight Access Control namespace, package-name wrapper collision, and Application service location). The source check also rejects analysed-root symlinks. Semantic wrapper intent beyond deterministic type-name collision remains review-enforced.

Documented the proof and deferred boundaries in `docs/engineering/OWNERSHIP.md`: TICKET-00009 owns persistence and transaction authority; TICKET-00010 owns representative HTTP/transport checks; TICKET-00011 owns recoverable external effects; feature TICKETs own concrete authorization evidence; and TICKET-00013 owns the complete style, static-analysis, Rector, coverage, and architecture gate. Composition-root registration is explicit in `config/common/query.php`; no business source locates services.

Changed authored files are `ARCHITECTURE.md`, `composer.json`, `composer.lock`, `config/common/query.php`, `bin/architecture`, `deptrac.php`, `docs/engineering/OWNERSHIP.md`, `scripts/architecture-contract-check.php`, the integration composition test, and this TASK. Generated planning views are refreshed separately.

Fresh focused verification passed on PHP 8.5.10: the composition test passed with 1 test / 11 assertions; the full suite passed with 5 tests / 48 assertions; direct syntax checks passed for all four changed PHP/configuration files; strict Composer validation passed; and the focused architecture check produced the counts above. The canonical `./bin/build` passed with strict Composer validation, the locked 106-package development graph, no known vulnerability advisories, current planning validation (95 records / 80 active), 5 PHPUnit tests / 48 assertions, a fresh 67-package production install, positive production contracts, and all intended copied-namespace and source-symlink rejection probes. Composer emitted informational funding notices for 76 development and 45 production packages. No PHPUnit warning, skip, deprecation, security advisory, architecture warning, skipped violation, required-check failure, or justified Deptrac exception was observed.

The initially invoked Composer dependency command targeted the base checkout instead of the selected isolated worktree. Its two tracked edits were copied into this TASK branch, then restored from the clean base; installing the base lock removed the 13 transient development packages, and the base returned to a clean `develop`. Composer reported Git's container-only dubious-ownership warning during those wrapper runs, but dependency resolution completed. The unrelated `planning/complete-agent-os-vision` worktree remains untouched.

Implementation is complete on `feature/application-ownership-boundaries`; independent review, publication, PR, merge, release, and deployment remain pending and unclaimed. Commit details are recorded after the implementation commit.

Review sequence 1 returned `revise` with one medium blocking finding: Deptrac reported otherwise-unclassified dependencies but exited successfully, allowing an unknown framework namespace from owned Domain/Application code to evade the claimed boundary. The revision makes `bin/architecture` both report and fail on uncovered dependencies. A new ignored TASK fixture using `App\Domain\Fixture\UnknownFrameworkDependency` → `PHPUnit\Framework\TestCase` now exits 1 with one named uncovered dependency, while the production source still passes with 0 violations, 0 skipped, 0 uncovered, 33 allowed, 0 warnings, and 0 errors. The existing known-layer fixture still exits 1 with two clear prohibited dependencies, and the focused composition test still passes with 1 test / 11 assertions. This disposes the sole persisted blocking finding without changing application behavior or broadening scope.

Fresh revision verification passed on PHP 8.5.10. `./bin/architecture` reported 0 violations, 0 skipped, 0 uncovered, 33 allowed, 0 warnings, and 0 errors over three production PHP files; the unclassified-framework fixture exited 1 with one explicit uncovered dependency, and the existing known-layer fixture exited 1 with two explicit prohibited dependencies. The focused composition test passed with 1 test / 11 assertions, strict Composer validation passed, planning validation reported 95 records / 80 active, and worktree/range `git diff --check` passed. After refreshing this completion record, the canonical `./bin/build` passed with the locked 106-package development graph, no known vulnerability advisories, 5 PHPUnit tests / 48 assertions, a fresh 67-package production install, and all expected production-contract rejection probes. Informational Composer funding notices covered 76 development and 45 production packages; no PHPUnit warning, skip, deprecation, security advisory, architecture exception, or required-check failure was observed. Revision-authored changes are limited to `bin/architecture`, `docs/engineering/OWNERSHIP.md`, and this completion record; ignored fixture/log updates remain TASK-owned scratch.

Implementation commits `3b40acec816494d413096fdcfbbd3f74b344945c` and `9f210b8695d51c296eab4ed6e913f925c992a210` were independently reviewed against `develop` at `640ab29cd25a7bf4f8b3294da3c691a3ef1529d4`. Review sequence 2 returned `accept` for exact head `9f210b8695d51c296eab4ed6e913f925c992a210`, with no blocking or non-blocking findings. It confirmed the sequence-1 uncovered-dependency blocker was corrected. Residual risk remains limited to semantic forwarding/alias wrappers that require review and to persistence, HTTP, authorization, transaction, and delivery boundaries assigned to downstream TICKETs.

Fresh landing pre-publication verification passed on PHP 8.5.10: `./bin/architecture` reported 0 violations, 0 skipped, 0 uncovered, 33 allowed, 0 warnings, and 0 errors over three production PHP files; the focused composition test passed with 1 test / 11 assertions; strict Composer validation passed; `./bin/planning-check` reported 95 records / 80 active; range `git diff --check` passed; and `./bin/build` passed with the locked 106-package development graph, no known vulnerability advisories, 5 PHPUnit tests / 48 assertions, a fresh 67-package production install, and all intended production-contract rejection probes. Informational Composer funding notices covered 76 development and 45 production packages; no PHPUnit warning, skip, deprecation, security advisory, architecture warning, skipped violation, or required-check failure was observed.

Landing commit `5977ee411978f8ea998f794e4d6702195657421b` recorded the accepted review and pre-publication verification. The branch was pushed without force and [PR #14](https://github.com/johnnickell/fight-agent-os/pull/14) was opened against `develop` with feature head `feature/application-ownership-boundaries`; its initial remote head matched that landing commit.

Fresh publication-metadata verification passed after recording the canonical PR URL and refreshing generated views: `./bin/architecture` again reported 0 violations, 0 skipped, 0 uncovered, 33 allowed, 0 warnings, and 0 errors; the focused composition test again passed with 1 test / 11 assertions; strict Composer validation passed; `./bin/planning-check` again reported 95 records / 80 active; worktree and range `git diff --check` passed; and `./bin/build` again passed with 5 PHPUnit tests / 48 assertions, no known vulnerability advisories, a fresh 67-package production install, and all expected production-contract probes. Funding notices remained the only warning. The metadata commit is created after this authoritative record; its final non-force push and remote-head confirmation remain pending at this record stage.

After final remote-head verification, cleanup will remove only the inspected TASK-owned ignored notes, generated `vendor/` and `var/cache/`, any exact Compose resources proven to the `application-ownership-boundaries` project, and the clean registered isolated worktree. The unrelated `planning/complete-agent-os-vision` worktree and canonical base worktree remain preserved. Human approval and merge, branch deletion, archive, release, deployment, and certification remain unclaimed.
