---
id: TASK-00004
ticket: TICKET-00004
kind: feature
title: Establish and prove controlled landing
status: in-progress
order: 4
blocked_by: TASK-00003
pr:
---

# Establish and prove controlled landing

## Outcome

The repository has a concise project-local `land` skill and shared landing/ownership standards that close reviewed implementation deterministically, publish only authorized work, clean only demonstrably owned resources, and return merge control to a human. Bounded demonstrations prove both successful handoff and safe refusal.

## Scope

- In scope: Add `.pi/skills/land/SKILL.md`; add or refine concise shared landing and resource-ownership standards; verify independent review and blocking-finding disposition; finalize TASK evidence/status and generated planning views; run required final checks; commit, push, and open/update a PR only when authorized; clean only TASK-owned resources; hand off for human review/merge; demonstrate a successful path and at least one refusal path.
- Out of scope: Repeating implementation review, repairing unresolved defects, planning decomposition, autonomous merge, archive, release/deployment/certification, broad environment cleanup, deleting shared or ambiguous resources, and publication without explicit authority.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Qualify reviewed work | N/A — repository workflow coordinates planning, Git, review evidence, and verification | Read TASK/TICKET, independent review, finding dispositions, acceptance evidence, diff/status, branch, remote, and PR state | N/A — no application domain event | Landing either establishes satisfied prerequisites or stops with explicit unmet conditions |
| Finalize implementation records | N/A — authorized planning/documentation operations | Read accepted evidence and generated-view state | N/A — no workflow domain event | Completion notes become truthful, TASK status becomes `done`, and generated views are refreshed and current |
| Publish human handoff | N/A — authorized Git and hosting operations | Inspect final checks, commits, remote branch, publication authority, and existing PR | N/A — no application domain event | Landing updates are committed/pushed and a PR is opened/updated only when authorized; human retains merge control |
| Clean owned resources | N/A — bounded filesystem, process, database, worktree, and container operations | Inspect ownership markers, paths, captured PIDs, worktrees, prototype scope, databases, containers, and volumes | N/A — no workflow domain event | Only demonstrably TASK-owned resources are stopped or removed; ambiguous/shared resources remain untouched |
| Prove landing behavior | N/A — bounded demonstration | Read controlled success and refusal fixtures/evidence | N/A — no application domain event | One path reaches human handoff and at least one unsafe path stops without merge or destructive cleanup |

## Validation and permissions

Stop for missing independent review, unresolved blocking findings, stale or incomplete acceptance evidence, failing required checks, unexpected unrelated changes, inconsistent branch/diff state, missing commit or publication authority, unavailable remote/hosting prerequisites, or ambiguous cleanup ownership. Trust the review verdict only after verifying its presence, independence disclosure, target/baseline, and finding dispositions; do not repeat implementation review.

Run `./bin/planning-check --write`, `./bin/planning-check`, required focused checks, `git diff --check`, and final `./bin/build`. Preserve every warning, skipped check, and remaining uncertainty. `done` records implementation acceptance and required verification, not PR publication, merge, release, deployment, certification, or archive.

Cleanup is limited to ownership-proven TASK scratch, worktrees, prototype folders, captured processes, test databases, and task-specific containers/volumes. Shared services, source files, durable evidence, and ambiguous resources require explicit human direction.

No application permission model applies. Landing may edit authorized planning/documentation and may commit, push, and create/update a PR only where TASK/user authority explicitly permits. It may not re-scope requirements, fix implementation, merge, deploy, release, certify, archive, or delete ambiguous resources.

## Acceptance criteria

- [x] `.pi/skills/land/SKILL.md` exists, remains concise, and loads project-owned landing/ownership standards instead of duplicating repository instructions.
- [x] Qualification verifies independent review and blocking-finding disposition without silently performing another implementation review.
- [x] Landing refuses missing/stale evidence, unresolved blockers, failed checks, unexpected changes, absent publication authority, and ambiguous cleanup ownership.
- [x] Finalization records actual acceptance evidence, sets TASK status `done`, refreshes generated views, and runs focused checks plus final `./bin/build`.
- [x] The workflow distinguishes TASK completion, commit, push, PR handoff, human merge, archive, release, deployment, and certification.
- [x] Publication occurs only when explicitly authorized and ends with clear retained human actions rather than a merge claim.
- [x] Cleanup removes or stops only demonstrably TASK-owned resources and preserves shared, durable, and ambiguous resources.
- [x] A bounded successful demonstration records planning changes, checks, commits, authorized or simulated push/PR effects, cleanup decisions, warnings, and human handoff.
- [x] A bounded refusal demonstration proves at least one unsafe landing or cleanup condition stops without merge or destructive effects.
- [ ] The `land` implementation receives independent review through the completed `review` capability.

## Verification and evidence

Inspect the skill and loaded standards directly for completeness, concision, links, authority boundaries, and consistency with `work`, `review`, planning conventions, and repository instructions. Use TASK-owned `.runs/` fixtures or another explicitly safe bounded target to exercise successful qualification/finalization/handoff and refusal for unresolved review findings or ambiguous cleanup ownership. Avoid real publication unless separately authorized; clearly label simulated effects. Capture before/after status, planning changes, check output/test counts, commit effects, publication decision, ownership proof, cleanup effects, warnings, and retained human actions.

Run `./bin/planning-check --write`, `./bin/planning-check`, focused checks for durable tooling/documentation, `git diff --check`, and `./bin/build`. Verify workflow documentation directly without adding product tests solely for Markdown. Arrange an independent `review` run for this implementation and record all finding dispositions before acceptance.

## Coordination

TASK-00003 must first deliver a usable independent review capability and complete its dependency on TASK-00002. Keep landing SUBTASK notes and disposable demonstration material under a TASK-owned `.runs/` directory. This TASK owns one independently reviewable outcome; do not split qualification, planning finalization, publication handoff, ownership cleanup, and refusal proof into separately publishable incomplete changes.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. Preserve uncommitted planning work and all unrelated local resources.

## Completion notes

Implemented the concise project-local `land` skill and shared landing/resource-ownership standards, with skill and engineering index updates. Authored files are `.pi/skills/land/SKILL.md`, `.pi/skills/_index.md`, `docs/engineering/LANDING.md`, `docs/engineering/_index.md`, and this TASK; generated views are `planning/tasks/BOARD.md`, `planning/tasks/README.md`, and `planning/tickets/00004-TICKET.md`. No production application file or product test changed.

The bounded successful demonstration used a disposable Git repository under `.runs/notes/TASK-00004/demonstration/success/`. A fresh no-session Pi run loaded the new skill, qualified an independent `accept` report for exact fixture commit `2d9e02b`, finalized the fixture TASK and Board, passed 2 focused checks plus its canonical 2-check build, and created landing commit `a02457a`. It recorded push and PR effects as simulation-only, performed no network operation or merge, removed only the inspected ownership-marked `owned-temp` resource, preserved `ambiguous-cache`, and returned real publication, approval, and merge to a human.

The refusal demonstration used exact fixture commit `0918033` with an independent `revise` report, missing canonical evidence, no side-effect authority, and an unmarked resource. A separate fresh no-session Pi run refused TASK finalization, commit, publication, cleanup, and merge. Before/after SHA-256 manifests matched, the fixture branch remained clean and `in-progress`, and `ambiguous-volume` remained present. Detailed fixtures, reports, manifests, and logs remain in ignored TASK-owned scratch.

Fresh focused validation passed for the skill frontmatter, 2 durable files, 2 relative documentation links, indexes, and 6 authority/boundary assertions. `./bin/planning-check` passed with 92 records / 87 active records, and `git diff --check` passed. The preliminary canonical `./bin/build` passed with PHPUnit 26 tests / 152 assertions, current planning validation, and the production Composer dependency contract. Composer retained the inherited warning that `johnnickell/fight-common` points to a commit reference; package-funding messages were informational. The first attempted demonstration invocation was unavailable because the selected Spark model does not support the configured ChatGPT account; it produced no fixture effect and the successful run used the configured Sol model. No required repository check was skipped.

Implementation remains `in-progress` pending the required independent `review` run and finding dispositions. The implementation commit is prepared for that handoff; no push, PR publication, landing of this TASK, merge, archive, release, deployment, or certification is authorized or claimed. TASK-owned scratch is retained because it contains demonstration and verification evidence; no ambiguous or shared real resource was cleaned. Completion means implementation acceptance, not merge, release, deployment, certification, or archive.
