---
id: TASK-00070
ticket:
kind: chore
title: Remove build and test self-verification bloat
status: done
order: 12
blocked_by:
pr:
---

# Remove build and test self-verification bloat

## Outcome

Agent OS has a fast, comprehensible build gate that checks the current application without testing its own tools,
wrappers, configuration wiring, or deliberately seeded failures, backed by durable testing guidance and corrected
future quality requirements.

## Scope

- In scope: Remove the configuration-only composition test; replace the image-building gate with a thin running-service wrapper and PHP phase orchestrator; retain only Composer manifest validation, planning validation, Deptrac, and behavioral PHPUnit checks; remove redundant custom contract checkers and recurring negative fixtures; update engineering guidance and affected future TASKs.
- Out of scope: Product behavior, new architecture rules, dependency installation/auditing, coverage tooling, frontend checks, PostgreSQL implementation, and implementing future quality TASKs.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Run the current build gate | N/A — repository tooling | Validate the Composer manifest, planning, dependency direction, and owned behavior | N/A — no domain event | Named phases run once in the existing Compose service and fail clearly |
| Verify application behavior | N/A — PHPUnit tooling | Exercise externally observable owned behavior | N/A — no domain event | Product failures remain covered without configuration or tool self-tests |

## Validation and permissions

`./bin/build` must require the running `web` service and delegate to `scripts/build.php`. The PHP orchestrator may
use the locked Symfony Process dependency and must propagate phase output and failures. It must not install or audit
dependencies, build disposable production trees, generate invalid fixtures, or test whether its checks detect seeded
violations. Deptrac remains the dependency-direction authority.

No runtime permission applies. Repository tooling, tests, instructions, engineering documentation, and planning
requirements are authorized; product source and behavior are not.

## Acceptance criteria

- [x] The configuration-only composition PHPUnit test is removed while externally observable HTTP behavior remains covered.
- [x] `./bin/build` delegates through the running Compose `web` service to a PHP orchestrator with named phases and exact failure propagation.
- [x] The gate runs Composer manifest validation, planning validation, Deptrac, and PHPUnit once each without installs, audits, production-copy checks, or seeded-invalid self-tests.
- [x] Redundant custom architecture and production contract scripts are removed, and focused architecture execution uses Deptrac directly.
- [x] `AGENTS.md` and engineering standards prohibit product-suite tests of architecture, configuration, wrappers, build/planning/static-analysis mechanisms, generated files, and test infrastructure.
- [x] Affected future TASKs no longer require tests or recurring seeded evidence for wrappers, configuration, migrations, or quality tools while preserving meaningful behavior coverage goals.
- [x] Focused checks, planning validation, `git diff --check`, and the simplified `./bin/build` pass with fresh evidence.

## Verification and evidence

Inspect the final PHPUnit inventory and build phase list directly. Run PHP syntax checks for the orchestrator,
`./bin/planning-check --write`, `./bin/planning-check`, focused PHPUnit and Deptrac checks, `git diff --check`, and
the simplified `./bin/build`. Record phase/test/assertion counts, warnings, and environment limitations without
adding tests for the build implementation.

## Coordination

This standalone chore pulls the already-planned thin PHP build shape forward from TASK-00033 without claiming that
future backend coverage, frontend, PostgreSQL, style, static-analysis, or production-artifact phases are complete.
TASK-00030 through TASK-00033 may extend the phase list only with checks that validate application source or
behavior, never with tests of the checks themselves.

The human selected the main checkout and a `feature/*` branch. This TASK owns one independently reviewable cleanup.

## Completion notes

Implemented the lean gate on `feature/remove-build-test-bloat`. `bin/build` now checks for the already-running
Compose `web` service and delegates to `scripts/build.php`; the PHP orchestrator runs exactly four named phases:
strict Composer manifest/lock validation, read-only planning validation, Deptrac, and PHPUnit. It performs no
install, update, audit, disposable production copy, generated invalid fixture, or quality-tool self-test. The
focused architecture wrapper now invokes Deptrac directly in the running service.

Removed `AccessControlQueryCompositionTest`, leaving four functional HTTP behavior tests; removed the custom
architecture and production contract scanners and their recurring negative probes. Added the anti-self-testing
rule to `AGENTS.md` and `docs/engineering/STANDARDS.md`, retained 100% coverage as a goal for meaningful owned
Domain/Application and practical Adapter behavior, and corrected the ownership ADR, quality TICKET, and affected
TASK-00012, TASK-00018, TASK-00021, TASK-00026, TASK-00030 through TASK-00033, TASK-00063, and TASK-00066 so future
work does not manufacture tests for infrastructure, wiring, migrations, wrappers, or quality tools.

Fresh verification on PHP 8.5.11 passed: `scripts/build.php`, `bin/build`, and `bin/architecture` syntax checks;
planning validation with 96 records / 79 active after this TASK closed; Deptrac with 0 violations, 0 skipped,
0 uncovered, 33 allowed, 0 warnings, and 0 errors; PHPUnit 10.5.64 with 4 tests / 37 assertions; `git diff --check`;
and the complete simplified `./bin/build` with all four phases. The first build correctly failed because the mounted
`vendor/` contained only production dependencies and no Deptrac; an explicit one-time `composer install` outside
the gate restored 13 locked development packages, after which the gate passed. That setup install emitted Composer's
informational funding notice for 76 packages and a container Git dubious-ownership warning; setting the root version
for manifest validation keeps the gate itself warning-free. `./bin/up` rebuilt and started the owned Compose service.

Implementation is complete. Independent review, publication, PR, merge, release, and deployment remain pending.
The unrelated `planning/complete-agent-os-vision` worktree was untouched. The implementation commit is identified in the handoff.
