---
id: TASK-00013
ticket: TICKET-00009
kind: feature
title: Persist identities and refresh sessions atomically
status: done
order: 13
blocked_by: TASK-00012
pr: https://github.com/johnnickell/fight-agent-os/pull/20
---

# Persist identities and refresh sessions atomically

## Outcome

Production PostgreSQL adapters satisfy the complete stable `UserRepository` and `RefreshSessionRepository` contracts, including canonical identity constraints, role-reference fences, session credential lookup, and the indivisible authentication-authority/session operation.

## Scope

- In scope: User, role-assignment, email-reservation state required by the repository contract, and refresh-session migrations/mappings; complete User/RefreshSession adapters; production composition; exact entity reconstruction; pagination/ordering; authoritative constraints; expected-state updates; transaction-duration fences; safe conflict translation; contract, rollback, and controlled concurrency tests.
- Out of scope: Authentication HTTP behavior, credential generation/JWT/cookies, login throttle, product session policy, activation/reset grant adapters, external effects, UI, and deferred email-change journey delivery even though repository state must faithfully support the package contract.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Add and resolve identity | Existing package commands use `UserRepository` | Resolve by ID/email, list users, inspect role references | Package events remain package-owned | Exact user and assignments persist without canonical-email or reservation conflict |
| Replace user state | Package authentication, assignment, invitation, and lifecycle commands | Compare complete expected predecessor and required references | Events only continue from committed package outcomes | One valid transition advances exact revisions; stale/invalid updates write nothing |
| Create login authority atomically | `AuthenticationService` package behavior | Resolve expected user authority and new session ownership/version | Package login/session outcomes | Authority replacement and refresh-session insert both commit or both roll back |
| Resolve/rotate sessions | Package refresh/revoke commands | Resolve by ID/current or used credential, list active sessions | Package session lifecycle events | Expected session replacement wins once; used digests remain detectable as contracted |

## Validation and permissions

Database constraints must enforce stable identifiers, canonical-email uniqueness including live reservations, role and user references, session ownership, credential/digest uniqueness, and valid storage shapes. Persist only one-way refresh credential material supplied by package entities. Reconstruct exact package value objects/entities and honor pagination and promised ordering.

Implement every `UserRepository` method, including authentication authority, coupled authority/session insertion, role assignment/reference validation, reservation/confirmation, pending email correction, lifecycle replacement, and add/list/lookups. Implement every `RefreshSessionRepository` method, including active filtering, complete active scan, current/used credential lookup, and exact-revision replacement.

Use the same transaction-duration user-authority and role-reference fences across relevant methods. Controlled races must cover stale user/session writes, role removal/assignment, canonical/reserved email claims, reset versus login/session insertion, and credential rotation/reuse. Safe failures expose no SQL or existence information beyond contract.

No end-user permission is introduced. Persistence adapters and composition are authorized; package/feature policy and transport are not.

## Acceptance criteria

- [x] Versioned migrations add snake_case user, assignment/reservation, refresh-session, and used-credential structures with authoritative constraints.
- [x] Production adapters implement all stable `UserRepository` and `RefreshSessionRepository` methods directly.
- [x] Exact package entities round-trip across lifecycle, role, authority/version/revision, reservation, and session states.
- [x] Canonical email and live reservation conflicts are atomically rejected for add, correction, reservation, and confirmation.
- [x] Role assignments cannot commit against removed/non-authoritative roles, including controlled races.
- [x] Authentication-authority replacement obeys exact expected-state/revision rules and its transaction fence.
- [x] `replaceAuthenticationAuthorityAndAddRefreshSession()` makes authority/session insertion indivisible and rejects mismatched ownership/version.
- [x] Session queries honor ownership, active-at time, pagination/ordering, current credential, and historical used-digest semantics.
- [x] Controlled reset/login and refresh replacement races allow only contract-valid outcomes; rollback leaves no partial state.
- [x] Focused migration/repository/transaction/concurrency checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Run contract tests from a clean migrated PostgreSQL database for every interface method, absence case, invalid transition, uniqueness/reference failure, pagination boundary, and entity state. Use deterministic IDs, clocks, credentials, and fixtures. Coordinate real competing PostgreSQL connections/transactions with bounded synchronization rather than timing sleeps. Prove coupled rollback by injecting failures at both sides of authority/session persistence.

Run focused wrappers/tests and architecture checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record migrations/constraints, package and PostgreSQL versions, method coverage, test/assertion counts, controlled race outcomes, warnings, and any contract ambiguity requiring upstream clarification.

## Coordination

TASK-00012 supplies authority references, the guarded database lifecycle, shared connection, mapping conventions, and fences. TASK-00014 and TASK-00015 depend on persisted users; TASK-00016 may proceed from TASK-00012 independently. Do not add endpoints or duplicate package behavior while proving repository contracts.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable authoritative identity/session persistence outcome.

## Completion notes

Implemented on `feature/task-00013-identity-session-persistence`. Migration originally named `Version20261002000000` (later corrected to `Version20260925231111`) adds `users`,
`user_email_claims`, `user_role_assignments`, `refresh_sessions`, and `refresh_session_used_credentials` with
snake_case identifiers and named constraints (`pk_users`, `ck_users_state`, `ck_users_password_shape`,
`ck_users_revisions`, `ck_users_timestamps`, `uq_user_email_claims_email`, `uq_refresh_sessions_credential_digest`,
`uq_refresh_session_used_credentials_digest`, plus foreign keys and lifetime checks). One normalized
`user_email_claims` table enforces a single unique canonical value across canonical addresses and live
reservations.

Exact reconstruction uses adapter-private subclasses (`PersistedUser`, `PersistedRefreshSession`) that forward
authoritative stored state through the package's protected constructor. The locked `v0.3.0` package exposes no
public full-state factory and no getter for reserved session rotation timestamps or historical digests; hand
reflection and serialization remain prohibited. The package itself reconstitutes entities through this subclass
constructor pattern, and no lifecycle policy is duplicated: `refresh_session_used_credentials` is maintained
incrementally on rotation exactly as `RefreshSession::rotate()` appends the superseded digest, and `rotated_at` is
written on rotation and left unchanged on revocation.

`PostgresUserRepository` implements all thirteen `UserRepository` methods with complete expected-state conditional
writes, a per-user `AuthenticationAuthorityFences` advisory lock for authority replacement and confirmation, the
shared `AuthorizationReferenceFences` for role assignment/reference validation, and an indivisible
`replaceAuthenticationAuthorityAndAddRefreshSession()` using a transaction-scoped savepoint. Canonical and
reservation claim conflicts throw `DuplicateEmailException` atomically; missing role references map to
`PersistenceConflict`. `PostgresRefreshSessionRepository` implements all six methods with `UPDATE ... WHERE
revision = ?` plus complete expected-state checks, current/used credential lookup, active-at filtering on both
expiry fields and revocation, and deterministic `created_at, id` ordering. `PostgresAtomicOperation` supplies
transaction-scoped savepoints so known conflicts roll back without partial state. Both repositories fail closed
outside an enclosing transaction. Composition binds `UserRepository` and `RefreshSessionRepository` on the shared
connection in `config/common/persistence.php`.

Fresh focused evidence: guarded reset reapplied both migrations from zero (14 SQL queries); migration status
reported 2 executed, 0 unavailable, 0 new; the guarded PostgreSQL 17.11 suite passed 30 tests / 492 assertions on
PHP 8.5.10. Coverage includes pending/active round trips, canonical and live-reservation add conflicts, complete
state authentication-authority replacement with stale rejection, indivisible authority/session insertion with
mismatched-ownership rejection, role assignment with removed/non-authoritative roles and the shared reference
fence race, email reservation/confirmation/correction with reservation conflicts, lifecycle transitions, injected
rollback, a two-connection authentication-authority fence race, session pagination/active/expiry queries, current
and historical credential lookup, rotation/revocation with stale and reused-digest rejection, and a two-connection
rotation race. No warning, skip, deprecation, or failure was reported.

Fresh canonical evidence: `git diff --check` passed and `./bin/build` passed Composer strict validation, planning
validation (154 records / 128 active), Deptrac (0 violations, skipped violations, uncovered dependencies, warnings,
or errors; 311 allowed), and PHPUnit 34 tests / 529 assertions. The only pre-existing test change is adding
`CASCADE` to the authority suite's shared `TRUNCATE`, required because `user_role_assignments` now references
`roles`.

Out of scope and unchanged: authentication HTTP behavior, credential/JWT/cookie generation, login throttle,
product session policy, activation/reset/email-change grant adapters, external delivery, audit evidence, UI, and
request-time schema creation. Outcome and durability remain PostgreSQL-verified; email-change product routing stays
deferred. Merge, release, and deployment remain separate human actions.

## Landing record

Independent review: `.runs/reviews/TASK-00013/review.md` (sequence 1, `review-00001.md`) returned **accept** for
exactly `e5d1e29..2d64f97` on `feature/task-00013-identity-session-persistence` with a clean tree; the reviewer
contributed nothing to the implementation or its evidence. Blocking findings: none. Non-blocking dispositions:

1. Completion-notes method count corrected from eleven to thirteen (the interface and adapter declare thirteen
   `UserRepository` methods; no code change).
2. `rotated_at` omitted from the refresh-session expected-state comparison — accepted now: the exact-revision
   conditional update pins it in every package-reachable flow, so no contract-valid outcome can diverge.
3. `add()` does not translate check-constraint violations — accepted now: `ck_users_password_shape` is unreachable
   through package entity flows, which are the only producers of persisted users.
4. N+1 hydration queries for role assignments and used digests — deferred: no approved performance requirement;
   candidate improvement alongside later scale work.
5. User pagination boundary exercised only with a single record — deferred: refresh-session pagination covers the
   shared `created_at, id` boundary; optional test improvement.

Fresh landing verification: before asserting completion `./bin/database test` — guarded PostgreSQL 17.11
suite, 30 tests / 492 assertions, no failures, skips, warnings, or deprecations; `./bin/planning-check` — 154
records / 128 active; `git diff --check` — clean; `./bin/build` — Composer valid, Deptrac 0 violations / 0
skipped / 0 uncovered / 0 warnings / 0 errors (311 allowed), PHPUnit 34 tests / 529 assertions, build passed.
After this record and the generated views were updated, the checks were rerun: focused suite 30 tests / 492
assertions, `./bin/planning-check` — 154 records / 127 active (TASK-00013 no longer counts as active),
`git diff --check` — clean, `./bin/build` — PHPUnit 34 tests / 529 assertions, build passed; no failures,
skips, warnings, or deprecations in any rerun. Review evidence
limitations (focused-suite claim reproduced fresh by the reviewer; partial migration-status capture corroborated
by a from-zero reset) and residual risks (sequential email-claim races behind the authoritative unique
constraint; the pending-activation→active authority-transition reading shared with the package activation
flow) are recorded in the review report and accepted.

Publication state: pushed without force and published as
[PR #20](https://github.com/johnnickell/fight-agent-os/pull/20) against `develop`; the `pr` field and generated
views record it. Cleanup decisions: the disposable `.runs/task-00013-test.log` focused-check output is removed after
publication; the two `.runs/task-00013-build*.log` files are retained because the review report manifest
records their digests as reviewed evidence; the review report directory, shared Compose services
(`postgres`, `postgres_test`, `web`), and the canonical base worktree are preserved. Retained human actions:
approve and merge the PR, then delete the local and remote feature branch after merge; archive, release,
and deployment remain human decisions.

## Revision after independent review sequence 2

The published PR's later head `8cb7cce` received **revise** in the canonical
`.runs/reviews/TASK-00013/review.md` (`review-00002.md`). That report supersedes the prior sequence-1 acceptance
for this head. This local revision is not yet independently reviewed or republished.

- Blocking finding 1 (cross-table current/used digest collision): the new versioned
  `Version20260926000253` migration backfills one authoritative `refresh_session_credential_claims` digest key
  across current and historical states, then claims every newly inserted or rotated current credential under a
  PostgreSQL trigger and constrains used digests to their owning claim; a second trigger prevents recording the
  current digest as already used. A conflicting backfill fails rather than silently selecting an owner. Existing
  adapter savepoints translate duplicate-claim failures to `false` and roll back the attempted authority/session
  or rotation together. New PostgreSQL tests rejected spent-to-current coupled insert, another session's rotation,
  same-session reuse, and a competing digest claim; negative current/used lookups and unchanged losing state were
  asserted. Before repair two new tests failed as expected (32 tests / 511 assertions, 2 failures).
- Blocking finding 2 (coupled rollback and reset/login evidence): new PostgreSQL tests inject duplicate-session
  insert failure *after* the authority update, inject a caller failure *after* both coupled writes, and race reset
  against coupled login in both orders on real guarded connections using bounded lock timeouts. They assert final
  authority and session state and reject stale retries. No new product tests were added for migration filenames,
  UUID conventions, or tooling.
- At the maintainer's explicit request, corrected the two earlier future-dated migration identifiers using their
  verifiable introduction-commit UTC timestamps: `Version20261001000000` → `Version20260925220023` (TASK-00012),
  `Version20261002000000` → `Version20260925231111` (TASK-00013). The new migration was created with Doctrine's
  `generate` command at `Version20260926000253`; its timestamp was not invented. The historical TASK-00012/00013
  notes now identify the original and corrected filenames. The requested migration/UUID conventions are recorded
  in `AGENTS.md`. No UUID literals were introduced.

Fresh verification on the revised local tree: `./bin/database test-reset` applied 3 migrations / 21 SQL queries
from zero on the guarded PostgreSQL 17.11 test database (Doctrine emitted migration notices);
`./bin/database test-status` reported 3 executed / 0 unavailable / 0 new; `./bin/database test` passed 35 tests / 627
assertions on PHP 8.5.10; `./bin/planning-check --write` and `./bin/planning-check` passed 154 records / 127
active; `git diff --check` passed; `./bin/build` passed Composer validation, planning validation, Deptrac
0 violations / 0 skipped / 0 uncovered / 0 warnings / 0 errors (312 allowed), and PHPUnit 39 tests / 664
assertions. No PHPUnit skips, warnings, failures, or deprecations appeared in these successful runs. Logs are
under ignored `.runs/notes/TASK-00013/`.

Migration-identity risk: before the rename the guarded test DB reported 3 executed; after the rename it reported
3 executed unavailable and 3 new. A guarded reset resolved that *test* database; development status had 0
executed. Other databases that already recorded the prior identifiers were not inspected or altered and **must
not** simply rerun renamed create-table migrations: their schema and Doctrine version metadata need an explicit
qualified reconciliation before migration. No development/production data was reset. The backfill against a
prepopulated external database and such a reconciliation remain unverified; new code and this revision need a
fresh independent review. PR #20 reflects the older pushed head until separately authorized publication.
