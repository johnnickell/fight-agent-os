---
id: TASK-00041
ticket: TICKET-00016
kind: feature
title: Expose authorized invitation issuance and safe status
status: ready-for-agent
order: 41
blocked_by: TASK-00040
pr:
---

# Expose authorized invitation issuance and safe status

## Outcome

Versioned JSend API operations let an authoritative authenticated actor issue ordinary or explicitly confirmed elevated invitations and inspect safe delivery status, with exact permission checks, bounded rate limiting, package-owned behavior, typed Views, correlated failures, and no grant or provider secret exposure.

## Scope

- In scope: Invitation request/status routes and single-use-case Actions; validated DTOs; authenticated actor resolution; ordinary/elevated application orchestration reusing TASK-00037/00038/package behavior; exact `INVITE_USERS`, `ASSIGN_SUPER_ADMIN`, and `MANAGE_USER_INVITATIONS` checks; explicit elevation confirmation; production rate limiter for invitation operations; JSend safe Views/error mapping; OpenAPI; PostgreSQL/permission/rate/HTTP/delivery/redaction tests.
- Out of scope: Admin React pages/forms, user listing, retry/resend/correction, public registration, custom roles, provider configuration, activation UI, bulk invitation, and client-side authorization as enforcement.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Invite ordinary user | Application orchestrator using `InvitePendingUser` and managed role assignment | Resolve current principal, canonical email, existing state, and managed `ROLE_USER` | `UserInvited`, `RoleAssignedToUser` as applicable | Pending ordinary user, audit, grant, and recoverable delivery state commit |
| Invite Super Admin | Explicit elevated orchestrator using the same package behavior | Resolve exact current permissions, confirmation, policy, and target state | Invitation/assignment events plus durable safe audit | Pending managed Super Admin exists only after authorized confirmed request |
| Inspect delivery status | N/A — read-only API | `FindInvitationDeliveryStatus` | N/A — no domain event | Safe user/status/expiry View is returned without credential/provider detail |
| Reject/throttle request | N/A until valid/authorized | Read authoritative principal and atomic limiter state | Safe failure only | No invitation/status disclosure or partial side effect occurs |

## Validation and permissions

Validate JSON shape/types/bounds and canonical email before dispatch. Accept only the two approved invitation modes; ordinary maps to stable managed `ROLE_USER`, elevated maps to stable managed `ROLE_SUPER_ADMIN`. Do not accept arbitrary role IDs/names/permissions. Elevated invitation requires `INVITE_USERS` and `ASSIGN_SUPER_ADMIN` plus explicit unambiguous confirmation; ordinary requires `INVITE_USERS`. Status requires `MANAGE_USER_INVITATIONS`.

Resolve the request-scoped authoritative principal server-side and compare exact permission names through the shared evaluator/capability. Never grant based on `ROLE_SUPER_ADMIN`, client route visibility, JWT/HTML claims, or submitted actor ID. Unknown/stale/mismatched authority fails closed. Actor ID comes from authenticated context, not JSON.

Use TASK-00037's coherent invitation orchestration and package commands/handlers directly; do not recreate identity/grant/assignment rules. Responses use explicit Views and may include only approved stable user/delivery identifiers, canonical email when justified for authorized actor, lifecycle/delivery status, and expiry. Distinguish package delivery handoff state from durable provider-attempt outcome honestly if both are exposed; no field implies receipt not evidenced.

Apply a real atomic bounded rate limiter keyed by safe non-secret actor/action and, where justified, one-way bounded target dimension. Validate windows/limits, avoid raw email/credential keys in diagnostics, and return safe retry metadata. Rate limiting supplements permission checks and database uniqueness.

Known validation/authorization/conflict/throttle/not-found/delivery failures map through centralized JSend handling and correlation. Do not return raw grant, digest, ciphertext, activation link, provider payload/message/key, SQL, stack trace, or arbitrary package exception text. Same-origin Bearer API behavior and CORS deny-by-default remain intact.

## Acceptance criteria

- [ ] Versioned request/status routes use single-use-case Actions, validated DTOs, JSend, explicit Views, and centralized safe error mapping.
- [ ] Request actor is request-scoped authoritative identity and cannot be supplied/overridden by JSON.
- [ ] Ordinary invitation requires exact `INVITE_USERS` and assigns only stable managed `ROLE_USER`.
- [ ] Elevated invitation requires exact `INVITE_USERS` plus `ASSIGN_SUPER_ADMIN`, explicit confirmation, and assigns only managed `ROLE_SUPER_ADMIN`.
- [ ] No arbitrary role/permission input or role-name authorization bypass exists.
- [ ] Invitation uses package behavior/TASK-00037 orchestration and commits pending identity, assignment, audit, grant, and recoverable delivery coherently.
- [ ] Status requires exact `MANAGE_USER_INVITATIONS` and maps package/durable state into an honest credential-free View.
- [ ] Production rate limiting is atomic, bounded, secret-safe, configurable, and returns controlled retry behavior without replacing authorization.
- [ ] Invalid JSON/input, duplicate/canonical conflict, missing/drifted policy, stale actor/role, forbidden, throttled, missing target, and delivery failure map safely.
- [ ] Responses/OpenAPI/logs/correlation/audit/snapshots contain no grant, password, token, ciphertext, credential link, provider secret/message, SQL, or stack trace.
- [ ] OpenAPI documents only implemented schemas, permissions, confirmation, rate limits, JSend outcomes, and Bearer security.
- [ ] Focused permission/rate/PostgreSQL/HTTP/OpenAPI/delivery/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Exercise ordinary/elevated/status operations with every permission combination, stale/missing authority, submitted actor spoofing, role-like inputs, absent/false/ambiguous confirmation, malformed JSON, Unicode/case-equivalent emails, duplicates, policy drift, and concurrent invitations. Inspect exact persisted role, audit, grant/delivery/intent and response Views. Test delivery/provider pending/failure/success distinctions.

Use deterministic clock and separate PostgreSQL connections to test limiter boundaries, concurrency, retry metadata, key redaction, and actor/target isolation without sleeps. Seed secret/error markers into credentials, ciphertext, provider failures, SQL exceptions, URLs, and headers; scan responses, OpenAPI examples, logs, audit, correlation diagnostics, and snapshots.

Run focused HTTP/application/PostgreSQL/permission/rate/delivery tests, architecture/style/static/coverage/OpenAPI checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record routes/schemas, permission matrix, limiter policy, tests/assertions/races, redaction, versions, durations, warnings, and deferred admin UI.

## Coordination

TASK-00040 supplies concrete recoverable invitation mail; TASK-00038 supplies managed/bootstrap/elevated authorization foundations and TASK-00037 ordinary orchestration. TASK-00042 adds recovery operations. TICKET-00021 later builds responsive Super Admin client routes and user listing over these lifecycle APIs; it must not duplicate them.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable invitation issuance/status API outcome.

## Completion notes

Not started. Record routes/DTOs/Views/OpenAPI, permission/confirmation/rate policy, ordinary/elevated/status and persistence/delivery evidence, redaction, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and admin UI deferrals here.
