---
id: TASK-00052
ticket: TICKET-00018
kind: feature
title: Deliver the honest permission-aware dashboard
status: ready-for-agent
order: 52
blocked_by: TASK-00051
pr:
---

# Deliver the honest permission-aware dashboard

## Outcome

The first authenticated Dashboard uses the accepted production design and sole current-principal authority to present an honest application destination, current-user/account/session navigation, and exact-permission administration shortcuts while explicitly stating that planning, observability, and agent-session capabilities are not installed and rendering no invented product data.

## Scope

- In scope: Protected Dashboard route/page composition; exact `VIEW_DASHBOARD` route requirement; safe current-user and installed-capability copy; account/security/session links; independently permission-gated Users/Invite/Invitation Recovery shortcuts; truthful empty/availability explanation; loading/stale/forbidden/system transitions inherited from frame; responsive/theme/reduced-motion production components and stories; route/component/permission/accessibility/redaction tests.
- Out of scope: Dashboard metrics/charts/activity, planning/observability/agent data APIs, workspace switching, password/session/user/invitation operation implementation, role-name shortcuts, new principal cache, server data endpoint with fabricated content, broad personalization, and client-side security claims.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Open Dashboard | N/A — navigation | Read current principal through sole cache and exact `VIEW_DASHBOARD` evaluator | Client navigation signal | Authorized user sees honest shell destination; denied authenticated user sees `403` |
| Navigate account security | N/A — navigation | Read authenticated route registration | Client navigation signal | User reaches truthful Account/Security or Active Sessions destination |
| Show administration shortcut | N/A — presentation/navigation | Evaluate exact permission required by each registered destination | Client navigation signal | Only individually allowed Users/Invite/Recovery shortcut is visible/enabled |
| Explain unavailable capabilities | N/A — static accepted product copy | Read versioned installed route/capability registry if needed | N/A — no domain event | Dashboard states what is and is not installed without synthetic records or counts |

## Validation and permissions

Require exact `VIEW_DASHBOARD` in complete route metadata. An authenticated principal without it receives TASK-00051's `403`; unknown/loading/stale state never renders Dashboard. The static page exposes no protected server data, so do not invent a no-op dashboard API merely to claim server authorization. Client checks are presentation-only, and every linked operation's API remains server-authorized.

Use only TASK-00049's safe principal projection for current-user presentation. Do not parse JWT, query an alternate authority store, infer from role names, or persist/broadcast roles and permissions. On principal invalidation, remove Dashboard/shortcuts immediately and return to frame state until authoritative refetch resolves.

Gate shortcuts independently: Users requires `LIST_USERS`; Invite User requires `INVITE_USERS`; Pending Invitation Status/Recovery requires `MANAGE_USER_INVITATIONS`; elevated assignment is not advertised unless later interaction has exact `ASSIGN_SUPER_ADMIN`. `ROLE_SUPER_ADMIN` itself grants nothing. Account/Security and owned-session navigation require authenticated ownership and registered route destinations, not administrative permission.

Every link resolves to TASK-00051's truthful registered destination and later feature route; do not emit dead URLs. Before later operations exist, do not display fake lists/forms/status or enabled controls that imply availability. Copy must clearly say planning, observability, agent sessions, charts, metrics, and workspace switching are not installed rather than showing placeholders that resemble data.

Recreate accepted Dashboard layout with semantic heading/landmarks/cards or links as approved, responsive order/reflow, theme modes, keyboard/focus, target size, contrast, reduced motion, and comprehensible empty/availability text. Permission hiding is not a substitute for server denial and inaccessible shortcuts cannot remain focusable.

## Acceptance criteria

- [ ] Dashboard route requires exact `VIEW_DASHBOARD`; unknown/stale is withheld and authenticated denial renders `403` rather than Login.
- [ ] Current-user display and all presentation authority come only from the sole current-principal cache, never JWT/role inference/persistence.
- [ ] Users, Invite User, and Invitation Recovery shortcuts evaluate `LIST_USERS`, `INVITE_USERS`, and `MANAGE_USER_INVITATIONS` independently.
- [ ] No `ROLE_SUPER_ADMIN` bypass exists; future elevated interaction remains separately gated by `ASSIGN_SUPER_ADMIN`.
- [ ] Account/Security and Active Sessions links are available to authenticated owners and resolve to registered truthful destinations.
- [ ] Administration links resolve to registered destinations and show no fabricated records/forms/status or enabled unavailable operation.
- [ ] Authority invalidation removes protected content/shortcuts immediately; stale responses cannot restore them.
- [ ] Copy honestly distinguishes installed authentication/account shell behavior from unavailable planning, observability, agent-session, metric, and workspace capabilities.
- [ ] No fake charts, counters, recent activity, planning records, agents, sessions, telemetry, or workspace data is rendered or fetched.
- [ ] Narrow/wide/system/light/dark/reduced-motion states meet accepted semantic, keyboard/focus, contrast, target, and reflow requirements.
- [ ] Stories/tests use sanitized deterministic data and do not overstate server authorization or accessibility conformance.
- [ ] Focused route/permission/component/Storybook/accessibility/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Generate the complete managed-permission combination matrix for `VIEW_DASHBOARD`, `LIST_USERS`, `INVITE_USERS`, `MANAGE_USER_INVITATIONS`, and `ASSIGN_SUPER_ADMIN`. Assert route result and each shortcut independently, including no roles, `ROLE_USER`, misleading role names, malformed/stale principal, invalidation during render, and out-of-order refetch. Confirm every rendered link is registered and its pre-feature destination remains truthful.

Inspect DOM/network fixtures/stories for forbidden charts/counts/activity/planning/observability/agent/workspace data and for invented no-op dashboard API calls. Seed token/cookie/session/hash/stack/private-user markers and inspect page, links, logs, snapshots, messages, URLs, and storage. Render accepted narrow/wide/system/light/dark/reduced-motion/long-email/error states and test headings/landmarks, keyboard order, visible/unobscured focus, target size, contrast, zoom/reflow, and link purpose.

Run focused frontend type/lint/Vitest/RTL/Storybook/build and route tests, minimal browser smoke only where rendering platform behavior requires it, permission/catalog/redaction scans, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record permission/route/state matrix, component/story/test counts, absence-of-fabrication evidence, automated/manual accessibility evidence and limits, versions, durations, and warnings.

## Coordination

TASK-00051 supplies frame, route manifest, states, and truthful landing destinations; TASK-00049 supplies principal authority. TICKET-00019/00020/00021 implement account/password/session/administrative operations behind these links and must retain exact server checks. Do not pre-implement those journeys or create a second capability/authority registry.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable honest Dashboard outcome.

## Completion notes

Not started. Record Dashboard copy/layout/link/permission contracts, permission/route/state/accessibility/no-fabrication/redaction evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and later journey handoffs here.
