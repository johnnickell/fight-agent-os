---
id: TASK-00104
ticket: TICKET-00028
kind: feature
title: Migrate Fight Agent OS Planning authority
status: ready-for-human
order: 101
blocked_by: TASK-00103
pr:
---

# Migrate Fight Agent OS Planning authority

## Outcome

An authorized human freezes one clean committed Fight Agent OS Planning source, reviews two equivalent clean PostgreSQL rehearsals, approves their exact complete report, invokes the final transactional cutover and directly verifies database-only Planning without changing, deleting or retiring the source Markdown.

## Scope

- In scope: Human selection of the exact source checkout/commit; explicit Planning-edit pause; clean checkout and current context/authority checks; immutable inventory/mapping; two independently clean PostgreSQL rehearsals; complete report review; exact human approval/confirmation; one production `CutOverPlanningAuthority`; direct post-commit stream/constraint/projection/Roadmap/queue/render/context/consumer observations; source byte/Git preservation; first database-authoritative Planning mutation and recovery-boundary acknowledgment; recording evidence/status through database authority after cutover; discrepancy stop/recovery handoff.
- Out of scope: Implementing or repairing migration code, editing source to make import pass during the operation, warning waiver, best-effort correction, dual write, automatic fallback/reverse migration, Markdown deletion/archive/retirement, creating/pushing a retirement branch/PR, browser Dashboard completion, merging a PR, release/deployment certification and manufacturing another operational TASK.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Freeze exact source | Human commits/chooses source and pauses Planning edits | Inspect checkout cleanliness, context/mode, commit/tree and manifest | N/A — preparation emits no Planning event | One immutable Markdown-authoritative snapshot becomes the sole approved candidate |
| Rehearse twice | `RehearseMarkdownPlanningImport` into two separate clean targets | Compare complete reports, streams, projections, renders and canonical queues | Baseline events exist only in disposable rehearsal targets | Equivalent reports provide review evidence; Repository authority/source remain unchanged |
| Approve and cut over | Human approval then `CutOverPlanningAuthority` with exact confirmation | Revalidate all source/report/target/context/authority facts | Imported baseline events and `PlanningAuthorityChanged` | Complete authoritative baseline and database mode commit atomically or no authority change remains |
| Verify operation | One explicit database-authoritative Planning interaction plus read-only queries/renders | Inspect current mode/context, hierarchy, queue, next-work and source integrity | Owning event for the explicit Planning interaction | First write proves database authority and closes pre-first-write rollback; source remains frozen |
| Stop on discrepancy | N/A — do not approve/cut over/continue | Inspect failed stage and safe recovery guidance | N/A unless separately authorized repair occurs | Evidence is retained honestly and authority remains/reverts only under accepted pre-first-write protocol |

## Validation and permissions

Do not start until TASK-00103 and every blocker is accepted/landed in the operating baseline, the installation and PostgreSQL backup/restore prerequisites are healthy, and the human explicitly selects the real Fight Agent OS Repository/checkout/source commit. Use a clean designated checkout, current registered/Markdown-authoritative context and no concurrent Planning edits. Record source commit/tree/manifest digest and byte/Git baseline before any rehearsal.

Run two rehearsals from the exact same immutable source/mapping/schema versions into separately guarded clean owned PostgreSQL targets. Review the complete human-readable reports, not only exit status or digest. Confirm every aggregate/child/reference, authored/supplemental/unclassified content, lifecycle/archive, hierarchy/dependency, criteria/evidence/delivery, Roadmap/TASK order, projection/render and canonical queue result. Every source/generated item must be accounted for. Any warning, rejection, omission, ambiguity, mismatch or changed source stops the operation; do not normalize, waive or repair it inside this TASK.

The authorized human approves the exact clean report and invokes one final cutover confirmation bound to Repository/CutoverId/source/mapping/report/target/context/mode. Observe the operation to completion without opening another writer. On failure, prove Markdown remains authoritative and no partial production streams/constraints/projections/mode exist. Do not retry against uncertain state until the operation reports the authoritative outcome and a fresh clean attempt is prepared where required.

After success, resolve a new database-authoritative context and compare the migrated Fight Agent OS Roadmap/Board/next frontier with the approved rehearsal and pre-cutover generated Markdown baseline. Query representative Wayfinder, research/prototype, EPIC/TICKET/TASK, standalone, archive, criteria/evidence, dependency and history Views. Render representative artifact/Board/Roadmap/index Markdown and verify authority/revision/template labels.

Perform one deliberate authorized low-risk database Planning mutation that records this operational evidence/status through semantic database authority; acknowledge that it permanently closes v1's pre-first-write return-to-Markdown boundary. Verify terminal/agent `next` uses the canonical query and service loss fails closed. Do not edit this frozen Markdown TASK to record completion: after cutover its database aggregate/projection is authority. Original files remain unchanged/read-only evidence until later separately approved retirement.

Require migration preview, report view, approval, cutover and representative Planning-operation Permissions as appropriate. The migration process/agent cannot impersonate the human. Keep credentials, source contents, absolute paths, database details and sensitive evidence out of reports/logs. Preserve all unrelated resources and clean only ownership-proven rehearsal targets after evidence retention decisions.

## Acceptance criteria

- [ ] All implementation blockers are landed and the human selected one clean committed designated source under registered/Markdown authority with Planning edits paused.
- [ ] Source commit/tree/manifest and byte/Git baseline are recorded without modifying source.
- [ ] Two separate clean PostgreSQL rehearsals from identical inputs produce equivalent normalized streams/projections/reports/renders/canonical queues.
- [ ] Human review accounts for every source/generated record, identity/reference, content, relationship, lifecycle, evidence, order and queue result with no unresolved warning/rejection/mismatch.
- [ ] Exact approval and final cutover confirmation are performed by an authorized human against current source/report/target/context/mode.
- [ ] Final cutover commits complete baseline/constraints/projections/order/mode atomically; direct inspection finds no partial or dual authority.
- [ ] Migrated canonical Roadmap/queue/next frontier matches the approved report and pre-cutover baseline or the operation is stopped for explicit correction.
- [ ] New database context, representative hierarchy/history/renders and database-only terminal/agent consumer behavior are observed directly.
- [ ] One explicit database Planning mutation is accepted and records operational completion/evidence, closing the pre-first-write recovery boundary knowingly.
- [ ] Original Markdown remains byte-for-byte unchanged and available; no retirement branch/PR, deletion, archive, sync, reverse migration or fallback occurs.

## Verification and evidence

Record exact landed application commit, Repository/Checkout identity, source commit/tree/manifest/mapping/report digests, importer/event/projection/template/queue versions, PostgreSQL/application versions and authority/context revisions. Retain both complete rehearsal reports and normalized equivalence evidence in approved durable operational storage, not tracked source or disposable-only scratch.

Before cutover run direct installation/database health and guarded backup readiness checks plus the canonical build on the exact operating code where feasible. Review inventory/mapping/report stages manually. After cutover query stream/constraint/projection counts and representative records, canonical queue reasons/order, rendered Markdown metadata, new context mode and source hashes/Git status. Exercise `next` and one safe service-unavailable observation without destructively stopping unrelated services.

Record which checks are automated, manually observed, inherited or incomplete. Surface every warning. If any expected count/digest/result differs, stop before approval/cutover or before first database write as applicable and follow the accepted recovery boundary; do not label failed/partial evidence as success. Clean only two rehearsal resources whose CutoverIds/ownership are proven and whose evidence no longer requires them.

## Coordination

TASK-00098 through TASK-00103 provide the only authorized inventory, mapping, rehearsal, approval, cutover, authority and consumer operations. TICKET-00029 requires this accepted cutover for final Dashboard operation. Source retirement is later independent work with its own dry run, branch, PR and human merge; it is not implied by completing this TASK.

This is an operational human-action TASK, not implementation authorization. Before execution, identify the exact operating checkout, source commit, production database target, backup readiness, maintenance window and responsible human. No planning file edit, branch publication, PR merge, retirement or cleanup beyond ownership-proven rehearsal resources is authorized implicitly.

## Completion notes

Not started. After cutover, record completion and evidence through the database-authoritative TASK aggregate rather than editing this frozen source file. Include exact source/rehearsal/report/cutover/context/queue/render/source-integrity evidence, counts/durations, warnings, first-write acknowledgment, retained/cleaned resources and deferred Dashboard/retirement work.
