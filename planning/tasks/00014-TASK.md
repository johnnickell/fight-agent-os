---
id: TASK-00014
ticket: TICKET-00009
kind: feature
title: Persist activation grants and replacement races
status: done
order: 14
blocked_by: TASK-00013
pr: https://github.com/johnnickell/fight-agent-os/pull/23
---

# Persist activation grants and replacement races

## Outcome

A production PostgreSQL adapter satisfies the stable `ActivationGrantRepository` contract with secret-safe storage, exact reconstruction, latest/delivery lookup, atomic replacement and succession, and controlled race behavior.

## Scope

- In scope: Activation-grant migration/mapping; complete production repository adapter and composition; package-approved credential digest and safe delivery/lifecycle state; stable, delivery, latest-user lookups; add, replace, replacement-with-successor, and successor insertion; reference/uniqueness/ordering constraints; rollback and controlled concurrency tests.
- Out of scope: Invitation commands/orchestration, credential generation, provider delivery, activation HTTP/UI, password policy, raw grant persistence, password-reset grants, and admin operations.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Add activation grant | Package invitation behavior later uses repository | Resolve user/reference validity | Package invitation events remain package-owned | One secret-safe grant state persists for its user/delivery identity |
| Resolve activation state | N/A — repository read | Find by ID, delivery ID, or latest user grant | N/A — no adapter event | Exact package aggregate is reconstructed with deterministic latest ordering |
| Replace grant | Package activation/delivery behavior | Compare expected predecessor and replacement transition | Events only continue after committed package behavior | One valid expected-state transition commits; stale writers fail without mutation |
| Supersede grant | Package resend behavior | Resolve predecessor/latest state | Package successor outcome remains package-owned | Predecessor and successor change atomically according to repository contract |

## Validation and permissions

Persist no raw activation credential. Store only package-approved one-way credential material and safe lifecycle/delivery metadata. Constraints must enforce stable and delivery identifiers, user references, any credential digest uniqueness, successor relationships, and deterministic latest ordering without making database rows transport models.

Implement every stable `ActivationGrantRepository` method directly. Expected-state operations must compare the complete contract-required predecessor, validate successor ownership and transitions, and use the ADR-selected lock/conditional strategy. Duplicate IDs/delivery identities, stale replacements, invalid successors, and missing users map to safe known outcomes.

No end-user permission or public activation behavior is implemented. Repository/schema/composition changes are authorized; grant issuance, delivery, and consumption orchestration remain later TICKETs.

## Acceptance criteria

- [x] A versioned snake_case migration adds activation-grant storage and authoritative user, unique, ordering, revision/state, and successor constraints.
- [x] No raw activation credential can be recovered from database rows, fixtures, logs, or diagnostics.
- [x] The production adapter implements every stable `ActivationGrantRepository` method directly.
- [x] ID, delivery ID, and latest-user lookups round-trip exact package entities and define deterministic ordering ties.
- [x] Add, replace, `replaceWithSuccessor`, and `addSuccessor` obey expected-state, lifecycle, ownership, and uniqueness contracts.
- [x] Duplicate add, activation/replacement, resend/activation, and competing-successor tests allow one valid authoritative outcome.
- [x] Injected failures prove predecessor and successor changes roll back together without orphans.
- [x] Database conflicts map safely without exposing SQL, credential material, or internal details.
- [x] Focused migration/repository/transaction/concurrency checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Use a clean migrated guarded PostgreSQL database and deterministic users, IDs, clocks, delivery IDs, and credential digests. Exercise every method, absence path, lifecycle transition, ordering case, duplicate/reference violation, and rollback. Coordinate competing connections for replacement and succession races without sleeps. Inspect persisted/logged values to prove raw credential absence.

Run focused database/repository tests and architecture checks, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record migration/constraint details, package/PostgreSQL versions, test/assertion counts, race outcomes, redaction evidence, warnings, and unresolved contract questions.

## Coordination

TASK-00013 supplies authoritative users, shared connection, transaction fences, and guarded fixtures. Preserve package grant ownership and leave durable intent/provider delivery to TASK-00016 and TICKET-00011. TASK-00015 is independent after TASK-00013; do not couple purpose-specific grant tables merely for convenience.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable activation-grant persistence outcome.

## Completion notes

Implemented on `feature/task-00014-activation-grant-persistence` in an isolated worktree. The Doctrine-generated migration `Version20260926002702` creates `activation_grants` with per-user generation and historical digest uniqueness; global grant/delivery identifiers; a user FK and same-owner predecessor FK/unique successor relationship; named revision, credential-shape, terminal, encrypted-material, claim, status and attempt constraints; and due-work indexing. No raw credential is stored: rows contain the package's SHA-256 credential digest and only package-approved opaque encrypted delivery material; terminal states destroy that material. Fixtures generate credentials and opaque delivery material at runtime rather than committing credential literals. The package remains responsible for encrypting material before calling its repository.

`PostgresActivationGrantRepository` implements all seven stable `v0.3.0` methods directly, including bounded, secret-free, deterministic due discovery. Adapter-private supported-constructor hydrators round-trip package entities. Complete predecessor comparison includes owned delivery material, claim state, times, email, revision and all authority fields. Replacement replays package operations to validate exact next transitions. A per-user advisory transaction fence and latest-row lock serialize initial adds, revisions and successors; a user reference lock prevents deletion while writing. Known uniqueness/check conflicts become safe false outcomes, missing users produce a safe conflict, and savepoints plus caller transaction rollback preserve atomic predecessor/successor behavior. No invitation commands, provider effects, HTTP, reset grant adapter or credential-generation policy were introduced.

Changed files: `database/migrations/Version20260926002702.php`, `config/common/persistence.php`, five `src/Adapter/Persistence/` files (records, transitions, two hydrators and repository), `tests/Integration/Postgres/ActivationGrantRepositoryTest.php`, and this TASK plus generated Board, TASK index and TICKET child table. Tests cover absence and exact round-trip; inspected digest/raw absence; ID/delivery/latest lookup; due ordering and limit, claimed lease, retry, expiry and terminal states; duplicate grant/delivery/digest/owner rejection; complete-state forgery and skipped revision; initial, replacement, resend/activation and competing successor fences on real independent PostgreSQL connections; and injected insert/caller rollback without partial rows.

Fresh preliminary verification on the isolated project: guarded `./bin/database test-reset` applied 4 migrations / 23 SQL queries from zero, and `./bin/database test-status` reported 4 executed, 0 unavailable, 0 new; PostgreSQL 17.11 and PHP 8.5.10. `./bin/database test` passed 47 tests / 768 assertions; `./bin/build` passed Composer validation, planning validation (154 records / 127 active before closeout), Deptrac (0 violations, skipped, uncovered, warnings or errors; 411 allowed) and PHPUnit 51 tests / 805 assertions. `git diff --check` and `./bin/planning-check` passed. Docker printed startup/health messages, and Doctrine printed ordinary migration notices; no PHPUnit skips, warnings, failures or deprecations in the successful runs. Earlier iteration failed on two test-fixture mistakes (a reused digest and Doctrine's expected closed EntityManager after injected rollback); both were corrected, and an initial build could not access the host Git worktree metadata inside Docker. A TASK-owned ignored Compose override read-only mounts the base Git directory for the canonical build; no tracked infrastructure files were changed. Following status closeout, `./bin/planning-check --write` refreshed generated views (154 records / 126 active); `./bin/planning-check` and `git diff --check` passed. After adding the same-owner predecessor relationship and observable successor-link checks, a second guarded `./bin/database test-reset` applied 4 migrations / 23 SQL queries from zero and `./bin/database test-status` reported 4 executed / 0 unavailable / 0 new. The guarded `./bin/database test` passed 47 tests / 770 assertions; `./bin/planning-check` (154 / 126) and `git diff --check` passed; `./bin/build` passed Composer validation, planning (154 / 126), Deptrac (0 violations / skipped / uncovered / warnings / errors; 412 allowed) and PHPUnit (51 tests / 807 assertions). No test skips, warnings, failures or deprecations occurred in this final application-code run. Doctrine migration notices remain informational.

Independent review: canonical ignored `.runs/reviews/TASK-00014/review.md` (sequence 1) independently **accepted** the implementation at base `46f8b9c9b852f29a98da88a5ffc39a7ee170a0d2`, head `40fbc95461e17659f9ca0178dfd72723c05c1954`, clean status. No blocking or non-blocking findings require disposition. Its limits remain: provider encryption/logging, populated upgrades and sustained load were outside this repository review.

Landing reconciliation: `develop` advanced to `4894bb9cf10359bf1d60e770abf1468d196d93cc` with independently completed TASK-00015 and the workflow-policy update. Before publication the unpublished, clean activation branch was rebased to `1c10447df664c3a6bdd1a84586b501750b1844f2`; the original report remains unchanged and is provenance for the original implementation, not an assertion that it reviewed the new OID. `git range-diff` and direct blob comparison showed all seven activation migration/source/test files unchanged. The only shared production conflict was `config/common/persistence.php`: both independent repository registrations now use the same shared connection. The other three conflicts were generated Board/index/parent-child tables; `./bin/planning-check --write` regenerated these from the two completed TASK records (154 records / 125 active). No contract or dependency lock changed; TASK-00015's separate reset table/adapter remained untouched. The integration preserved both grant families' behavior.

Fresh reconciliation verification on the TASK-specific guarded Compose test database: `./bin/database test-reset` migrated five versions / 25 SQL queries from zero; `./bin/database test-status` reported five executed, zero unavailable/new; PostgreSQL 17.11 and PHP 8.5.10. `./bin/database test` passed 57 tests / 903 assertions, including activation and reset contracts. `./bin/planning-check` passed (154 / 125), `git diff --check develop...HEAD` passed, and `./bin/build` passed Composer manifest, planning, Deptrac (0 violations/skipped/uncovered/warnings/errors; 501 allowed) and PHPUnit (61 tests / 940 assertions). Docker health/startup output and informational Doctrine migration notices appeared; no test skips, failures, warnings or deprecations appeared. These are post-rebase receipts, not the earlier independent review's receipts.

Publication: the feature branch was pushed without force and [PR #23](https://github.com/johnnickell/fight-agent-os/pull/23) opened against `develop` at initial published head `58fe4fb54e1ce1698abd6af4b85cf166846a7e77`. The PR's remote head must match the final post-metadata commit; initial publication alone does not establish that or imply human approval. Cleanup preview identified TASK-specific Compose containers and disposable ignored `vendor/` and `var/cache/` under its isolated worktree. The task-scoped development PostgreSQL volume may hold durable local state and is retained unless proven disposable; ignored review and notes are retained as evidence. The clean isolated worktree is eligible for removal only after remote-head verification.

Residual limits: concurrency evidence uses bounded lock timeouts and serial retries, not sustained load; external provider encryption/delivery and populated external-schema migration are not exercised here. Human approval/merge remain separate; no approval, merge, release or deployment is claimed. The base checkout, other branches and review history remain untouched.
