---
id: TASK-00009
ticket: TICKET-00008
kind: chore
title: Accept the application ownership and orchestration ADR
status: done
order: 9
blocked_by: TASK-00008
pr: https://github.com/johnnickell/fight-agent-os/pull/13
---

# Accept the application ownership and orchestration ADR

## Outcome

An explicitly accepted architecture decision defines ownership between Fight Access Control and Fight Agent OS, the narrow role of application orchestration, dependency direction, CQRS composition, HTTP interaction boundaries, authorization ownership, and transaction/effect constraints before affected production scaffolding begins.

## Scope

- In scope: Create and obtain explicit acceptance for a concise ADR covering direct Fight Access Control Domain/Application use; justified `App\Application` orchestration; Domain/Application/Adapter and composition-root dependencies; capability-oriented adapters; command/query/event dispatch; one-Action/one-interaction Action–Domain–Responder delivery; server-side authorization; transaction/external-effect boundaries; structural enforcement and deferred checks.
- Out of scope: Production scaffolding or representative code, persistence schemas, HTTP representations, authentication/invitation endpoints, React behavior, event sourcing, architecture-tool installation, upstream package changes, and speculative contexts or abstractions.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Record package ownership | N/A — ADR work does not dispatch application commands | Inspect EPIC/WF decisions and Fight Access Control public Domain/Application contracts | N/A — no application domain event | ADR distinguishes package-owned behavior/types from Agent OS integration and genuine orchestration |
| Define dependency and composition rules | N/A — architecture decision | Inspect current source/configuration, CQRS buses, framework boundaries, and expected adapters | N/A — no application domain event | Allowed dependencies, composition exceptions, adapter capabilities, and prohibited directions are explicit |
| Define HTTP and authorization boundaries | N/A — architecture decision | Inspect accepted Action–Domain–Responder and server-authorization requirements | N/A — no application domain event | Transport mapping, one interaction per Action, response ownership, safe Views, and authorization responsibility are explicit |
| Accept the decision | N/A — human architecture decision | Review context, alternatives, consequences, enforcement plan, and deferred concerns | N/A — no application domain event | ADR status records explicit acceptance before production structure depends on it |

## Validation and permissions

The ADR must state that Fight Access Control owns identity and authorization Domain/Application commands, queries, entities, policies, Views, events, and capability contracts. Agent OS uses those types directly and owns framework integration, persistence, transport, composition, external effects, and only genuine application-specific or cross-boundary orchestration. `App\Application` is justified by behavior spanning ownership boundaries or application policy, never by a renaming wrapper.

Domain cannot depend on Application, Adapter, frameworks, persistence, HTTP, or external providers. Application may depend on Domain/package Domain/Application contracts and capability abstractions but not Adapter/framework implementations. Adapters implement capabilities and depend inward. Configuration/composition may know concrete outer and inner types. Business code uses injected dependencies rather than service location.

Actions map transport input and dispatch one command, query, or explicit orchestrator; they contain no business or persistence policy. Endpoint Responders own successful safe representations. Package entities, hashes, grants, credentials, and secrets are not transport models. Authorization remains server-side and permission/ownership based, with no role-name bypass.

Transactions contain business state and required durable intent, not email/HTTP/filesystem effects. Post-commit delivery and recovery belong to later requirements. The ADR must identify what architecture tooling can enforce now, what requires review or later representative code, and the owning downstream TICKET.

No runtime permission model applies. Architecture documentation and planning evidence are authorized; production structure and implementation are not.

## Acceptance criteria

- [x] A numbered ADR records context, decision, considered alternatives, consequences, enforcement approach, and deferred concerns.
- [x] The ADR explicitly assigns identity/authorization Domain/Application ownership to Fight Access Control and integration/persistence/transport/composition/effects ownership to Agent OS.
- [x] Direct package-type use and the narrow test for justified `App\Application` orchestration are clear and illustrated without creating speculative wrappers.
- [x] Domain/Application/Adapter dependency direction, capability-oriented adapters, constructor injection, and composition-root exceptions are explicit.
- [x] CQRS command/query/event ownership and dispatch rules preserve package contracts and actual event ownership.
- [x] One-Action/one-interaction, Responder, safe View, validation/error, and server-authorization boundaries are stated without pre-deciding feature representations.
- [x] Business transactions exclude direct external effects and preserve later durable-intent/recovery decisions.
- [x] Prohibited renaming wrappers, service location, role-name bypasses, entity exposure, framework imports inward, and speculative abstractions are explicit.
- [x] Enforcement responsibilities and currently non-automatable rules are assigned to TASK-00010 or later owning TICKETs.
- [x] A human explicitly accepts the ADR before TASK-00010 begins structural production work.

## Verification and evidence

Trace each ADR statement to EPIC-00003, WF-004, reviewed package public contracts, current repository boundaries, and downstream TICKET responsibilities. Check that examples use repository terminology and do not invent application behavior. Record reviewer feedback, revisions, explicit human acceptance, and any deferred enforcement gap.

Run `./bin/planning-check --write`, `./bin/planning-check`, link/path checks appropriate to the ADR, `git diff --check`, and `./bin/build`. Record fresh output/test counts and warnings even though this TASK changes architecture documentation rather than application behavior. Verify the ADR directly; do not add product-suite tests for Markdown.

## Coordination

TASK-00008 must first establish the truthful Agent OS baseline. TASK-00010 is blocked until this ADR is explicitly accepted. Keep draft discussion or comparison notes beneath a TASK-owned `.runs/` path; the accepted decision belongs in `planning/adr/`.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable architecture-decision outcome and does not authorize the affected production structure.

## Completion notes

Created and accepted [ADR 0001](../adr/0001-application-ownership-and-orchestration.md). It assigns Fight Access Control ownership of identity/authorization Domain and Application contracts; assigns Agent OS integration, persistence, transport, composition, external effects, and narrowly justified cross-boundary orchestration; defines the four-part `App\Application` orchestration test; records inward dependencies, CQRS/event ownership, one-Action/one-interaction delivery, safe Views/Responders, server authorization, and transaction/effect boundaries; and explicitly prohibits renaming wrappers, business-code service location, role-name bypasses, entity/secret exposure, inward framework imports, direct transactional external effects, and speculative abstractions.

The alternatives considered and rejected were recreating package behavior under `App`, wrapping every package contract, placing all coordination in package handlers, placing workflow/authorization in Slim Actions, and performing provider effects inside business transactions. The installed Fight Access Control `UnitOfWork` compatibility contract is recorded as an explicit TICKET-00009 reconciliation item rather than hidden behind an Agent OS alias.

During human review, the maintainer requested an exact pre-submit direction. ADR 0001 and TASK-00030, TASK-00031, and TASK-00033 now require PHP syntax linting, the standard shipped by locked Fight Common 1.2, PHPStan, Deptrac, Rector dry-run, a thin `bin/build` delegating through `docker compose exec -T web php scripts/build.php` to the already-running Compose PHP service, one coverage-enabled PHPUnit invocation that runs Unit/Integration/Functional/PostgreSQL-backed suites exactly once, bounded frontend checks, no new Python quality-gate orchestration, and one-time rather than recurring seeded-failure evidence. The maintainer explicitly accepted the revised ADR on 2026-09-23.

Changed authored files are ADR 0001, the ADR index, this TASK, and TASK-00030/TASK-00031/TASK-00033. Refreshed generated views are the TASK Board/index and TICKET-00008 child table. No production source, schema, endpoint, dependency, or tool configuration was added.

Fresh final-record verification passed: `./bin/planning-check --write` refreshed generated views and read-only `./bin/planning-check` reported 95 records / 81 active; a direct ADR/planning path check validated 15 local links; and `git diff --check` passed. The canonical `./bin/build` passed on PHP 8.5.10: strict Composer validation, the locked 93-package development graph, audit with no known vulnerability advisories, current planning validation, PHPUnit with 4 tests / 37 assertions, clean 67-package production install, production dependency/source checks, exact-case and case-variant copied-namespace rejection for Fight Common and Fight Access Control, source-directory symlink rejection, and final clean contract proof. Composer emitted informational funding notices for 69 development and 45 production packages. No PHPUnit warning, skip, deprecation, security advisory, required-check failure, or unresolved ADR acceptance item was observed.

TASK-00010 owns representative composition and initial architecture enforcement. TICKET-00009 through TICKET-00011 own persistence/transaction reconciliation, HTTP boundaries, and recoverable delivery; TICKET-00013 owns the complete accepted pre-submit gate. Semantic orchestration, Action, authorization, event-ownership, and anti-speculation rules remain review-enforced until representative code exists.

Independent review sequence 1 accepted implementation commit `11b56f57b4276f438f1b97fceb947722005fce09` against `develop` commit `edb13528ccce5b97d456a19663aea5e2a98702f3`. The review reported no blocking or non-blocking findings. It retained the evidence limitations that maintainer acceptance is durable in this ADR/TASK rather than a separate conversation artifact, the development install reused the ignored vendor tree while production performed a fresh install, audit results are time-sensitive, and representative runtime boundaries intentionally belong to later work. Residual risk remains that semantic rules require review until representative checks exist and future package upgrades may require reconciliation.

Fresh landing pre-publication verification passed on the accepted implementation head: the focused documentation check validated 16 local links with zero failures; `./bin/planning-check` reported 95 records / 81 active with current generated views; range and worktree `git diff --check` passed; and `./bin/build` passed on PHP 8.5.10 with strict Composer validation, the locked 93-package development graph, no known vulnerability advisories, 4 PHPUnit tests / 37 assertions, a fresh 67-package production install, positive source contracts, and all intended copied-namespace and source-symlink rejection probes. Composer emitted funding notices for 69 development and 45 production packages; no PHPUnit warning, skip, deprecation, advisory, required-check failure, or tracked mutation was observed.

Landing commit `b129f90777ff09eef6f56fd4c8dcdbefc061d974` recorded the accepted review and pre-publication verification. The branch was pushed without force and [PR #13](https://github.com/johnnickell/fight-agent-os/pull/13) was opened against `develop` with feature head `feature/application-ownership-adr`; its initial remote head matched that landing commit.

Fresh publication-metadata verification then passed after recording the canonical PR URL and refreshing generated views: the focused documentation check again validated 16 local links with zero failures; `./bin/planning-check` again reported 95 records / 81 active; range and worktree `git diff --check` passed; and `./bin/build` again passed with 4 PHPUnit tests / 37 assertions, no known vulnerability advisories, a fresh 67-package production install, and the expected source-contract probes. Funding notices remained the only warning. The metadata commit is created after this authoritative record; its final non-force push and remote-head confirmation remain pending at this record stage.

After final remote-head verification, landing will remove the registered clean isolated worktree and its ignored root-owned `vendor/` and `var/` build output through an exact-worktree helper if required. The shared base Compose service and common build image, all ambiguous or unrelated scratch, the independent review reports, the base worktree, and the unrelated planning worktree are retained. Human approval and merge, later branch deletion, archive, release, deployment, and certification remain explicitly unclaimed and unauthorised.
