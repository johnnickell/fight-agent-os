---
id: TASK-00069
ticket:
kind: bug
title: Persist and discover every implementation review handoff
status: in-progress
order: 4
blocked_by:
pr:
---

# Persist and discover every implementation review handoff

## Outcome

Every completed `review` invocation atomically writes its full report to the base worktree at
`.runs/reviews/<TASK-ID>/review.md` before returning a verdict, and the `work` workflow automatically locates,
validates, and consumes that canonical handoff when revising an implementation. A later session can continue from the
review findings without relying on chat history or searching linked worktrees.

## Scope

- In scope:
  - Update `.pi/skills/review/SKILL.md` and `docs/engineering/REVIEW.md` to require the complete final report at the
    canonical base-worktree path before the reviewer returns its response.
  - Define base-worktree discovery from both the primary checkout and linked worktrees without assuming the current
    working directory is the base worktree.
  - Require an atomic write, a stable `review.md` filename, exact TASK/target/base/head identity in the report, and an
    explicit final-response handoff containing the canonical path.
  - Fail closed: if the report cannot be written and read back, do not present the review as a completed handoff or
    return an actionable acceptance verdict only in ephemeral chat.
  - Update `.pi/skills/work/SKILL.md` and `docs/engineering/EXECUTION.md` so intake checks the canonical review path,
    validates that the report belongs to the requested TASK and reviewed target, and uses a `revise` report as the
    authoritative correction handoff.
  - Preserve first-time implementation behavior when no review exists, and reject stale, mismatched, partial, or
    ambiguous review files instead of silently applying them.
  - Reproduce the TASK-00005 incident and prove the corrected review-to-work handoff from a linked target worktree.
- Out of scope:
  - Fixing TASK-00005's implementation findings, changing its verdict, or treating the recovered report as acceptance.
  - Committing review reports, moving them into authoritative planning, or requiring chat/session history retention.
  - Changing design-review, landing, publication, merge, release, or deployment workflows.
  - Adding product-suite tests for Markdown workflow instructions.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Persist an implementation review | `/skill:review TASK-NNNNN` | Read TASK, target, evidence, Git common directory, and base worktree | N/A — repository workflow | Complete report is atomically written and read back at base `.runs/reviews/TASK-NNNNN/review.md` before the verdict response |
| Resume rejected work | `/skill:work TASK-NNNNN` | Locate canonical review, validate TASK and reviewed target identity, and read blocking findings | N/A — repository workflow | Revision scope starts from the persisted `revise` handoff without requiring transcript context |
| Reject unusable handoff | N/A — workflow validation | Inspect file existence, completeness, identity, target head, and verdict | N/A — repository workflow | Missing-on-required-revision, stale, mismatched, partial, or unreadable reports stop with the exact path and reason |

## Validation and permissions

Review remains read-only over implementation and planning files. It may create its owned directory and report only
beneath the base worktree's ignored `.runs/reviews/` root. Resolve the base worktree through inspected Git worktree
metadata; never infer it from an arbitrary parent path, write into the linked target worktree by default, or search
unbounded home/project directories.

The report must not contain secrets, credentials, private production data, or unrelated worktree content. Create
parent directories safely, write through a report-owned temporary file, rename atomically, read the final file back,
and verify that it contains the complete ordered review sections and verdict. On any path, identity, write, rename, or
read-back failure, preserve the target and state that no completed review handoff exists.

`work` receives read authority for the canonical report, not authority to alter review findings. It must distinguish a
missing report during first implementation from a missing report during an explicitly requested revision. A report
for another TASK, base/head, or superseded target is not applicable evidence.

## Acceptance criteria

- [x] `review` always resolves the base worktree and writes the complete report to
      `.runs/reviews/<TASK-ID>/review.md` before returning a completed verdict.
- [x] Report publication is atomic and verified by read-back; write failure prevents an ephemeral-only completed
      handoff and names the exact failed canonical path.
- [x] The persisted report contains the required ordered sections, TASK identity, exact target/base/head and status,
      findings, evidence, verification, limitations, risks, and verdict.
- [x] The final review response prominently returns the canonical report path so a human or later agent can open it
      directly.
- [x] `work` automatically checks the canonical path, validates report identity and reviewed target, and consumes a
      matching `revise` verdict and its blocking findings as revision input.
- [x] First implementation may proceed when no report exists; requested revision fails closed for a missing,
      unreadable, partial, stale, mismatched, or ambiguous report.
- [x] A linked-worktree demonstration proves the report is written under the base worktree rather than the target
      worktree and that a fresh `work` invocation can locate it without transcript context.
- [x] The TASK-00005 failure is recorded as the regression: the reviewer originally returned findings only in chat,
      forcing manual recovery to `.runs/reviews/TASK-00005/review.md` in the base worktree.

## Verification and evidence

Before repair, preserve a focused reproduction showing that the current `review` skill ends at “Stop at the report”
without requiring a file write and that `work` has no canonical review lookup. Use the TASK-00005 incident as
operational evidence; do not delete or rewrite its recovered base-worktree report.

After repair, run focused direct checks and a disposable linked-worktree exercise covering base-root resolution,
canonical path creation, atomic replacement, read-back, complete report sections, final path handoff, automatic work
lookup, first-implementation absence, and rejection of stale/mismatched/partial reports. Keep demonstration scratch
under `.runs/`; verify documentation directly rather than adding product tests for Markdown.

Run `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record fresh exit
results, assertion/test counts, warnings, skips, evidence limitations, changed files, preserved unrelated work, and
independent review state.

## Coordination

This standalone bug is one independently reviewable workflow correction and needs no SUBTASK split. Before
implementation, choose the main checkout or an isolated worktree from `develop` and preserve the base checkout's
unrelated Wayfinder work. The implementation may read the recovered TASK-00005 report as regression evidence but
must not modify TASK-00005's feature worktree or findings.

## Completion notes

Implementation is complete on `feature/task-00069-persist-review-handoffs` from `develop` at
`76bff8914d077be6d702eb058cc34b45c22bf597`. TASK-00069 was not yet present on `develop`; with explicit user
authority, its exact source from planning commit `4086d24` was copied into this branch and generated views were
refreshed. A direct cherry-pick first conflicted only in generated Board/index views, so that owned operation was
aborted before the source record was copied and views were regenerated.

The pre-repair reproduction confirmed that `review` ended at “Stop at the report” without canonical persistence and
that `work` had no canonical review lookup or revision handoff. The TASK-00005 incident remains the regression:
findings originally existed only in chat and required manual recovery to the base checkout at
`.runs/reviews/TASK-00005/review.md`. That report was not modified; its observed SHA-256 remained
`0345664d19fa4ada7ebbb42678b4c72b48f6e3305ee3fd4b32b02897961673ed`.

Base discovery now canonicalizes `git rev-parse --path-format=absolute --git-common-dir`, enumerates only registered
worktrees, and accepts the sole worktree whose canonical absolute Git directory equals the common directory after
verifying its top level. It rejects bare, absent, duplicate, malformed, or untrusted results and never infers the base
from the current directory, parent paths, or list order. `review` publishes version-1 YAML-identified reports through
a same-directory restrictive temporary file, atomic rename, and final read-back before returning the verdict and
absolute path. `work` checks that exact path automatically, permits absence only for first implementation, validates
the complete report and target snapshot, consumes matching `revise` blockers, and rejects unusable or accepted/stale
handoffs rather than searching chat or worktrees.

Changed tracked files are `.pi/skills/review/SKILL.md`, `.pi/skills/work/SKILL.md`,
`docs/engineering/REVIEW.md`, `docs/engineering/EXECUTION.md`, this TASK source, and generated
`planning/tasks/BOARD.md` and `planning/tasks/README.md`. No product code or product-suite test changed.

Fresh verification before this completion update:

- `python3 .runs/notes/TASK-00069/verify_docs.py` — exit 0, 38 direct documentation assertions.
- `python3 .runs/notes/TASK-00069/verify_handoff.py` — exit 0, 16 assertions in a disposable repository with a linked
  target worktree. It selected the separate base, atomically replaced and read back the canonical report, preserved a
  prior report on a simulated pre-rename failure, located the report from a fresh invocation, permitted first-use
  absence, and rejected TASK mismatch, stale head, and a partial report.
- `./bin/planning-check --write` and `./bin/planning-check` — exit 0, 95 records / 86 active; views refreshed and then
  current.
- `git diff --check` — exit 0 with no output.
- `./bin/build` — exit 0; planning validation passed, PHPUnit passed 26 tests / 152 assertions, and the production
  Composer dependency contract passed.

Composer warned that `johnnickell/fight-common` uses a commit reference and reported 69 development / 45 production
funding notices. No deprecations or required-check skips were reported. The build produced ignored `vendor/` and
root-owned `var/` output in the isolated worktree; status inspection reported `var/storage/` as unreadable. Those
owned build outputs are retained for later bounded landing cleanup. The first scratch documentation check had one
false negative because its phrase match was narrower than the documented `canonical_report` key; the checker was
corrected and the 38-assertion rerun passed.

The linked-worktree exercise is a workflow simulation because Pi skills are interpreted instructions rather than an
executable product API; it did not force an operating-system rename or read-back error, but it proved preservation on
a simulated pre-rename failure and the documented failure path is fail-closed. Dirty-target manifest behavior is
defined but the demonstration used a clean target. User-confirmed unrelated changes in the primary checkout's
Wayfinder map and WF-012 record were preserved. This record is part of the implementation commit reported in the
handoff. Independent review is pending; no push, PR, landing, merge, archive, release, or deployment has been claimed
or authorized.
