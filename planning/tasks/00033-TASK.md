---
id: TASK-00033
ticket: TICKET-00013
kind: chore
title: Make bin build the complete read-only application gate
status: ready-for-agent
order: 33
blocked_by: TASK-00031, TASK-00032
pr:
---

# Make bin build the complete read-only application gate

## Outcome

`./bin/build` is the thin sole canonical entrypoint into a deterministic, non-interactive, PHP-orchestrated application acceptance gate running in the already-started Docker Compose PHP service. Named phases validate the Composer manifest, planning, PHP syntax/style/types/dependencies, one all-suite PHPUnit/coverage execution, and bounded frontend source/behavior/build checks without testing the gate itself.

## Scope

- In scope: Extend the thin `bin/build` and `scripts/build.php` introduced by TASK-00070; explicit named phase ordering and failure propagation; Composer manifest/lock validation without install or audit; read-only planning validation; PHP syntax/PHPCS Fight Common 1.2/PHPStan/Deptrac/Rector dry-run checks; one all-suite backend PHPUnit/coverage run with guarded PostgreSQL; bounded frontend format/lint/type/test/coverage/build/Storybook checks; tracked-tree mutation detection; final evidence format and documentation of focused-versus-canonical use.
- Out of scope: Automatic source/planning formatting, dependency updates, merge/release/deployment approval, provider/network product tests, lowest/candidate lanes, scheduler enrollment, broad E2E automation, and silently accepting warnings or unavailable mandatory phases.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Run canonical gate | N/A — thin `./bin/build` delegates to `scripts/build.php` inside the running Compose PHP service | Inspect locks, planning, owned source/tests, database, dependencies, and production artifact | N/A — no domain event | All required phases pass once or the gate exits nonzero at a clearly named phase |
| Prove read-only behavior | N/A — gate invariant | Compare tracked state before/after while allowing ignored caches/reports | N/A — no domain event | Tracked planning/source/locks remain unchanged |

## Validation and permissions

`bin/build` contains only host-side preflight and delegation: it verifies that the Compose `web` PHP service is running, then uses non-interactive `docker compose exec -T web php scripts/build.php`. It does not build an image or start an ad hoc container on every invocation. A missing/stale service fails with an actionable instruction rather than silently changing the environment.

The PHP orchestrator owns named phase execution, exit-code propagation, and concise diagnostics. It may use the locked Symfony Process capability, but must not duplicate orchestration in shell or add another task-runner dependency without demonstrated need. Existing `./bin/planning-check` remains the authoritative planning phase and may retain its implementation language.

The gate assumes dependencies were installed during explicit setup. It never installs, updates, or audits dependencies; invokes `planning-check --write`; runs format fixers; generates tracked source; or migrates a non-test database. Composer validation may lint the manifest and verify lock consistency without network access. Caches/reports/databases stay under ignored or disposable paths.

Run read-only planning validation, PHP syntax lint, PHPCS using the standard shipped by locked Fight Common 1.2, PHPStan, Deptrac, and Rector dry-run. Then run one PHPUnit invocation containing all configured Unit, Integration, Functional, and guarded PostgreSQL-backed suites with coverage enabled; validate Domain/Application and Adapter coverage from that same report without rerunning any suite. Run each accepted frontend format/lint/type/Vitest/coverage/production-build/Storybook phase once. Order should fail fast with useful diagnostics.

`./bin/build` is canonical. Focused wrappers remain supported iteration tools but cannot be cited instead of the full gate and are not called as duplicate passes by the orchestrator. The gate and its tools are inspected and invoked directly; no product test or seeded-invalid scenario may test whether they detect failures. The gate reports/fails; it cannot approve review, merge, release, or deployment.

## Acceptance criteria

- [ ] Thin `./bin/build` verifies the already-running Compose PHP service and delegates non-interactively to `docker compose exec -T web php scripts/build.php` without rebuilding a one-off image.
- [ ] The PHP orchestrator runs every accepted backend, frontend, planning, dependency, database, and coverage phase once with named diagnostics and correct exit propagation.
- [ ] No new Python quality-gate orchestration is introduced; the existing authoritative planning tool remains a called phase.
- [ ] The gate is containerized, deterministic, non-interactive, clearly phased, fail-fast where safe, and returns nonzero on any mandatory failure.
- [ ] Composer validation is network-free and performs no install, update, or audit; planning runs read-only and no fixer rewrites source.
- [ ] Generated caches/reports/artifacts/temp production trees and test databases remain ignored/disposable and are cleaned appropriately.
- [ ] Guarded PostgreSQL lifecycle cannot target known development/production databases and cleans isolated test state safely.
- [ ] Backend syntax, Fight Common 1.2 PHPCS, PHPStan, Deptrac, Rector dry-run, behavior, coverage, and PostgreSQL checks enforce TASK-00030/00031 policies.
- [ ] One coverage-enabled PHPUnit invocation runs every configured Unit, Integration, Functional, and PostgreSQL-backed suite exactly once; coverage validation consumes its report without a duplicate test pass.
- [ ] Frontend source, behavior, coverage, build, Storybook, artifact, and dependency checks enforce TASK-00032 policy.
- [ ] Product suites contain no tests of architecture, configuration wiring, migrations, wrappers, build/planning/static-analysis mechanisms, generated files, test infrastructure, or deliberately seeded tool failures.
- [ ] Running the successful gate leaves tracked status identical to its recorded starting state.
- [ ] Fresh output/evidence records versions, test/assertion/story counts, coverage, migration/database outcomes, useful duration, warnings, and exclusions.

## Verification and evidence

Run the canonical gate through the thin host entrypoint from the authorized working state and preserve before/after tracked-status evidence. Inspect the running-service preflight, PHP phase diagnostics, phase list, cache/report/database locations, and product-test inventory directly. Do not seed failures or add tests for wrappers, orchestration, configuration, or quality tools.

Run focused checks while iterating, then final `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and the completed `./bin/build`. Record the exact command/environment, phase list, tool/runtime versions, counts/coverage, database results, duration where useful, warnings, exclusions, and unchanged tracked-state receipt. Do not present inherited receipts as fresh evidence.

## Coordination

TASK-00031 and TASK-00032 provide accepted backend/frontend policies and focused commands. Preserve TASK-00007's stable dependency decisions and TASK-00008's truthful HTTP smoke behavior. TASK-00070 supplies the thin wrapper, PHP orchestrator, and anti-self-testing baseline. The final gate is necessary acceptance evidence but does not replace independent review or controlled landing.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable canonical-gate outcome.

## Completion notes

Not started. Record final phase graph/commands, database results, changed files, full verification with counts/coverage/durations, warnings, review state, commit/PR details, and unresolved operational limitations here.
