---
id: TASK-00057
ticket: TICKET-00020
kind: feature
title: Deliver authoritative owned-session visibility
status: ready-for-agent
order: 57
blocked_by: TASK-00051
pr:
---

# Deliver authoritative owned-session visibility

## Outcome

The authenticated Active Sessions route and versioned API derive actor, owner, and current session from `CurrentPrincipalProvider`, use package `ListActiveSessions` to return only usable owned refresh sessions, and present authoritative creation/activity/idle/absolute/remembered/current state with deliberately coarse bounded device metadata captured at Agent OS session creation.

## Scope

- In scope: Coarse session-presentation metadata model/persistence/migration/parser; activation/login metadata capture and safe missing-data fallback; authenticated paginated listing Action/query mapping/explicit View/OpenAPI; principal-derived actor/user/current session; fixed deterministic ordering/bounds; typed client feature service; replacement Active Sessions route/page; current/remembered/device/activity/expiry presentation; loading/empty/error/pagination states; PostgreSQL/HTTP/client/component/accessibility/redaction tests.
- Out of scope: Raw User-Agent/IP/geolocation/fingerprinting, credential or used-digest exposure, current/other-session revocation, cross-user administration, logout everywhere, editing session lifetimes, live presence claims, background tracking, broad browser automation, and changing package session authority.

## Use cases and contracts

| Use case | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|
| Capture coarse presentation metadata | N/A — Agent OS session-response orchestration | Parse bounded request presentation input after successful activation/login session creation | N/A — no domain event | Idempotent sidecar metadata records browser family/device class or safe unknown for stable session ID |
| List owned active sessions | N/A — read-only API | `ListActiveSessions(actorId, userId, currentSessionId, pagination)` | N/A — no domain event | Explicit paginated View returns only current owner's usable sessions and safe presentation metadata |
| Identify current session | N/A — query input | Read refresh-session ID from request-scoped current principal | N/A — no domain event | Exactly the session carrying authenticated request is marked current; no client/cookie claim is trusted |
| Render session security page | N/A — client read | Load typed active-session page through shared API client | Client cache/navigation only | User sees truthful coarse activity/lifetime data and current marker without credentials or presence inference |

## Validation and permissions

Require TASK-00044 Bearer authentication and resolve one request-scoped `CurrentPrincipalProvider`. Construct package `ListActiveSessions` with actor ID and user ID both equal to the principal user and current-session ID equal to the principal refresh-session ID. Request input may supply only bounded pagination from an allowlist; it cannot select actor/user/current session, administrative mode, arbitrary ordering, or internal fields. No managed permission applies because authenticated ownership is the authority.

Use package handler/repository for usable-session selection and `SessionView` creation. Map through an explicit HTTP View rather than returning package objects directly. Include stable session ID required for later revocation, coarse device label/structured fields, `created_at`, `last_activity_at`, `idle_expires_at`, `absolute_expires_at`, `remembered`, and `current`. Omit redundant user ID from public View unless a proven client need is approved. Exclude credential/digests/used digests, authentication version/revisions, revoked flags/history, audit/provider data, cookie values, and raw persistence state. Use no-store/private cache policy as selected by API standards.

Capture session-presentation metadata in an Agent OS-owned sidecar keyed uniquely by stable package session ID when activation/login creates a TokenSet. Define a fixed local parser with strict input length, allowlisted browser family and device class (for example desktop/mobile/tablet/unknown), no browser/OS version, no raw header retention, no IP, no geolocation, no canvas/hardware identifier, and no external enrichment. Treat User-Agent as untrusted presentation input, escape output, and use deterministic `Unknown device` fallback. Refresh cannot silently rewrite the original descriptor. Metadata is non-authoritative: capture failure/missing legacy row cannot invalidate or extend a session and must produce safe unknown presentation. Reconcile/delete retention with session expiry operationally without making metadata an alternate session source.

Activity is package `lastActivityAt` from successful refresh rotation, not page views, access-token requests, websocket activity, or inferred online presence. Explain timestamps/session type truthfully in the UI. Render current session distinctly without relying on color alone. Use bounded pagination and one fixed allowlisted deterministic ordering with stable tie-break; expired/revoked sessions are absent under package clock semantics.

Strictly decode client response, use existing API-cache conventions without persisting rows, and replace TASK-00051's truthful Active Sessions landing state. Recreate accepted list/card/table behavior across narrow/wide modes with semantic headings, timestamp labels, current/remembered explanations, loading/empty/failure/pagination, keyboard/focus, status announcements, target/reflow, themes, and reduced motion.

## Acceptance criteria

- [ ] Versioned no-store listing API requires Bearer authentication and derives actor, owner, and current-session ID only from one current principal.
- [ ] Request cannot select another user/actor/current session, administrative mode, arbitrary sort, or internal field.
- [ ] Package `ListActiveSessions` owns usable-session filtering, ownership query, pagination, and current marker input.
- [ ] Explicit View exposes only stable session ID, coarse device presentation, creation/activity/idle/absolute timestamps, remembered, and current.
- [ ] Credential/digests/used credentials, auth version/revisions, raw User-Agent/IP/location/fingerprint, audit/provider/cookie/internal state are absent.
- [ ] Coarse metadata parser is bounded, allowlisted, deterministic, local, escaped, version-free, and stores no raw header or external-enrichment result.
- [ ] Activation/login integrate idempotent metadata capture without changing package authority; missing/capture-failed metadata safely renders unknown.
- [ ] Refresh does not rewrite original device descriptor and activity remains authoritative successful-refresh time rather than invented presence.
- [ ] Only usable owned sessions appear with bounded pagination, fixed deterministic ordering, and exactly one current marker for valid authority.
- [ ] Typed client service replaces the landing state without a duplicate session/authority store or browser persistence.
- [ ] Responsive loading/empty/current/remembered/pagination/error states meet accepted semantic, keyboard/focus, announcement, theme, target, and reflow requirements.
- [ ] OpenAPI truthfully documents ownership, fields, pagination/order, no-store behavior, and omitted sensitive/fingerprinting data.
- [ ] Focused PostgreSQL/ownership/pagination/metadata/HTTP/client/component/accessibility/redaction checks and `./bin/build` pass with fresh evidence.

## Verification and evidence

Create ordinary/remembered sessions through activation/login with representative bounded desktop/mobile/tablet/unknown/malformed/oversized User-Agent inputs; prove only allowlisted coarse fields persist and raw values/IP/versions do not. Inject metadata failure/missing rows and verify session creation/authority remains valid and listing safely says unknown. Confirm refresh changes package activity/idle only and cannot overwrite descriptor.

List zero/one/many sessions at created/activity/idle/absolute ordering boundaries using deterministic clock and clean PostgreSQL. Exercise pagination bounds/ties, revoked/idle-expired/absolute-expired exclusion, current marker, malformed query, body actor/user/current injection, cross-user attempt, stale principal, and transaction/read failures. Seed credential/digest/token/cookie/raw-UA/IP/SQL markers and inspect response/OpenAPI/logs/audit/correlation/snapshots/DOM/messages/storage.

Run focused backend architecture/style/static/coverage, migration/PostgreSQL/HTTP/OpenAPI/security tests, frontend type/lint/Vitest/RTL/Storybook/build, accessibility automation plus manual keyboard/reflow/theme observations, storage/redaction scans, then `./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and `./bin/build`. Record metadata schema/parser allowlist/retention, API View/order/bounds, tests/assertions/session counts, accessibility limits, redaction evidence, versions, durations, and warnings.

## Coordination

TASK-00051 supplies the registered Active Sessions route/frame; TASK-00049 supplies current principal; TASK-00043/00046 produce activation/login sessions; TASK-00047 owns refresh activity. Store only Agent OS presentation metadata alongside, never inside or instead of package refresh authority. TASK-00058 adds confirmed other-session revocation to this page.

Before implementation, ask whether to use the current checkout or an isolated worktree unless already chosen. This TASK owns one independently reviewable owned-session visibility outcome.

## Completion notes

Not started. Record metadata schema/parser/capture/retention, Action/View/OpenAPI/client/page, ownership/order/pagination/session/accessibility/redaction evidence, changed files, focused/canonical verification with counts/durations, warnings, review state, commit/PR details, and revocation handoff here.
